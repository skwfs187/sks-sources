local zalk = {}
local function addCheck(func)
    table.insert(zalk, func)
end

addCheck(function()
    return game:GetService("ReflectionService")
end)
addCheck(function()
    local v = Vector3.new(1, 2, 3)
    local v2 = Vector3.new(4, 5, 6)
    local dot = v:Dot(v2)
    if dot ~= 32 then
        return false
    end
    local cross = v:Cross(v2)
    if cross.X ~= -3 or cross.Y ~= 6 or cross.Z ~= -3 then
        return false
    end
    local sum = v + v2
    if sum.X ~= 5 or sum.Y ~= 7 or sum.Z ~= 9 then
        return false
    end
    local mag = v.Magnitude
    if math.abs(mag - math.sqrt(14)) > 0.001 then
        return false
    end
    return true
end)
addCheck(function()
    local c = Color3.new(0.5, 0.6, 0.7)
    local c2 = Color3.fromRGB(255, 0, 0)
    if c2.R ~= 1 or c2.G ~= 0 or c2.B ~= 0 then
        return false
    end
    if c.R ~= 0.5 then
        return false
    end
    return true
end)
addCheck(function()
    local cf = CFrame.new(1, 2, 3)
    if cf.Position.X ~= 1 or cf.Position.Y ~= 2 or cf.Position.Z ~= 3 then
        return false
    end
    local inv = cf:Inverse()
    if not inv then
        return false
    end
    return true
end)
addCheck(function()
    local u2 = UDim2.new(0.5, 10, 0.3, 5)
    if u2.X.Scale ~= 0.5 or u2.X.Offset ~= 10 then
        return false
    end
    return true
end)
addCheck(function()
    local ray = Ray.new(Vector3.new(0, 0, 0), Vector3.new(1, 0, 0))
    if ray.Origin.X ~= 0 then
        return false
    end
    return true
end)
addCheck(function()
    local p = Instance.new("Part")
    p.Name = "Test"
    if p.Name ~= "Test" then
        return false
    end
    local child = Instance.new("Part")
    child.Name = "Child"
    child.Parent = p
    if p:FindFirstChild("Child") ~= child then
        return false
    end
    local clone = p:Clone()
    if clone.Name ~= "Test" then
        return false
    end
    clone:Destroy()
    p:Destroy()
    return true
end)
addCheck(function()
    local p = Instance.new("Part")
    local signal = p:GetPropertyChangedSignal("Position")
    if not signal or not signal.IsA or not signal:IsA("RBXScriptSignal") then
        p:Destroy()
        return false
    end
    p:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Accessory")
    if not inst then
        return false
    end
    if inst.ClassName ~= "Accessory" then
        return false
    end
    local props = ReflectionService:GetPropertiesOfClass(
        "Accessory",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
        end
        break
    end
    local methods = ReflectionService:GetMethodsOfClass(
        "Accessory",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AccessoryDescription")
    if not inst then
        return false
    end
    if inst.ClassName ~= "AccessoryDescription" then
        return false
    end
    local props = ReflectionService:GetPropertiesOfClass(
        "AccessoryDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
        end
        break
    end
    local methods = ReflectionService:GetMethodsOfClass(
        "AccessoryDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Accoutrement")
    if not inst then
        return false
    end
    if inst.ClassName ~= "Accoutrement" then
        return false
    end
    local props = ReflectionService:GetPropertiesOfClass(
        "Accoutrement",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
        end
        break
    end
    local methods = ReflectionService:GetMethodsOfClass(
        "Accoutrement",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Actor")
    if not inst then
        return false
    end
    if inst.ClassName ~= "Actor" then
        return false
    end
    local props = ReflectionService:GetPropertiesOfClass(
        "Actor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
        end
        break
    end
    local methods = ReflectionService:GetMethodsOfClass(
        "Actor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AdGui")
    if not inst then
        return false
    end
    if inst.ClassName ~= "AdGui" then
        return false
    end
    local props = ReflectionService:GetPropertiesOfClass(
        "AdGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AdGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AdPortal")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AdPortal" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AdPortal",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AdPortal",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AdService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AdService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AdService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AdService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AdvancedDragger")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AdvancedDragger" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AdvancedDragger",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AdvancedDragger",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AirController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AirController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AirController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AirController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AlignOrientation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AlignOrientation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AlignOrientation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AlignOrientation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AlignPosition")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AlignPosition" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AlignPosition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AlignPosition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnalyticsService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnalyticsService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnalyticsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnalyticsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AngularVelocity")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AngularVelocity" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AngularVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AngularVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Animation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Animation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Animation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Animation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationClip")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationClip" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationClip",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationClip",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationClipProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationClipProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationClipProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationClipProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationFromVideoCreatorService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationFromVideoCreatorService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationFromVideoCreatorService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationFromVideoCreatorService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationGraphDefinition")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationGraphDefinition" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationGraphDefinition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationGraphDefinition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationNode")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationNode" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationNode",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationNode",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationNodeDefinition")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationNodeDefinition" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationNodeDefinition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationNodeDefinition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationRigData")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationRigData" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationRigData",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationRigData",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AnimationTrack")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AnimationTrack" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AnimationTrack",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AnimationTrack",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Animator")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Animator" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Animator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Animator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Annotation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Annotation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Annotation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Annotation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ArcHandles")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ArcHandles" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ArcHandles",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ArcHandles",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AssetDeliveryProxy")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AssetDeliveryProxy" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AssetDeliveryProxy",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AssetDeliveryProxy",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AssetPatchSettings")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AssetPatchSettings" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AssetPatchSettings",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AssetPatchSettings",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AssetService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AssetService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AssetService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AssetService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Atmosphere")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Atmosphere" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Atmosphere",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Atmosphere",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AtmosphereSensor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AtmosphereSensor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AtmosphereSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AtmosphereSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Attachment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Attachment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Attachment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Attachment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioAnalyzer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioAnalyzer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioAnalyzer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioAnalyzer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioChannelMixer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioChannelMixer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioChannelMixer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioChannelMixer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioChannelSplitter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioChannelSplitter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioChannelSplitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioChannelSplitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioChorus")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioChorus" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioChorus",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioChorus",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioCompressor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioCompressor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioCompressor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioCompressor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioDeviceInput")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioDeviceInput" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioDeviceInput",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioDeviceInput",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioDeviceOutput")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioDeviceOutput" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioDeviceOutput",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioDeviceOutput",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioDistortion")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioDistortion" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioDistortion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioDistortion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioEcho")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioEcho" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioEcho",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioEcho",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioEmitter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioEmitter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioEmitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioEmitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioEqualizer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioEqualizer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioEqualizer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioEqualizer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioFader")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioFader" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioFader",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioFader",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioFilter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioFilter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioFilter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioFilter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioFlanger")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioFlanger" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioFlanger",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioFlanger",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioGate")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioGate" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioGate",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioGate",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioLimiter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioLimiter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioLimiter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioLimiter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioListener")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioListener" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioListener",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioListener",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioPitchShifter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioPitchShifter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioPitchShifter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioPitchShifter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioPlayer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioPlayer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioPlayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioPlayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioRecorder")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioRecorder" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioRecorder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioRecorder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioReverb")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioReverb" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioReverb",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioReverb",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioSearchParams")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioSearchParams" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioSearchParams",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioSearchParams",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioSpeechToText")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioSpeechToText" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioSpeechToText",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioSpeechToText",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioTextToSpeech")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioTextToSpeech" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioTextToSpeech",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioTextToSpeech",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AudioTremolo")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AudioTremolo" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AudioTremolo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AudioTremolo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AuroraScriptService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AuroraScriptService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AuroraScriptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AuroraScriptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AvatarCreationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AvatarCreationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AvatarCreationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AvatarCreationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("AvatarEditorService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "AvatarEditorService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "AvatarEditorService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "AvatarEditorService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Backpack")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Backpack" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Backpack",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Backpack",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BackpackItem")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BackpackItem" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BackpackItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BackpackItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BadgeService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BadgeService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BadgeService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BadgeService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BallSocketConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BallSocketConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BallSocketConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BallSocketConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BanHistoryPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BanHistoryPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BanHistoryPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BanHistoryPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BaseCoreGuiConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BaseCoreGuiConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BaseCoreGuiConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BaseCoreGuiConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BasePart")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BasePart" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BasePart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BasePart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BasePlayerGui")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BasePlayerGui" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BasePlayerGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BasePlayerGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BaseRemoteEvent")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BaseRemoteEvent" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BaseRemoteEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BaseRemoteEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BaseScript")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BaseScript" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BaseScript",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BaseScript",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BaseWrap")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BaseWrap" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BaseWrap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BaseWrap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Beam")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Beam" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Beam",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Beam",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BevelMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BevelMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BevelMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BevelMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BillboardGui")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BillboardGui" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BillboardGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BillboardGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BinaryStringValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BinaryStringValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BinaryStringValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BinaryStringValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BindableEvent")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BindableEvent" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BindableEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BindableEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BindableFunction")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BindableFunction" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BindableFunction",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BindableFunction",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BlockMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BlockMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BlockMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BlockMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BloomEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BloomEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BloomEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BloomEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BlurEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BlurEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BlurEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BlurEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyAngularVelocity")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyAngularVelocity" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyAngularVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyAngularVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyColors")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyColors" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyColors",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyColors",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyForce")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyForce" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyForce",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyForce",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyGyro")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyGyro" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyGyro",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyGyro",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyMover")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyMover" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyMover",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyMover",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyPartDescription")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyPartDescription" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyPartDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyPartDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyPosition")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyPosition" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyPosition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyPosition",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyThrust")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyThrust" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyThrust",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyThrust",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BodyVelocity")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BodyVelocity" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BodyVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BodyVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Bone")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Bone" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Bone",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Bone",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BoolValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BoolValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BoolValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BoolValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BoxHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BoxHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BoxHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BoxHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BrickColorValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BrickColorValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BrickColorValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BrickColorValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BrowserService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BrowserService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BrowserService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BrowserService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BubbleChatConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BubbleChatConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BubbleChatConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BubbleChatConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BubbleChatMessageProperties")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BubbleChatMessageProperties" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BubbleChatMessageProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BubbleChatMessageProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("BuoyancySensor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "BuoyancySensor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "BuoyancySensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "BuoyancySensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CFrameValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CFrameValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CFrameValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CFrameValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CacheableContentProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CacheableContentProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CacheableContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CacheableContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Camera")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Camera" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Camera",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Camera",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CanvasGroup")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CanvasGroup" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CanvasGroup",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CanvasGroup",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Capture")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Capture" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Capture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Capture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CaptureService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CaptureService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CaptureService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CaptureService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CapturesPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CapturesPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CapturesPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CapturesPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CapturesViewConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CapturesViewConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CapturesViewConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CapturesViewConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CatalogPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CatalogPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CatalogPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CatalogPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ChangeHistoryService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ChangeHistoryService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ChangeHistoryService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ChangeHistoryService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ChannelTabsConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ChannelTabsConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ChannelTabsConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ChannelTabsConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CharacterAppearance")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CharacterAppearance" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CharacterAppearance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CharacterAppearance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CharacterMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CharacterMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CharacterMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CharacterMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Chat")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Chat" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Chat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Chat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ChatInputBarConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ChatInputBarConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ChatInputBarConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ChatInputBarConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ChatWindowConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ChatWindowConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ChatWindowConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ChatWindowConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ChatWindowMessageProperties")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ChatWindowMessageProperties" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ChatWindowMessageProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ChatWindowMessageProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ChorusSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ChorusSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ChorusSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ChorusSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ClickDetector")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ClickDetector" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ClickDetector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ClickDetector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ClientReplicator")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ClientReplicator" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ClientReplicator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ClientReplicator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ClimbController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ClimbController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ClimbController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ClimbController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Clothing")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Clothing" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Clothing",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Clothing",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Clouds")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Clouds" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Clouds",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Clouds",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ClusterPacketCache")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ClusterPacketCache" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ClusterPacketCache",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ClusterPacketCache",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CollectionService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CollectionService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CollectionService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CollectionService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Color3Value")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Color3Value" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Color3Value",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Color3Value",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ColorCorrectionEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ColorCorrectionEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ColorCorrectionEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ColorCorrectionEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ColorGradingEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ColorGradingEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ColorGradingEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ColorGradingEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CommerceService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CommerceService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CommerceService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CommerceService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CompositeValueCurve")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CompositeValueCurve" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CompositeValueCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CompositeValueCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CompressorSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CompressorSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CompressorSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CompressorSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ConeHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ConeHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ConeHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ConeHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ConfigService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ConfigService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ConfigService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ConfigService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ConfigSnapshot")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ConfigSnapshot" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ConfigSnapshot",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ConfigSnapshot",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Configuration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Configuration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Configuration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Configuration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ConfigureServerService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ConfigureServerService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ConfigureServerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ConfigureServerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Constraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Constraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Constraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Constraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ContentProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ContentProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ContextActionService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ContextActionService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ContextActionService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ContextActionService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Controller")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Controller" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Controller",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Controller",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ControllerBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ControllerBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ControllerBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ControllerBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ControllerManager")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ControllerManager" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ControllerManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ControllerManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ControllerPartSensor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ControllerPartSensor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ControllerPartSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ControllerPartSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ControllerSensor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ControllerSensor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ControllerSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ControllerSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ControllerService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ControllerService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ControllerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ControllerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CookiesService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CookiesService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CookiesService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CookiesService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CoreGuiConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CoreGuiConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CoreGuiConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CoreGuiConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CoreScriptDebuggingManagerHelper")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CoreScriptDebuggingManagerHelper" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CoreScriptDebuggingManagerHelper",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CoreScriptDebuggingManagerHelper",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CornerWedgePart")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CornerWedgePart" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CornerWedgePart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CornerWedgePart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CreatorStoreService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CreatorStoreService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CreatorStoreService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CreatorStoreService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CurveAnimation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CurveAnimation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CurveAnimation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CurveAnimation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CustomEvent")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CustomEvent" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CustomEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CustomEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CustomEventReceiver")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CustomEventReceiver" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CustomEventReceiver",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CustomEventReceiver",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CylinderHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CylinderHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CylinderHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CylinderHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CylinderMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CylinderMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CylinderMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CylinderMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("CylindricalConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "CylindricalConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "CylindricalConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "CylindricalConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataModel")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataModel" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataModel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataModel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataModelMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataModelMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataModelMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataModelMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataModelSession")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataModelSession" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataModelSession",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataModelSession",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStore")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStore" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStore",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStore",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreGetOptions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreGetOptions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreGetOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreGetOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreIncrementOptions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreIncrementOptions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreIncrementOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreIncrementOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreInfo")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreInfo" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreInfo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreInfo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreKey")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreKey" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreKey",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreKey",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreKeyInfo")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreKeyInfo" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreKeyInfo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreKeyInfo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreKeyPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreKeyPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreKeyPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreKeyPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreListingPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreListingPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreListingPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreListingPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreObjectVersionInfo")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreObjectVersionInfo" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreObjectVersionInfo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreObjectVersionInfo",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreOptions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreOptions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStorePages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStorePages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStorePages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStorePages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreSetOptions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreSetOptions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreSetOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreSetOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DataStoreVersionPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DataStoreVersionPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DataStoreVersionPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DataStoreVersionPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Debris")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Debris" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Debris",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Debris",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Decal")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Decal" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Decal",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Decal",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DepthOfFieldEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DepthOfFieldEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DepthOfFieldEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DepthOfFieldEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Dialog")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Dialog" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Dialog",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Dialog",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DialogChoice")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DialogChoice" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DialogChoice",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DialogChoice",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DigitsRigDescription")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DigitsRigDescription" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DigitsRigDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DigitsRigDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DistortionSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DistortionSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DistortionSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DistortionSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DoubleConstrainedValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DoubleConstrainedValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DoubleConstrainedValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DoubleConstrainedValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DragDetector")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DragDetector" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DragDetector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DragDetector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Dragger")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Dragger" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Dragger",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Dragger",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("DynamicRotate")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "DynamicRotate" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "DynamicRotate",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "DynamicRotate",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("EchoSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "EchoSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "EchoSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "EchoSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("EditableImage")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "EditableImage" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "EditableImage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "EditableImage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("EditableMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "EditableMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "EditableMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "EditableMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("EncodingService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "EncodingService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "EncodingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "EncodingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("EqualizerSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "EqualizerSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "EqualizerSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "EqualizerSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("EulerRotationCurve")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "EulerRotationCurve" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "EulerRotationCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "EulerRotationCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ExperienceInviteOptions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ExperienceInviteOptions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ExperienceInviteOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ExperienceInviteOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ExperienceNotificationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ExperienceNotificationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ExperienceNotificationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ExperienceNotificationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Explosion")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Explosion" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Explosion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Explosion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FaceControls")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FaceControls" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FaceControls",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FaceControls",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FaceInstance")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FaceInstance" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FaceInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FaceInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Feature")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Feature" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Feature",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Feature",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FeatureRestrictionManager")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FeatureRestrictionManager" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FeatureRestrictionManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FeatureRestrictionManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("File")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "File" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "File",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "File",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FileMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FileMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FileMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FileMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Fire")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Fire" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Fire",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Fire",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Flag")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Flag" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Flag",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Flag",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FlagStand")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FlagStand" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FlagStand",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FlagStand",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FlagStandService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FlagStandService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FlagStandService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FlagStandService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FlangeSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FlangeSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FlangeSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FlangeSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FloatCurve")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FloatCurve" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FloatCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FloatCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FloorWire")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FloorWire" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FloorWire",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FloorWire",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FluidForceSensor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FluidForceSensor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FluidForceSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FluidForceSensor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Folder")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Folder" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Folder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Folder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ForceField")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ForceField" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ForceField",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ForceField",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FormFactorPart")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FormFactorPart" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FormFactorPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FormFactorPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Frame")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Frame" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Frame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Frame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FriendPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FriendPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FriendPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FriendPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FriendService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FriendService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FriendService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FriendService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("FunctionalTest")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "FunctionalTest" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "FunctionalTest",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "FunctionalTest",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GamePassService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GamePassService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GamePassService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GamePassService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GamepadService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GamepadService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GamepadService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GamepadService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GeneratedFolder")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GeneratedFolder" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GeneratedFolder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GeneratedFolder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GenerationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GenerationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GenerationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GenerationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GenericChallengeService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GenericChallengeService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GenericChallengeService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GenericChallengeService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Geometry")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Geometry" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Geometry",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Geometry",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GeometryService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GeometryService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GeometryService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GeometryService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GetTextBoundsParams")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GetTextBoundsParams" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GetTextBoundsParams",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GetTextBoundsParams",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GlobalDataStore")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GlobalDataStore" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GlobalDataStore",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GlobalDataStore",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Glue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Glue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Glue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Glue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GroundController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GroundController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GroundController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GroundController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GroupService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GroupService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GroupService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GroupService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiBase2d")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiBase2d" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiBase2d",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiBase2d",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiBase3d")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiBase3d" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiBase3d",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiBase3d",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiButton")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiButton" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiButton",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiButton",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiLabel")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiLabel" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiLabel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiLabel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiMain")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiMain" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiMain",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiMain",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiObject")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiObject" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiObject",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiObject",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuiService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuiService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuiService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuiService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("GuidRegistryService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "GuidRegistryService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "GuidRegistryService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "GuidRegistryService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HSRDataContentProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HSRDataContentProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HSRDataContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HSRDataContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Handles")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Handles" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Handles",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Handles",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HandlesBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HandlesBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HandlesBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HandlesBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HapticEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HapticEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HapticEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HapticEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HapticService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HapticService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HapticService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HapticService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Hat")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Hat" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Hat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Hat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HeapProfilerService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HeapProfilerService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HeapProfilerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HeapProfilerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HeightmapImporterService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HeightmapImporterService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HeightmapImporterService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HeightmapImporterService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HiddenSurfaceRemovalAsset")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HiddenSurfaceRemovalAsset" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HiddenSurfaceRemovalAsset",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HiddenSurfaceRemovalAsset",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Highlight")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Highlight" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Highlight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Highlight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HingeConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HingeConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HingeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HingeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Hint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Hint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Hint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Hint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Hole")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Hole" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Hole",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Hole",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Hopper")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Hopper" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Hopper",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Hopper",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HopperBin")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HopperBin" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HopperBin",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HopperBin",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HttpRbxApiService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HttpRbxApiService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HttpRbxApiService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HttpRbxApiService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HttpService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HttpService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HttpService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HttpService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Humanoid")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Humanoid" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Humanoid",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Humanoid",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HumanoidController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HumanoidController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HumanoidController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HumanoidController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HumanoidDescription")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HumanoidDescription" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HumanoidDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HumanoidDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("HumanoidRigDescription")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "HumanoidRigDescription" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "HumanoidRigDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "HumanoidRigDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("IKControl")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "IKControl" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "IKControl",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "IKControl",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ILegacyStudioBridge")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ILegacyStudioBridge" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ILegacyStudioBridge",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ILegacyStudioBridge",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ImageButton")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ImageButton" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ImageButton",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ImageButton",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ImageHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ImageHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ImageHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ImageHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ImageLabel")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ImageLabel" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ImageLabel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ImageLabel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("IncrementalPatchBuilder")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "IncrementalPatchBuilder" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "IncrementalPatchBuilder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "IncrementalPatchBuilder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InputAction")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InputAction" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InputAction",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InputAction",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InputBinding")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InputBinding" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InputBinding",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InputBinding",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InputContext")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InputContext" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InputContext",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InputContext",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InputObject")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InputObject" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InputObject",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InputObject",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InsertService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InsertService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InsertService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InsertService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Instance")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Instance" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Instance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Instance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InstanceAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InstanceAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InstanceAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InstanceAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InstanceExtensionsService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InstanceExtensionsService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InstanceExtensionsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InstanceExtensionsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("IntConstrainedValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "IntConstrainedValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "IntConstrainedValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "IntConstrainedValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("IntValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "IntValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "IntValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "IntValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("IntersectOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "IntersectOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "IntersectOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "IntersectOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("InventoryPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "InventoryPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "InventoryPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "InventoryPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("JointInstance")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "JointInstance" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "JointInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "JointInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("JointsService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "JointsService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "JointsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "JointsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("KeyboardService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "KeyboardService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "KeyboardService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "KeyboardService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Keyframe")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Keyframe" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Keyframe",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Keyframe",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("KeyframeMarker")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "KeyframeMarker" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "KeyframeMarker",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "KeyframeMarker",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("KeyframeSequence")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "KeyframeSequence" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "KeyframeSequence",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "KeyframeSequence",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("KeyframeSequenceProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "KeyframeSequenceProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "KeyframeSequenceProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "KeyframeSequenceProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LayerCollector")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LayerCollector" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LayerCollector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LayerCollector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Light")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Light" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Light",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Light",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Lighting")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Lighting" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Lighting",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Lighting",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LineForce")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LineForce" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LineForce",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LineForce",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LineHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LineHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LineHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LineHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LinearVelocity")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LinearVelocity" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LinearVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LinearVelocity",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LocalScript")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LocalScript" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LocalScript",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LocalScript",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LocalizationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LocalizationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LocalizationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LocalizationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LocalizationTable")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LocalizationTable" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LocalizationTable",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LocalizationTable",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LogService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LogService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LogService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LogService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LoginService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LoginService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LoginService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LoginService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LuaSourceContainer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LuaSourceContainer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LuaSourceContainer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LuaSourceContainer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("LuaWebService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "LuaWebService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "LuaWebService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "LuaWebService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MLService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MLService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MLService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MLService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MLSession")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MLSession" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MLSession",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MLSession",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MakeupDescription")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MakeupDescription" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MakeupDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MakeupDescription",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ManualGlue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ManualGlue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ManualGlue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ManualGlue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ManualSurfaceJointInstance")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ManualSurfaceJointInstance" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ManualSurfaceJointInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ManualSurfaceJointInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ManualWeld")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ManualWeld" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ManualWeld",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ManualWeld",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MarkerCurve")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MarkerCurve" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MarkerCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MarkerCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MarketplaceService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MarketplaceService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MarketplaceService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MarketplaceService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MatchmakingService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MatchmakingService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MatchmakingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MatchmakingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MaterialService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MaterialService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MaterialService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MaterialService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MaterialVariant")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MaterialVariant" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MaterialVariant",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MaterialVariant",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MemStorageConnection")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MemStorageConnection" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MemStorageConnection",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MemStorageConnection",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MemStorageService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MemStorageService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MemStorageService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MemStorageService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MemoryStoreHashMap")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MemoryStoreHashMap" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MemoryStoreHashMap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MemoryStoreHashMap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MemoryStoreHashMapPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MemoryStoreHashMapPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MemoryStoreHashMapPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MemoryStoreHashMapPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MemoryStoreQueue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MemoryStoreQueue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MemoryStoreQueue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MemoryStoreQueue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MemoryStoreService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MemoryStoreService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MemoryStoreService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MemoryStoreService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MemoryStoreSortedMap")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MemoryStoreSortedMap" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MemoryStoreSortedMap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MemoryStoreSortedMap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MeshContentProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MeshContentProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MeshContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MeshContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MeshPart")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MeshPart" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MeshPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MeshPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Message")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Message" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Message",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Message",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MessagingService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MessagingService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MessagingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MessagingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MicroProfilerService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MicroProfilerService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MicroProfilerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MicroProfilerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Model")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Model" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Model",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Model",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ModerationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ModerationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ModerationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ModerationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ModuleScript")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ModuleScript" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ModuleScript",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ModuleScript",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Motor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Motor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Motor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Motor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Motor6D")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Motor6D" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Motor6D",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Motor6D",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MotorFeature")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MotorFeature" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MotorFeature",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MotorFeature",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Mouse")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Mouse" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Mouse",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Mouse",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("MouseService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "MouseService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "MouseService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "MouseService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NegateOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NegateOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NegateOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NegateOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NetworkMarker")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NetworkMarker" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NetworkMarker",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NetworkMarker",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NetworkPeer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NetworkPeer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NetworkPeer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NetworkPeer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NetworkReplicator")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NetworkReplicator" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NetworkReplicator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NetworkReplicator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NoCollisionConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NoCollisionConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NoCollisionConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NoCollisionConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NotificationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NotificationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NotificationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NotificationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NumberPose")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NumberPose" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NumberPose",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NumberPose",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("NumberValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "NumberValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "NumberValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "NumberValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Object")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Object" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Object",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Object",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ObjectValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ObjectValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ObjectValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ObjectValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("OpenCloudApiV1")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "OpenCloudApiV1" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "OpenCloudApiV1",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "OpenCloudApiV1",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("OpenCloudService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "OpenCloudService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "OpenCloudService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "OpenCloudService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("OperationGraph")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "OperationGraph" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "OperationGraph",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "OperationGraph",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("OrderedDataStore")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "OrderedDataStore" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "OrderedDataStore",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "OrderedDataStore",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("OutfitPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "OutfitPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "OutfitPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "OutfitPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PVAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PVAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PVAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PVAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PVInstance")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PVInstance" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PVInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PVInstance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PackageLink")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PackageLink" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PackageLink",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PackageLink",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PackageService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PackageService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PackageService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PackageService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Packages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Packages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Packages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Packages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Pages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Pages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Pages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Pages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Pants")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Pants" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Pants",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Pants",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Part")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Part" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Part",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Part",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PartAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PartAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PartAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PartAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PartOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PartOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PartOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PartOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PartOperationAsset")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PartOperationAsset" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PartOperationAsset",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PartOperationAsset",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ParticleEmitter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ParticleEmitter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ParticleEmitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ParticleEmitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PatchBundlerFileWatch")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PatchBundlerFileWatch" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PatchBundlerFileWatch",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PatchBundlerFileWatch",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PatchMapping")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PatchMapping" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PatchMapping",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PatchMapping",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Path")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Path" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Path",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Path",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Path2D")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Path2D" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Path2D",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Path2D",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PathfindingLink")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PathfindingLink" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PathfindingLink",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PathfindingLink",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PathfindingModifier")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PathfindingModifier" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PathfindingModifier",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PathfindingModifier",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PathfindingService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PathfindingService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PathfindingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PathfindingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PermissionsService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PermissionsService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PermissionsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PermissionsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PhysicsService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PhysicsService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PhysicsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PhysicsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PitchShiftSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PitchShiftSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PitchShiftSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PitchShiftSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlacesService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlacesService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlacesService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlacesService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Plane")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Plane" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Plane",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Plane",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlaneConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlaneConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlaneConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlaneConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Platform")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Platform" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Platform",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Platform",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerData")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerData" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerData",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerData",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerDataRecord")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerDataRecord" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerDataRecord",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerDataRecord",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerDataRecordConfig")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerDataRecordConfig" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerDataRecordConfig",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerDataRecordConfig",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerDataService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerDataService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerDataService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerDataService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerGui")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerGui" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerListConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerListConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerListConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerListConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerMouse")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerMouse" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerMouse",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerMouse",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerScripts")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerScripts" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerScripts",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerScripts",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PlayerViewService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PlayerViewService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PlayerViewService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PlayerViewService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Players")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Players" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Players",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Players",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PluginManagementService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PluginManagementService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PluginManagementService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PluginManagementService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PointLight")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PointLight" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PointLight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PointLight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PointsService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PointsService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PointsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PointsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PolicyService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PolicyService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PolicyService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PolicyService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Pose")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Pose" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Pose",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Pose",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PoseBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PoseBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PoseBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PoseBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PostEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PostEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PostEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PostEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Preloaded")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Preloaded" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Preloaded",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Preloaded",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PrismaticConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PrismaticConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PrismaticConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PrismaticConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ProceduralModel")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ProceduralModel" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ProceduralModel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ProceduralModel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ProcessInstancePhysicsService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ProcessInstancePhysicsService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ProcessInstancePhysicsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ProcessInstancePhysicsService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ProximityPrompt")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ProximityPrompt" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ProximityPrompt",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ProximityPrompt",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ProximityPromptService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ProximityPromptService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ProximityPromptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ProximityPromptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PublishService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PublishService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PublishService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PublishService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("PyramidHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "PyramidHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "PyramidHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "PyramidHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RayValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RayValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RayValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RayValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RecommendationPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RecommendationPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RecommendationPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RecommendationPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RecommendationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RecommendationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RecommendationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RecommendationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadata")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadata" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadata",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadata",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataCallbacks")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataCallbacks" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataCallbacks",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataCallbacks",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataClass")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataClass" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataClass",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataClass",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataClasses")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataClasses" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataClasses",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataClasses",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataEnum")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataEnum" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataEnum",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataEnum",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataEnumItem")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataEnumItem" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataEnumItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataEnumItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataEnums")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataEnums" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataEnums",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataEnums",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataEvents")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataEvents" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataEvents",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataEvents",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataFunctions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataFunctions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataFunctions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataFunctions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataItem")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataItem" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataMember")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataMember" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataMember",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataMember",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataProperties")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataProperties" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionMetadataYieldFunctions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionMetadataYieldFunctions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionMetadataYieldFunctions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionMetadataYieldFunctions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReflectionService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReflectionService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReflectionService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReflectionService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RemoteCommandService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RemoteCommandService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RemoteCommandService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RemoteCommandService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RemoteDebuggerServer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RemoteDebuggerServer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RemoteDebuggerServer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RemoteDebuggerServer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RemoteEvent")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RemoteEvent" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RemoteEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RemoteEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RemoteFunction")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RemoteFunction" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RemoteFunction",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RemoteFunction",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RenderingTest")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RenderingTest" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RenderingTest",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RenderingTest",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReplicatedFirst")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReplicatedFirst" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReplicatedFirst",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReplicatedFirst",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReplicatedStorage")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReplicatedStorage" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReplicatedStorage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReplicatedStorage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ReverbSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ReverbSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ReverbSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ReverbSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RigidConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RigidConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RigidConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RigidConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RocketPropulsion")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RocketPropulsion" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RocketPropulsion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RocketPropulsion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RodConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RodConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RodConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RodConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RopeConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RopeConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RopeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RopeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Rotate")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Rotate" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Rotate",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Rotate",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RotateP")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RotateP" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RotateP",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RotateP",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RotateV")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RotateV" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RotateV",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RotateV",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RotationCurve")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RotationCurve" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RotationCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RotationCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RunService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RunService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RunService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RunService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RunningAverageItemDouble")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RunningAverageItemDouble" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RunningAverageItemDouble",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RunningAverageItemDouble",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RunningAverageItemInt")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RunningAverageItemInt" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RunningAverageItemInt",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RunningAverageItemInt",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("RunningAverageTimeIntervalItem")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "RunningAverageTimeIntervalItem" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "RunningAverageTimeIntervalItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "RunningAverageTimeIntervalItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SceneAnalysisService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SceneAnalysisService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SceneAnalysisService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SceneAnalysisService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScreenGui")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScreenGui" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScreenGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScreenGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScreenshotCapture")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScreenshotCapture" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScreenshotCapture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScreenshotCapture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScreenshotHud")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScreenshotHud" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScreenshotHud",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScreenshotHud",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Script")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Script" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Script",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Script",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScriptBuilder")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScriptBuilder" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScriptBuilder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScriptBuilder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScriptContext")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScriptContext" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScriptContext",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScriptContext",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScriptProfilerService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScriptProfilerService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScriptProfilerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScriptProfilerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScriptService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScriptService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScriptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScriptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ScrollingFrame")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ScrollingFrame" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ScrollingFrame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ScrollingFrame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Seat")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Seat" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Seat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Seat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Selection")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Selection" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Selection",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Selection",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SelectionBox")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SelectionBox" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SelectionBox",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SelectionBox",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SelectionHighlightManager")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SelectionHighlightManager" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SelectionHighlightManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SelectionHighlightManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SelectionLasso")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SelectionLasso" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SelectionLasso",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SelectionLasso",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SelectionPartLasso")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SelectionPartLasso" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SelectionPartLasso",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SelectionPartLasso",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SelectionPointLasso")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SelectionPointLasso" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SelectionPointLasso",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SelectionPointLasso",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SelectionSphere")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SelectionSphere" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SelectionSphere",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SelectionSphere",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SelfViewConfiguration")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SelfViewConfiguration" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SelfViewConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SelfViewConfiguration",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SensorBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SensorBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SensorBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SensorBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ServerReplicator")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ServerReplicator" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ServerReplicator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ServerReplicator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ServerScriptService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ServerScriptService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ServerScriptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ServerScriptService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ServerStorage")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ServerStorage" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ServerStorage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ServerStorage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ServiceProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ServiceProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ServiceProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ServiceProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ServiceVisibilityService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ServiceVisibilityService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ServiceVisibilityService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ServiceVisibilityService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SessionCheckService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SessionCheckService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SessionCheckService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SessionCheckService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SharedTableRegistry")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SharedTableRegistry" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SharedTableRegistry",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SharedTableRegistry",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Shirt")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Shirt" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Shirt",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Shirt",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ShirtGraphic")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ShirtGraphic" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ShirtGraphic",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ShirtGraphic",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SkateboardController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SkateboardController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SkateboardController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SkateboardController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SkateboardPlatform")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SkateboardPlatform" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SkateboardPlatform",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SkateboardPlatform",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Skin")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Skin" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Skin",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Skin",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Sky")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Sky" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Sky",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Sky",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SlidingBallConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SlidingBallConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SlidingBallConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SlidingBallConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SlimContentProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SlimContentProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SlimContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SlimContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Smoke")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Smoke" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Smoke",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Smoke",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SmoothVoxelsUpgraderService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SmoothVoxelsUpgraderService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SmoothVoxelsUpgraderService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SmoothVoxelsUpgraderService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Snap")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Snap" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Snap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Snap",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SocialService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SocialService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SocialService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SocialService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SolidModelContentProvider")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SolidModelContentProvider" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SolidModelContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SolidModelContentProvider",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Sound")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Sound" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Sound",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Sound",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SoundGroup")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SoundGroup" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SoundGroup",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SoundGroup",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SoundService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SoundService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SoundService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SoundService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Sparkles")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Sparkles" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Sparkles",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Sparkles",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SpawnLocation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SpawnLocation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SpawnLocation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SpawnLocation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SpawnerService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SpawnerService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SpawnerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SpawnerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SpecialMesh")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SpecialMesh" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SpecialMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SpecialMesh",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SphereHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SphereHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SphereHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SphereHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SpotLight")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SpotLight" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SpotLight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SpotLight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SpringConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SpringConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SpringConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SpringConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StandardPages")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StandardPages" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StandardPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StandardPages",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StarterCharacterScripts")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StarterCharacterScripts" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StarterCharacterScripts",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StarterCharacterScripts",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StarterGear")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StarterGear" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StarterGear",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StarterGear",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StarterGui")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StarterGui" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StarterGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StarterGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StarterPack")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StarterPack" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StarterPack",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StarterPack",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StarterPlayer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StarterPlayer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StarterPlayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StarterPlayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StarterPlayerScripts")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StarterPlayerScripts" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StarterPlayerScripts",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StarterPlayerScripts",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StartupMessageService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StartupMessageService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StartupMessageService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StartupMessageService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Stats")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Stats" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Stats",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Stats",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StatsItem")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StatsItem" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StatsItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StatsItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Status")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Status" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Status",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Status",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StopWatchReporter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StopWatchReporter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StopWatchReporter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StopWatchReporter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StringValue")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StringValue" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StringValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StringValue",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StudioTestService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StudioTestService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StudioTestService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StudioTestService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StudioTheme")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StudioTheme" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StudioTheme",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StudioTheme",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StyleBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StyleBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StyleBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StyleBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StyleDerive")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StyleDerive" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StyleDerive",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StyleDerive",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StyleLink")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StyleLink" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StyleLink",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StyleLink",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StyleQuery")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StyleQuery" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StyleQuery",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StyleQuery",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StyleRule")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StyleRule" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StyleRule",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StyleRule",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StyleSheet")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StyleSheet" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StyleSheet",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StyleSheet",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("StylingService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "StylingService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "StylingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "StylingService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SunRaysEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SunRaysEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SunRaysEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SunRaysEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SurfaceAppearance")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SurfaceAppearance" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SurfaceAppearance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SurfaceAppearance",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SurfaceGui")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SurfaceGui" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SurfaceGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SurfaceGui",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SurfaceGuiBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SurfaceGuiBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SurfaceGuiBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SurfaceGuiBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SurfaceLight")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SurfaceLight" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SurfaceLight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SurfaceLight",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SurfaceSelection")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SurfaceSelection" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SurfaceSelection",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SurfaceSelection",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SwimController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SwimController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SwimController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SwimController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("SyncScriptBuilder")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "SyncScriptBuilder" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "SyncScriptBuilder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "SyncScriptBuilder",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Team")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Team" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Team",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Team",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Teams")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Teams" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Teams",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Teams",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TeleportAsyncResult")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TeleportAsyncResult" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TeleportAsyncResult",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TeleportAsyncResult",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TeleportOptions")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TeleportOptions" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TeleportOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TeleportOptions",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TeleportService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TeleportService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TeleportService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TeleportService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Terrain")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Terrain" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Terrain",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Terrain",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TerrainDetail")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TerrainDetail" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TerrainDetail",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TerrainDetail",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TerrainIterateOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TerrainIterateOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TerrainIterateOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TerrainIterateOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TerrainModifyOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TerrainModifyOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TerrainModifyOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TerrainModifyOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TerrainReadOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TerrainReadOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TerrainReadOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TerrainReadOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TerrainRegion")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TerrainRegion" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TerrainRegion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TerrainRegion",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TerrainWriteOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TerrainWriteOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TerrainWriteOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TerrainWriteOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TestCase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TestCase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TestCase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TestCase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TestService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TestService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TestService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TestService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextBox")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextBox" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextBox",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextBox",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextBoxService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextBoxService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextBoxService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextBoxService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextButton")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextButton" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextButton",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextButton",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextChannel")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextChannel" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextChannel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextChannel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextChannelWindow")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextChannelWindow" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextChannelWindow",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextChannelWindow",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextChatCommand")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextChatCommand" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextChatCommand",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextChatCommand",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextChatConfigurations")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextChatConfigurations" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextChatConfigurations",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextChatConfigurations",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextChatMessage")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextChatMessage" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextChatMessage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextChatMessage",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextChatMessageProperties")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextChatMessageProperties" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextChatMessageProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextChatMessageProperties",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextChatService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextChatService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextChatService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextChatService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextFilterResult")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextFilterResult" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextFilterResult",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextFilterResult",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextFilterTranslatedResult")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextFilterTranslatedResult" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextFilterTranslatedResult",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextFilterTranslatedResult",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextGenerator")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextGenerator" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextGenerator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextGenerator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextLabel")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextLabel" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextLabel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextLabel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TextSource")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TextSource" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TextSource",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TextSource",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Texture")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Texture" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Texture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Texture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TimerService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TimerService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TimerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TimerService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Tool")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Tool" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Tool",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Tool",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Torque")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Torque" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Torque",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Torque",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TorsionSpringConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TorsionSpringConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TorsionSpringConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TorsionSpringConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TotalCountTimeIntervalItem")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TotalCountTimeIntervalItem" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TotalCountTimeIntervalItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TotalCountTimeIntervalItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TouchInputService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TouchInputService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TouchInputService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TouchInputService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TouchTransmitter")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TouchTransmitter" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TouchTransmitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TouchTransmitter",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TraceRouteService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TraceRouteService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TraceRouteService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TraceRouteService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Trail")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Trail" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Trail",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Trail",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Translator")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Translator" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Translator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Translator",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TremoloSoundEffect")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TremoloSoundEffect" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TremoloSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TremoloSoundEffect",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TriangleMeshPart")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TriangleMeshPart" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TriangleMeshPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TriangleMeshPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TrussPart")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TrussPart" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TrussPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TrussPart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Tween")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Tween" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Tween",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Tween",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TweenBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TweenBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TweenBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TweenBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("TweenService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "TweenService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "TweenService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "TweenService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UGCValidationService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UGCValidationService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UGCValidationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UGCValidationService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIAspectRatioConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIAspectRatioConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIAspectRatioConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIAspectRatioConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIComponent")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIComponent" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIComponent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIComponent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UICorner")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UICorner" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UICorner",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UICorner",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIDragDetector")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIDragDetector" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIDragDetector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIDragDetector",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIDragDetectorService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIDragDetectorService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIDragDetectorService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIDragDetectorService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIFlexItem")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIFlexItem" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIFlexItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIFlexItem",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIGradient")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIGradient" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIGradient",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIGradient",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIGridLayout")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIGridLayout" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIGridLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIGridLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIGridStyleLayout")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIGridStyleLayout" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIGridStyleLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIGridStyleLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UILayout")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UILayout" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UILayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UILayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIListLayout")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIListLayout" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIListLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIListLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIPadding")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIPadding" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIPadding",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIPadding",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIPageLayout")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIPageLayout" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIPageLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIPageLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIScale")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIScale" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIScale",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIScale",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIShadow")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIShadow" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIShadow",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIShadow",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UISizeConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UISizeConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UISizeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UISizeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UIStroke")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UIStroke" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UIStroke",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UIStroke",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UITableLayout")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UITableLayout" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UITableLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UITableLayout",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UITextSizeConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UITextSizeConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UITextSizeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UITextSizeConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UnionOperation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UnionOperation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UnionOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UnionOperation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UniqueIdLookupService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UniqueIdLookupService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UniqueIdLookupService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UniqueIdLookupService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UniversalConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UniversalConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UniversalConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UniversalConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UnreliableRemoteEvent")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UnreliableRemoteEvent" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UnreliableRemoteEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UnreliableRemoteEvent",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UserInputService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UserInputService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UserInputService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UserInputService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("UserService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "UserService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "UserService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "UserService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VRService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VRService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VRService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VRService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VRStatusService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VRStatusService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VRStatusService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VRStatusService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ValueBase")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ValueBase" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ValueBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ValueBase",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ValueCurve")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ValueCurve" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ValueCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ValueCurve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Vector3Curve")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Vector3Curve" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Vector3Curve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Vector3Curve",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Vector3Value")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Vector3Value" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Vector3Value",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Vector3Value",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VectorForce")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VectorForce" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VectorForce",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VectorForce",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VehicleController")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VehicleController" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VehicleController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VehicleController",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VehicleSeat")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VehicleSeat" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VehicleSeat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VehicleSeat",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VelocityMotor")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VelocityMotor" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VelocityMotor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VelocityMotor",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoCapture")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoCapture" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoCapture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoCapture",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoCaptureService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoCaptureService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoCaptureService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoCaptureService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoDisplay")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoDisplay" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoDisplay",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoDisplay",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoFrame")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoFrame" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoFrame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoFrame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoPlayer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoPlayer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoPlayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoPlayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoSampler")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoSampler" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoSampler",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoSampler",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoScreenCaptureService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoScreenCaptureService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoScreenCaptureService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoScreenCaptureService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VideoService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VideoService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VideoService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VideoService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("ViewportFrame")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "ViewportFrame" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "ViewportFrame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "ViewportFrame",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VirtualInput")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VirtualInput" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VirtualInput",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VirtualInput",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VirtualInputManager")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VirtualInputManager" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VirtualInputManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VirtualInputManager",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VirtualUser")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VirtualUser" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VirtualUser",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VirtualUser",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VisibilityCheckDispatcher")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VisibilityCheckDispatcher" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VisibilityCheckDispatcher",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VisibilityCheckDispatcher",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Visit")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Visit" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Visit",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Visit",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VisualizationMode")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VisualizationMode" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VisualizationMode",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VisualizationMode",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VisualizationModeCategory")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VisualizationModeCategory" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VisualizationModeCategory",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VisualizationModeCategory",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VisualizationModeService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VisualizationModeService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VisualizationModeService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VisualizationModeService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VoiceChatInternal")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VoiceChatInternal" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VoiceChatInternal",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VoiceChatInternal",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("VoiceChatService")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "VoiceChatService" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "VoiceChatService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "VoiceChatService",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WebStreamClient")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WebStreamClient" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WebStreamClient",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WebStreamClient",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WedgePart")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WedgePart" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WedgePart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WedgePart",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Weld")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Weld" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Weld",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Weld",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WeldConstraint")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WeldConstraint" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WeldConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WeldConstraint",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Wire")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Wire" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Wire",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Wire",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WireframeHandleAdornment")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WireframeHandleAdornment" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WireframeHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WireframeHandleAdornment",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("Workspace")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "Workspace" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "Workspace",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "Workspace",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WorkspaceAnnotation")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WorkspaceAnnotation" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WorkspaceAnnotation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WorkspaceAnnotation",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WorldModel")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WorldModel" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WorldModel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WorldModel",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WorldRoot")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WorldRoot" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WorldRoot",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WorldRoot",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WrapDeformer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WrapDeformer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WrapDeformer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WrapDeformer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WrapLayer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WrapLayer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WrapLayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WrapLayer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WrapTarget")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WrapTarget" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WrapTarget",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WrapTarget",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local inst = Instance.new("WrapTextureTransfer")
    if not inst then
        return false
    end
    -- Just check that we can get ClassName and it matches
    if inst.ClassName ~= "WrapTextureTransfer" then
        return false
    end
    -- Try to get a property that exists (if any)
    local props = ReflectionService:GetPropertiesOfClass(
        "WrapTextureTransfer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, prop in ipairs(props) do
        local val = inst[prop.Name]
        -- If it's a writable property, try to set it
        local ok, err = pcall(function()
            inst[prop.Name] = val
        end)
        if not ok then
            -- If it fails, it might be read-only, but we still pass
        end
        break
    end
    -- Try to call a method (if any) with dummy args
    local methods = ReflectionService:GetMethodsOfClass(
        "WrapTextureTransfer",
        { Security = SecurityCapabilities.fromCurrent() }
    )
    for _, method in ipairs(methods) do
        if
            method.Name ~= "Destroy"
            and method.Name ~= "Clone"
            and method.Name ~= "GetChildren"
        then
            -- Try calling with no args (or dummy)
            local ok, err = pcall(function()
                inst[method.Name](inst)
            end)
            -- If it fails, that's fine as long as it didn't crash
        end
        break
    end
    inst:Destroy()
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessModifierType
    if not enumObj then
        return false
    end
    local item = enumObj.Allow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessModifierType.Allow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessModifierType
    if not enumObj then
        return false
    end
    local item = enumObj.Deny
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessModifierType.Deny" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Hat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Hat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Hair
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Hair" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Face
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Face" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Neck
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Neck" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Shoulder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Shoulder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Front
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Front" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Back
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Back" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Waist
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Waist" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.TShirt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.TShirt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Shirt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Shirt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Pants
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Pants" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Jacket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Jacket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Sweater
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Sweater" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Shorts
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Shorts" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftShoe
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.LeftShoe" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.RightShoe
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.RightShoe" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.DressSkirt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.DressSkirt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Eyebrow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Eyebrow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AccessoryType
    if not enumObj then
        return false
    end
    local item = enumObj.Eyelash
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AccessoryType.Eyelash" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionOnAutoResumeSync
    if not enumObj then
        return false
    end
    local item = enumObj.DontResume
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionOnAutoResumeSync.DontResume" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionOnAutoResumeSync
    if not enumObj then
        return false
    end
    local item = enumObj.KeepStudio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionOnAutoResumeSync.KeepStudio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionOnAutoResumeSync
    if not enumObj then
        return false
    end
    local item = enumObj.KeepLocal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionOnAutoResumeSync.KeepLocal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionOnStopSync
    if not enumObj then
        return false
    end
    local item = enumObj.AlwaysAsk
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionOnStopSync.AlwaysAsk" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionOnStopSync
    if not enumObj then
        return false
    end
    local item = enumObj.KeepLocalFiles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionOnStopSync.KeepLocalFiles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionOnStopSync
    if not enumObj then
        return false
    end
    local item = enumObj.DeleteLocalFiles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionOnStopSync.DeleteLocalFiles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Nothing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionType.Nothing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Pause
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionType.Pause" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Lose
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionType.Lose" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Draw
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionType.Draw" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Win
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActionType.Win" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActivePayerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActivePayerStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActivePayerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Never
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActivePayerStatus.Never" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActivePayerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Lapsed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActivePayerStatus.Lapsed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActivePayerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Casual50Percent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActivePayerStatus.Casual50Percent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActivePayerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Intermediate35Percent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActivePayerStatus.Intermediate35Percent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActivePayerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Top15Percent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActivePayerStatus.Top15Percent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActuatorRelativeTo
    if not enumObj then
        return false
    end
    local item = enumObj.Attachment0
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActuatorRelativeTo.Attachment0" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActuatorRelativeTo
    if not enumObj then
        return false
    end
    local item = enumObj.Attachment1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActuatorRelativeTo.Attachment1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActuatorRelativeTo
    if not enumObj then
        return false
    end
    local item = enumObj.World
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActuatorRelativeTo.World" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActuatorType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActuatorType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActuatorType
    if not enumObj then
        return false
    end
    local item = enumObj.Motor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActuatorType.Motor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ActuatorType
    if not enumObj then
        return false
    end
    local item = enumObj.Servo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ActuatorType.Servo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdAvailabilityResult
    if not enumObj then
        return false
    end
    local item = enumObj.IsAvailable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdAvailabilityResult.IsAvailable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdAvailabilityResult
    if not enumObj then
        return false
    end
    local item = enumObj.DeviceIneligible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdAvailabilityResult.DeviceIneligible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdAvailabilityResult
    if not enumObj then
        return false
    end
    local item = enumObj.ExperienceIneligible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdAvailabilityResult.ExperienceIneligible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdAvailabilityResult
    if not enumObj then
        return false
    end
    local item = enumObj.InternalError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdAvailabilityResult.InternalError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdAvailabilityResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoFill
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdAvailabilityResult.NoFill" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdAvailabilityResult
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerIneligible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdAvailabilityResult.PlayerIneligible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdAvailabilityResult
    if not enumObj then
        return false
    end
    local item = enumObj.PublisherIneligible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdAvailabilityResult.PublisherIneligible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdEventType
    if not enumObj then
        return false
    end
    local item = enumObj.VideoLoaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdEventType.VideoLoaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdEventType
    if not enumObj then
        return false
    end
    local item = enumObj.VideoRemoved
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdEventType.VideoRemoved" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdEventType
    if not enumObj then
        return false
    end
    local item = enumObj.UserCompletedVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdEventType.UserCompletedVideo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdEventType
    if not enumObj then
        return false
    end
    local item = enumObj.RewardedAdLoaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdEventType.RewardedAdLoaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdEventType
    if not enumObj then
        return false
    end
    local item = enumObj.RewardedAdGrant
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdEventType.RewardedAdGrant" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdEventType
    if not enumObj then
        return false
    end
    local item = enumObj.RewardedAdUnloaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdEventType.RewardedAdUnloaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdFormat
    if not enumObj then
        return false
    end
    local item = enumObj.RewardedVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdFormat.RewardedVideo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdShape
    if not enumObj then
        return false
    end
    local item = enumObj.HorizontalRectangle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdShape.HorizontalRectangle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdTeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Undefined
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdTeleportMethod.Undefined" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdTeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.PortalForward
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdTeleportMethod.PortalForward" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdTeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.InGameMenuBackButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdTeleportMethod.InGameMenuBackButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdTeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.UIBackButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdTeleportMethod.UIBackButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.AdLabelClicked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.AdLabelClicked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.VolumeButtonClicked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.VolumeButtonClicked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.FullscreenButtonClicked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.FullscreenButtonClicked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayButtonClicked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.PlayButtonClicked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.PauseButtonClicked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.PauseButtonClicked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.CloseButtonClicked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.CloseButtonClicked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.WhyThisAdClicked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.WhyThisAdClicked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayEventTriggered
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.PlayEventTriggered" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIEventType
    if not enumObj then
        return false
    end
    local item = enumObj.PauseEventTriggered
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIEventType.PauseEventTriggered" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIType
    if not enumObj then
        return false
    end
    local item = enumObj.Image
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIType.Image" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUIType
    if not enumObj then
        return false
    end
    local item = enumObj.Video
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUIType.Video" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUnitStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Inactive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUnitStatus.Inactive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdUnitStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Active
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdUnitStatus.Active" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdornCullingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdornCullingMode.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdornCullingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Never
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdornCullingMode.Never" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdornShading
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdornShading.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdornShading
    if not enumObj then
        return false
    end
    local item = enumObj.Shaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdornShading.Shaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdornShading
    if not enumObj then
        return false
    end
    local item = enumObj.XRay
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdornShading.XRay" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdornShading
    if not enumObj then
        return false
    end
    local item = enumObj.XRayShaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdornShading.XRayShaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AdornShading
    if not enumObj then
        return false
    end
    local item = enumObj.AlwaysOnTop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AdornShading.AlwaysOnTop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AgeCheckStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unchecked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AgeCheckStatus.Unchecked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AgeCheckStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Checked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AgeCheckStatus.Checked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlignType
    if not enumObj then
        return false
    end
    local item = enumObj.Parallel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlignType.Parallel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlignType
    if not enumObj then
        return false
    end
    local item = enumObj.Perpendicular
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlignType.Perpendicular" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlignType
    if not enumObj then
        return false
    end
    local item = enumObj.PrimaryAxisParallel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlignType.PrimaryAxisParallel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlignType
    if not enumObj then
        return false
    end
    local item = enumObj.PrimaryAxisPerpendicular
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlignType.PrimaryAxisPerpendicular" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlignType
    if not enumObj then
        return false
    end
    local item = enumObj.PrimaryAxisLookAt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlignType.PrimaryAxisLookAt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlignType
    if not enumObj then
        return false
    end
    local item = enumObj.AllAxes
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlignType.AllAxes" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlphaMode
    if not enumObj then
        return false
    end
    local item = enumObj.Overlay
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlphaMode.Overlay" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlphaMode
    if not enumObj then
        return false
    end
    local item = enumObj.Transparency
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlphaMode.Transparency" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlphaMode
    if not enumObj then
        return false
    end
    local item = enumObj.TintMask
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlphaMode.TintMask" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AlphaMode
    if not enumObj then
        return false
    end
    local item = enumObj.Opaque
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AlphaMode.Opaque" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsCustomFieldKeys
    if not enumObj then
        return false
    end
    local item = enumObj.CustomField01
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsCustomFieldKeys.CustomField01" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsCustomFieldKeys
    if not enumObj then
        return false
    end
    local item = enumObj.CustomField02
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsCustomFieldKeys.CustomField02" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsCustomFieldKeys
    if not enumObj then
        return false
    end
    local item = enumObj.CustomField03
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsCustomFieldKeys.CustomField03" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyAction
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyAction.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyAction
    if not enumObj then
        return false
    end
    local item = enumObj.Acquire
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyAction.Acquire" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyAction
    if not enumObj then
        return false
    end
    local item = enumObj.Spend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyAction.Spend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyFlowType
    if not enumObj then
        return false
    end
    local item = enumObj.Sink
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyFlowType.Sink" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyFlowType
    if not enumObj then
        return false
    end
    local item = enumObj.Source
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyFlowType.Source" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyTransactionType
    if not enumObj then
        return false
    end
    local item = enumObj.IAP
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyTransactionType.IAP" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyTransactionType
    if not enumObj then
        return false
    end
    local item = enumObj.Shop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyTransactionType.Shop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyTransactionType
    if not enumObj then
        return false
    end
    local item = enumObj.Gameplay
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyTransactionType.Gameplay" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyTransactionType
    if not enumObj then
        return false
    end
    local item = enumObj.ContextualPurchase
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AnalyticsEconomyTransactionType.ContextualPurchase"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyTransactionType
    if not enumObj then
        return false
    end
    local item = enumObj.TimedReward
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyTransactionType.TimedReward" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsEconomyTransactionType
    if not enumObj then
        return false
    end
    local item = enumObj.Onboarding
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsEconomyTransactionType.Onboarding" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsLogLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Trace
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsLogLevel.Trace" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsLogLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Debug
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsLogLevel.Debug" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsLogLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Information
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsLogLevel.Information" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsLogLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Warning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsLogLevel.Warning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsLogLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsLogLevel.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsLogLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Fatal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsLogLevel.Fatal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionStatus.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Begin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionStatus.Begin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Complete
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionStatus.Complete" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Abandon
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionStatus.Abandon" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Fail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionStatus.Fail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionType
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionType.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionType
    if not enumObj then
        return false
    end
    local item = enumObj.Start
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionType.Start" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionType
    if not enumObj then
        return false
    end
    local item = enumObj.Fail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionType.Fail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnalyticsProgressionType
    if not enumObj then
        return false
    end
    local item = enumObj.Complete
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnalyticsProgressionType.Complete" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Initializing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationClipFromVideoStatus.Initializing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Pending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationClipFromVideoStatus.Pending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Processing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationClipFromVideoStatus.Processing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorGeneric
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationClipFromVideoStatus.ErrorGeneric" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationClipFromVideoStatus.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorVideoTooLong
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AnimationClipFromVideoStatus.ErrorVideoTooLong"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorNoPersonDetected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AnimationClipFromVideoStatus.ErrorNoPersonDetected"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorVideoUnstable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AnimationClipFromVideoStatus.ErrorVideoUnstable"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationClipFromVideoStatus.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Cancelled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationClipFromVideoStatus.Cancelled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorMultiplePeople
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AnimationClipFromVideoStatus.ErrorMultiplePeople"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationClipFromVideoStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorUploadingVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AnimationClipFromVideoStatus.ErrorUploadingVideo"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeBlend2DInputMode
    if not enumObj then
        return false
    end
    local item = enumObj.Cartesian
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeBlend2DInputMode.Cartesian" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeBlend2DInputMode
    if not enumObj then
        return false
    end
    local item = enumObj.Polar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeBlend2DInputMode.Polar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeInterruptible
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeInterruptible.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeInterruptible
    if not enumObj then
        return false
    end
    local item = enumObj.Finished
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeInterruptible.Finished" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeInterruptible
    if not enumObj then
        return false
    end
    local item = enumObj.Trigger
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeInterruptible.Trigger" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodePhaseSync
    if not enumObj then
        return false
    end
    local item = enumObj.Synced
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodePhaseSync.Synced" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodePhaseSync
    if not enumObj then
        return false
    end
    local item = enumObj.Unsynced
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodePhaseSync.Unsynced" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodePlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.Loop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodePlayMode.Loop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodePlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.PingPong
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodePlayMode.PingPong" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodePlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.OnceAndHold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodePlayMode.OnceAndHold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodePlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.OnceAndReset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodePlayMode.OnceAndReset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeTransitionType
    if not enumObj then
        return false
    end
    local item = enumObj.CrossFade
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeTransitionType.CrossFade" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeTransitionType
    if not enumObj then
        return false
    end
    local item = enumObj.InertialBlend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeTransitionType.InertialBlend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeTransitionType
    if not enumObj then
        return false
    end
    local item = enumObj.DeadBlend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeTransitionType.DeadBlend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.InvalidNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.AddNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.AddNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.OverNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.OverNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.Blend1DNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.Blend1DNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.Blend2DNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.Blend2DNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.ClipNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.ClipNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.GraphOutput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.GraphOutput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.MaskNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.MaskNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.PrioritySelectNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.PrioritySelectNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.RandomSequenceNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.RandomSequenceNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.SelectNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.SelectNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.SequenceNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.SequenceNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.SpeedNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.SpeedNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeType
    if not enumObj then
        return false
    end
    local item = enumObj.SubtractNode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeType.SubtractNode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeWaitFor
    if not enumObj then
        return false
    end
    local item = enumObj.Finished
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeWaitFor.Finished" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationNodeWaitFor
    if not enumObj then
        return false
    end
    local item = enumObj.Trigger
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationNodeWaitFor.Trigger" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Idle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationPriority.Idle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Movement
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationPriority.Movement" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Action
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationPriority.Action" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Action2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationPriority.Action2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Action3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationPriority.Action3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Action4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationPriority.Action4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimationPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Core
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimationPriority.Core" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimatorRetargetingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimatorRetargetingMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimatorRetargetingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimatorRetargetingMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnimatorRetargetingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnimatorRetargetingMode.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationChannelContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationChannelContentPreference.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationChannelContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationChannelContentPreference.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationChannelContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationChannelContentPreference.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationEditingMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationEditingMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationEditingMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlacingNew
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationEditingMode.PlacingNew" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationEditingMode
    if not enumObj then
        return false
    end
    local item = enumObj.WritingNew
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationEditingMode.WritingNew" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationPlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationPlaceContentPreference.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationPlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationPlaceContentPreference.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationPlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.MentionsAndReplies
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AnnotationPlaceContentPreference.MentionsAndReplies"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationPlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationPlaceContentPreference.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestStatus.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Loading
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestStatus.Loading" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorInternalFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AnnotationRequestStatus.ErrorInternalFailure"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorNotFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestStatus.ErrorNotFound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorModerated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestStatus.ErrorModerated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestType.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Create
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestType.Create" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Resolve
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestType.Resolve" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Delete
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestType.Delete" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AnnotationRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Edit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AnnotationRequestType.Edit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AntiAliasing
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AntiAliasing.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AntiAliasing
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AntiAliasing.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppLifecycleManagerState
    if not enumObj then
        return false
    end
    local item = enumObj.Detached
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppLifecycleManagerState.Detached" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppLifecycleManagerState
    if not enumObj then
        return false
    end
    local item = enumObj.Active
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppLifecycleManagerState.Active" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppLifecycleManagerState
    if not enumObj then
        return false
    end
    local item = enumObj.Inactive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppLifecycleManagerState.Inactive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppLifecycleManagerState
    if not enumObj then
        return false
    end
    local item = enumObj.Hidden
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppLifecycleManagerState.Hidden" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.OpenApp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.OpenApp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.TapChatTab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.TapChatTab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.TapConversationEntry
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.TapConversationEntry" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.TapAvatarTab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.TapAvatarTab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.ReadConversation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.ReadConversation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.TapGamePageTab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.TapGamePageTab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.TapHomePageTab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.TapHomePageTab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.GamePageLoaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.GamePageLoaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.HomePageLoaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.HomePageLoaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarEditorPageLoaded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.AvatarEditorPageLoaded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellActionType
    if not enumObj then
        return false
    end
    local item = enumObj.HomePageInteractive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellActionType.HomePageInteractive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.Chat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.Chat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarEditor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.AvatarEditor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.GamePage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.GamePage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.HomePage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.HomePage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.More
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.More" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.Landing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.Landing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppShellFeature
    if not enumObj then
        return false
    end
    local item = enumObj.WatchPage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppShellFeature.WatchPage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppUpdateStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppUpdateStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppUpdateStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotSupported
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppUpdateStatus.NotSupported" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppUpdateStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Failed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppUpdateStatus.Failed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppUpdateStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotAvailable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppUpdateStatus.NotAvailable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppUpdateStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Available
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppUpdateStatus.Available" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppUpdateStatus
    if not enumObj then
        return false
    end
    local item = enumObj.AvailableBoundChannel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppUpdateStatus.AvailableBoundChannel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AppUpdateStatus
    if not enumObj then
        return false
    end
    local item = enumObj.AvailableBetaProgram
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AppUpdateStatus.AvailableBetaProgram" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ApplyStrokeMode
    if not enumObj then
        return false
    end
    local item = enumObj.Contextual
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ApplyStrokeMode.Contextual" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ApplyStrokeMode
    if not enumObj then
        return false
    end
    local item = enumObj.Border
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ApplyStrokeMode.Border" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AspectType
    if not enumObj then
        return false
    end
    local item = enumObj.FitWithinMaxSize
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AspectType.FitWithinMaxSize" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AspectType
    if not enumObj then
        return false
    end
    local item = enumObj.ScaleWithParentSize
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AspectType.ScaleWithParentSize" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetCreatorType
    if not enumObj then
        return false
    end
    local item = enumObj.User
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetCreatorType.User" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetCreatorType
    if not enumObj then
        return false
    end
    local item = enumObj.Group
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetCreatorType.Group" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetFetchStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetFetchStatus.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetFetchStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Failure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetFetchStatus.Failure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetFetchStatus
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetFetchStatus.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetFetchStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Loading
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetFetchStatus.Loading" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetFetchStatus
    if not enumObj then
        return false
    end
    local item = enumObj.TimedOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetFetchStatus.TimedOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetRepresentation
    if not enumObj then
        return false
    end
    local item = enumObj.FullLength
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetRepresentation.FullLength" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetRepresentation
    if not enumObj then
        return false
    end
    local item = enumObj.ShortPreview
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetRepresentation.ShortPreview" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Image
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Image" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.TShirt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.TShirt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Audio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Audio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Mesh
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Mesh" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Lua
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Lua" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Hat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Hat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Place
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Place" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Model
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Model" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Shirt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Shirt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Pants
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Pants" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Decal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Decal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Head
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Head" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Face
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Face" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Gear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Gear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Badge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Badge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Animation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Animation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Torso
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Torso" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RightArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.RightArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.LeftArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.LeftLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RightLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.RightLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Package
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Package" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.GamePass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.GamePass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Plugin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Plugin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.MeshPart
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.MeshPart" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.HairAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.HairAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FaceAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.FaceAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.NeckAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.NeckAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ShoulderAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.ShoulderAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FrontAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.FrontAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.BackAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.BackAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.WaistAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.WaistAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ClimbAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.ClimbAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.DeathAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.DeathAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FallAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.FallAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.IdleAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.IdleAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.JumpAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.JumpAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RunAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.RunAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.SwimAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.SwimAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.WalkAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.WalkAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.PoseAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.PoseAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EarAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.EarAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EyeAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.EyeAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EmoteAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.EmoteAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Video
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.Video" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.TShirtAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.TShirtAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ShirtAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.ShirtAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.PantsAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.PantsAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.JacketAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.JacketAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.SweaterAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.SweaterAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ShortsAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.ShortsAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftShoeAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.LeftShoeAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RightShoeAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.RightShoeAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.DressSkirtAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.DressSkirtAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FontFamily
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.FontFamily" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EyebrowAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.EyebrowAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EyelashAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.EyelashAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.MoodAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.MoodAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicHead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.DynamicHead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FaceMakeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.FaceMakeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LipMakeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.LipMakeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EyeMakeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.EyeMakeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.VoxelFragment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.VoxelFragment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetType.AvatarBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetTypeVerification
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetTypeVerification.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetTypeVerification
    if not enumObj then
        return false
    end
    local item = enumObj.ClientOnly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetTypeVerification.ClientOnly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AssetTypeVerification
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AssetTypeVerification.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioApiRollout
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioApiRollout.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioApiRollout
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioApiRollout.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioApiRollout
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioApiRollout.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioChannelLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Mono
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioChannelLayout.Mono" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioChannelLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Stereo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioChannelLayout.Stereo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioChannelLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Quad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioChannelLayout.Quad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioChannelLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Surround_5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioChannelLayout.Surround_5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioChannelLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Surround_5_1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioChannelLayout.Surround_5_1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioChannelLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Surround_7_1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioChannelLayout.Surround_7_1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioChannelLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Surround_7_1_4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioChannelLayout.Surround_7_1_4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Peak
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Peak" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.LowShelf
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.LowShelf" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.HighShelf
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.HighShelf" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Lowpass12dB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Lowpass12dB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Lowpass24dB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Lowpass24dB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Lowpass48dB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Lowpass48dB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Highpass12dB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Highpass12dB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Highpass24dB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Highpass24dB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Highpass48dB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Highpass48dB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Bandpass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Bandpass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Notch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Notch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Lowpass6dB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioFilterType.Lowpass6dB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioSimulationFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioSimulationFidelity.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioSimulationFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioSimulationFidelity.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioSubType
    if not enumObj then
        return false
    end
    local item = enumObj.Music
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioSubType.Music" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioSubType
    if not enumObj then
        return false
    end
    local item = enumObj.SoundEffect
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioSubType.SoundEffect" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioWindowSize
    if not enumObj then
        return false
    end
    local item = enumObj.Small
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioWindowSize.Small" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioWindowSize
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioWindowSize.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AudioWindowSize
    if not enumObj then
        return false
    end
    local item = enumObj.Large
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AudioWindowSize.Large" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AuthorityMode
    if not enumObj then
        return false
    end
    local item = enumObj.Server
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AuthorityMode.Server" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AuthorityMode
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AuthorityMode.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AutoIndentRule
    if not enumObj then
        return false
    end
    local item = enumObj.Off
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AutoIndentRule.Off" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AutoIndentRule
    if not enumObj then
        return false
    end
    local item = enumObj.Absolute
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AutoIndentRule.Absolute" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AutoIndentRule
    if not enumObj then
        return false
    end
    local item = enumObj.Relative
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AutoIndentRule.Relative" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AutomaticSize
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AutomaticSize.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AutomaticSize
    if not enumObj then
        return false
    end
    local item = enumObj.X
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AutomaticSize.X" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AutomaticSize
    if not enumObj then
        return false
    end
    local item = enumObj.Y
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AutomaticSize.Y" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AutomaticSize
    if not enumObj then
        return false
    end
    local item = enumObj.XY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AutomaticSize.XY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.TShirt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.TShirt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Hat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.Hat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Shirt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.Shirt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Pants
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.Pants" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Head
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.Head" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Face
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.Face" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Gear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.Gear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.Torso
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.Torso" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RightArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.RightArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.LeftArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.LeftLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RightLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.RightLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.HairAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.HairAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FaceAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.FaceAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.NeckAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.NeckAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ShoulderAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.ShoulderAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FrontAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.FrontAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.BackAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.BackAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.WaistAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.WaistAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ClimbAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.ClimbAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FallAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.FallAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.IdleAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.IdleAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.JumpAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.JumpAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RunAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.RunAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.SwimAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.SwimAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.WalkAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.WalkAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EmoteAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.EmoteAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.TShirtAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.TShirtAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ShirtAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.ShirtAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.PantsAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.PantsAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.JacketAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.JacketAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.SweaterAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.SweaterAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.ShortsAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.ShortsAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftShoeAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.LeftShoeAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.RightShoeAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.RightShoeAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.DressSkirtAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.DressSkirtAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EyebrowAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.EyebrowAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EyelashAccessory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.EyelashAccessory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.MoodAnimation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.MoodAnimation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicHead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.DynamicHead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.FaceMakeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.FaceMakeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.LipMakeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.LipMakeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.EyeMakeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.EyeMakeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarAssetType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarAssetType.AvatarBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UniverseAudio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.UniverseAudio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UniverseVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.UniverseVideo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.PlaceAudio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.PlaceAudio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.PlaceVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.PlaceVideo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UserAudioEligible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.UserAudioEligible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UserAudio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.UserAudio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UserVideoEligible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.UserVideoEligible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UserVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.UserVideo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UserBanned
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarChatServiceFeature.UserBanned" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarChatServiceFeature
    if not enumObj then
        return false
    end
    local item = enumObj.UserVerifiedForVoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarChatServiceFeature.UserVerifiedForVoice"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarContextMenuOption
    if not enumObj then
        return false
    end
    local item = enumObj.Friend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarContextMenuOption.Friend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarContextMenuOption
    if not enumObj then
        return false
    end
    local item = enumObj.Chat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarContextMenuOption.Chat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarContextMenuOption
    if not enumObj then
        return false
    end
    local item = enumObj.Emote
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarContextMenuOption.Emote" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarContextMenuOption
    if not enumObj then
        return false
    end
    local item = enumObj.InspectMenu
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarContextMenuOption.InspectMenu" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarGenerationError
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarGenerationError.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarGenerationError
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarGenerationError.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarGenerationError
    if not enumObj then
        return false
    end
    local item = enumObj.DownloadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarGenerationError.DownloadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarGenerationError
    if not enumObj then
        return false
    end
    local item = enumObj.Canceled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarGenerationError.Canceled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarGenerationError
    if not enumObj then
        return false
    end
    local item = enumObj.Offensive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarGenerationError.Offensive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarGenerationError
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarGenerationError.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarGenerationError
    if not enumObj then
        return false
    end
    local item = enumObj.JobNotFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarGenerationError.JobNotFound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarItemType
    if not enumObj then
        return false
    end
    local item = enumObj.Asset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarItemType.Asset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarItemType
    if not enumObj then
        return false
    end
    local item = enumObj.Bundle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarItemType.Bundle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarPromptResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarPromptResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarPromptResult
    if not enumObj then
        return false
    end
    local item = enumObj.PermissionDenied
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarPromptResult.PermissionDenied" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarPromptResult
    if not enumObj then
        return false
    end
    local item = enumObj.Failed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarPromptResult.Failed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAccessoryLimitMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Scale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAccessoryLimitMethod.Scale" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAccessoryLimitMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Remove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAccessoryLimitMethod.Remove" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAccessoryLimitMethod
    if not enumObj then
        return false
    end
    local item = enumObj.PreviewScale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsAccessoryLimitMethod.PreviewScale"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAccessoryLimitMethod
    if not enumObj then
        return false
    end
    local item = enumObj.PreviewRemove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AvatarSettingsAccessoryLimitMethod.PreviewRemove"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAccessoryMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAccessoryMode.PlayerChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAccessoryMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomLimit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAccessoryMode.CustomLimit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAnimationClipsMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsAnimationClipsMode.PlayerChoice"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAnimationClipsMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomClips
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsAnimationClipsMode.CustomClips"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAnimationPacksMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsAnimationPacksMode.PlayerChoice"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAnimationPacksMode
    if not enumObj then
        return false
    end
    local item = enumObj.StandardR15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsAnimationPacksMode.StandardR15"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAnimationPacksMode
    if not enumObj then
        return false
    end
    local item = enumObj.StandardR6
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAnimationPacksMode.StandardR6" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAppearanceMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAppearanceMode.PlayerChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAppearanceMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomParts
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAppearanceMode.CustomParts" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsAppearanceMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomBody
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsAppearanceMode.CustomBody" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsBuildMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsBuildMode.PlayerChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsBuildMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomBuild
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsBuildMode.CustomBuild" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCharacterControllerMode
    if not enumObj then
        return false
    end
    local item = enumObj.LegacyHumanoid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AvatarSettingsCharacterControllerMode.LegacyHumanoid"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCharacterControllerMode
    if not enumObj then
        return false
    end
    local item = enumObj.LuaCharacterController
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AvatarSettingsCharacterControllerMode.LuaCharacterController"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsClothingMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsClothingMode.PlayerChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsClothingMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomLimit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsClothingMode.CustomLimit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCollisionMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsCollisionMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCollisionMode
    if not enumObj then
        return false
    end
    local item = enumObj.SingleCollider
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsCollisionMode.SingleCollider" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCollisionMode
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsCollisionMode.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCustomAccessoryMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsCustomAccessoryMode.PlayerChoice"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCustomAccessoryMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomAccessories
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AvatarSettingsCustomAccessoryMode.CustomAccessories"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCustomBodyType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarReference
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsCustomBodyType.AvatarReference"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCustomBodyType
    if not enumObj then
        return false
    end
    local item = enumObj.BundleId
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsCustomBodyType.BundleId" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCustomClothingMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsCustomClothingMode.PlayerChoice"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsCustomClothingMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomClothing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsCustomClothingMode.CustomClothing"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsHitAndTouchDetectionMode
    if not enumObj then
        return false
    end
    local item = enumObj.UseParts
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsHitAndTouchDetectionMode.UseParts"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsHitAndTouchDetectionMode
    if not enumObj then
        return false
    end
    local item = enumObj.UseCollider
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AvatarSettingsHitAndTouchDetectionMode.UseCollider"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsJumpMode
    if not enumObj then
        return false
    end
    local item = enumObj.JumpHeight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsJumpMode.JumpHeight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsJumpMode
    if not enumObj then
        return false
    end
    local item = enumObj.JumpPower
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsJumpMode.JumpPower" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsLegacyCollisionMode
    if not enumObj then
        return false
    end
    local item = enumObj.R6Colliders
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.AvatarSettingsLegacyCollisionMode.R6Colliders"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsLegacyCollisionMode
    if not enumObj then
        return false
    end
    local item = enumObj.InnerBoxColliders
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.AvatarSettingsLegacyCollisionMode.InnerBoxColliders"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsScaleMode
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsScaleMode.PlayerChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarSettingsScaleMode
    if not enumObj then
        return false
    end
    local item = enumObj.CustomScale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarSettingsScaleMode.CustomScale" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarThumbnailCustomizationType
    if not enumObj then
        return false
    end
    local item = enumObj.Closeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarThumbnailCustomizationType.Closeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarThumbnailCustomizationType
    if not enumObj then
        return false
    end
    local item = enumObj.FullBody
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarThumbnailCustomizationType.FullBody" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarUnificationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarUnificationMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarUnificationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarUnificationMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.AvatarUnificationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.AvatarUnificationMode.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Axis
    if not enumObj then
        return false
    end
    local item = enumObj.X
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Axis.X" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Axis
    if not enumObj then
        return false
    end
    local item = enumObj.Y
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Axis.Y" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Axis
    if not enumObj then
        return false
    end
    local item = enumObj.Z
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Axis.Z" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BenefitType
    if not enumObj then
        return false
    end
    local item = enumObj.DeveloperProduct
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BenefitType.DeveloperProduct" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BenefitType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarAsset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BenefitType.AvatarAsset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BenefitType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarBundle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BenefitType.AvatarBundle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BinType
    if not enumObj then
        return false
    end
    local item = enumObj.Script
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BinType.Script" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BinType
    if not enumObj then
        return false
    end
    local item = enumObj.GameTool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BinType.GameTool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BinType
    if not enumObj then
        return false
    end
    local item = enumObj.Grab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BinType.Grab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BinType
    if not enumObj then
        return false
    end
    local item = enumObj.Clone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BinType.Clone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BinType
    if not enumObj then
        return false
    end
    local item = enumObj.Hammer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BinType.Hammer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPart
    if not enumObj then
        return false
    end
    local item = enumObj.Head
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPart.Head" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPart
    if not enumObj then
        return false
    end
    local item = enumObj.Torso
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPart.Torso" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPart
    if not enumObj then
        return false
    end
    local item = enumObj.LeftArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPart.LeftArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPart
    if not enumObj then
        return false
    end
    local item = enumObj.RightArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPart.RightArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPart
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPart.LeftLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPart
    if not enumObj then
        return false
    end
    local item = enumObj.RightLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPart.RightLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.Head
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.Head" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.UpperTorso
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.UpperTorso" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.LowerTorso
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.LowerTorso" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.LeftFoot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.LeftFoot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLowerLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.LeftLowerLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.LeftUpperLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.LeftUpperLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.RightFoot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.RightFoot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.RightLowerLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.RightLowerLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.RightUpperLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.RightUpperLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.LeftHand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.LeftHand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLowerArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.LeftLowerArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.LeftUpperArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.LeftUpperArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.RightHand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.RightHand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.RightLowerArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.RightLowerArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.RightUpperArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.RightUpperArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.RootPart
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.RootPart" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BodyPartR15
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BodyPartR15.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BorderMode
    if not enumObj then
        return false
    end
    local item = enumObj.Outline
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BorderMode.Outline" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BorderMode
    if not enumObj then
        return false
    end
    local item = enumObj.Middle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BorderMode.Middle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BorderMode
    if not enumObj then
        return false
    end
    local item = enumObj.Inset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BorderMode.Inset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BorderStrokePosition
    if not enumObj then
        return false
    end
    local item = enumObj.Outer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BorderStrokePosition.Outer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BorderStrokePosition
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BorderStrokePosition.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BorderStrokePosition
    if not enumObj then
        return false
    end
    local item = enumObj.Inner
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BorderStrokePosition.Inner" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BreakReason
    if not enumObj then
        return false
    end
    local item = enumObj.Other
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BreakReason.Other" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BreakReason
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BreakReason.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BreakReason
    if not enumObj then
        return false
    end
    local item = enumObj.SpecialBreakpoint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BreakReason.SpecialBreakpoint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BreakReason
    if not enumObj then
        return false
    end
    local item = enumObj.UserBreakpoint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BreakReason.UserBreakpoint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BreakpointRemoveReason
    if not enumObj then
        return false
    end
    local item = enumObj.Requested
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BreakpointRemoveReason.Requested" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BreakpointRemoveReason
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptChanged
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BreakpointRemoveReason.ScriptChanged" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BreakpointRemoveReason
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptRemoved
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BreakpointRemoveReason.ScriptRemoved" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BulkMoveMode
    if not enumObj then
        return false
    end
    local item = enumObj.FireAllEvents
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BulkMoveMode.FireAllEvents" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BulkMoveMode
    if not enumObj then
        return false
    end
    local item = enumObj.FireCFrameChanged
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BulkMoveMode.FireCFrameChanged" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BundleType
    if not enumObj then
        return false
    end
    local item = enumObj.BodyParts
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BundleType.BodyParts" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BundleType
    if not enumObj then
        return false
    end
    local item = enumObj.Animations
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BundleType.Animations" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BundleType
    if not enumObj then
        return false
    end
    local item = enumObj.Shoes
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BundleType.Shoes" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BundleType
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicHead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BundleType.DynamicHead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.BundleType
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicHeadAvatar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.BundleType.DynamicHeadAvatar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Button
    if not enumObj then
        return false
    end
    local item = enumObj.Dismount
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Button.Dismount" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Button
    if not enumObj then
        return false
    end
    local item = enumObj.Jump
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Button.Jump" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ButtonStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ButtonStyle.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ButtonStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxButtonDefault
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ButtonStyle.RobloxButtonDefault" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ButtonStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ButtonStyle.RobloxButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ButtonStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxRoundButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ButtonStyle.RobloxRoundButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ButtonStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxRoundDefaultButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ButtonStyle.RobloxRoundDefaultButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ButtonStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxRoundDropdownButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ButtonStyle.RobloxRoundDropdownButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CageType
    if not enumObj then
        return false
    end
    local item = enumObj.Inner
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CageType.Inner" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CageType
    if not enumObj then
        return false
    end
    local item = enumObj.Outer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CageType.Outer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraMode
    if not enumObj then
        return false
    end
    local item = enumObj.LockFirstPerson
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraMode.LockFirstPerson" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraNavigationModel
    if not enumObj then
        return false
    end
    local item = enumObj.Roblox
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraNavigationModel.Roblox" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraNavigationModel
    if not enumObj then
        return false
    end
    local item = enumObj.IndustryCompatible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraNavigationModel.IndustryCompatible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraPanMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraPanMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraPanMode
    if not enumObj then
        return false
    end
    local item = enumObj.EdgeBump
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraPanMode.EdgeBump" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraSpeedAdjustBinding
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraSpeedAdjustBinding.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraSpeedAdjustBinding
    if not enumObj then
        return false
    end
    local item = enumObj.RmbScroll
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraSpeedAdjustBinding.RmbScroll" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraSpeedAdjustBinding
    if not enumObj then
        return false
    end
    local item = enumObj.AltScroll
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraSpeedAdjustBinding.AltScroll" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Fixed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Fixed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Attach
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Attach" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Watch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Watch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Track
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Track" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Follow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Follow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Scriptable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Scriptable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CameraType
    if not enumObj then
        return false
    end
    local item = enumObj.Orbital
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CameraType.Orbital" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.Invalid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.Invalid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.NotAgeVerified
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.NotAgeVerified" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.OutsideAgeBucket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.OutsideAgeBucket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.TooManyCollaborators
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.TooManyCollaborators" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.PCBlock
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.PCBlock" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.NotFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.NotFound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.OutsideOwnerAgeBucket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.OutsideOwnerAgeBucket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.NotAuthorized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.NotAuthorized" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CanCollaborateError
    if not enumObj then
        return false
    end
    local item = enumObj.OutsideAgeBucketTcPc
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CanCollaborateError.OutsideAgeBucketTcPc" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CaptureGalleryPermission
    if not enumObj then
        return false
    end
    local item = enumObj.ReadAndUpload
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CaptureGalleryPermission.ReadAndUpload" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CaptureType
    if not enumObj then
        return false
    end
    local item = enumObj.Screenshot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CaptureType.Screenshot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CaptureType
    if not enumObj then
        return false
    end
    local item = enumObj.Video
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CaptureType.Video" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogCategoryFilter
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogCategoryFilter.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogCategoryFilter
    if not enumObj then
        return false
    end
    local item = enumObj.Featured
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogCategoryFilter.Featured" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogCategoryFilter
    if not enumObj then
        return false
    end
    local item = enumObj.Collectibles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogCategoryFilter.Collectibles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogCategoryFilter
    if not enumObj then
        return false
    end
    local item = enumObj.CommunityCreations
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogCategoryFilter.CommunityCreations" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogCategoryFilter
    if not enumObj then
        return false
    end
    local item = enumObj.Premium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogCategoryFilter.Premium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogCategoryFilter
    if not enumObj then
        return false
    end
    local item = enumObj.Recommended
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogCategoryFilter.Recommended" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortAggregation
    if not enumObj then
        return false
    end
    local item = enumObj.Past12Hours
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortAggregation.Past12Hours" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortAggregation
    if not enumObj then
        return false
    end
    local item = enumObj.PastDay
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortAggregation.PastDay" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortAggregation
    if not enumObj then
        return false
    end
    local item = enumObj.Past3Days
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortAggregation.Past3Days" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortAggregation
    if not enumObj then
        return false
    end
    local item = enumObj.PastWeek
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortAggregation.PastWeek" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortAggregation
    if not enumObj then
        return false
    end
    local item = enumObj.PastMonth
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortAggregation.PastMonth" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortAggregation
    if not enumObj then
        return false
    end
    local item = enumObj.AllTime
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortAggregation.AllTime" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortType
    if not enumObj then
        return false
    end
    local item = enumObj.Relevance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortType.Relevance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortType
    if not enumObj then
        return false
    end
    local item = enumObj.PriceHighToLow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortType.PriceHighToLow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortType
    if not enumObj then
        return false
    end
    local item = enumObj.PriceLowToHigh
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortType.PriceLowToHigh" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortType
    if not enumObj then
        return false
    end
    local item = enumObj.MostFavorited
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortType.MostFavorited" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortType
    if not enumObj then
        return false
    end
    local item = enumObj.RecentlyCreated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortType.RecentlyCreated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CatalogSortType
    if not enumObj then
        return false
    end
    local item = enumObj.Bestselling
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CatalogSortType.Bestselling" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellBlock
    if not enumObj then
        return false
    end
    local item = enumObj.Solid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellBlock.Solid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellBlock
    if not enumObj then
        return false
    end
    local item = enumObj.VerticalWedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellBlock.VerticalWedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellBlock
    if not enumObj then
        return false
    end
    local item = enumObj.CornerWedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellBlock.CornerWedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellBlock
    if not enumObj then
        return false
    end
    local item = enumObj.InverseCornerWedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellBlock.InverseCornerWedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellBlock
    if not enumObj then
        return false
    end
    local item = enumObj.HorizontalWedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellBlock.HorizontalWedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Empty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Empty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Grass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Grass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Sand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Sand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Brick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Brick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Granite
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Granite" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Asphalt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Asphalt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Iron
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Iron" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Aluminum
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Aluminum" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Gold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Gold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.WoodPlank
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.WoodPlank" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.WoodLog
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.WoodLog" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Gravel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Gravel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.CinderBlock
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.CinderBlock" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.MossyStone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.MossyStone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Cement
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Cement" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.RedPlastic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.RedPlastic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.BluePlastic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.BluePlastic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellMaterial
    if not enumObj then
        return false
    end
    local item = enumObj.Water
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellMaterial.Water" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.NegZ
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellOrientation.NegZ" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.X
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellOrientation.X" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.Z
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellOrientation.Z" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CellOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.NegX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CellOrientation.NegX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CenterDialogType
    if not enumObj then
        return false
    end
    local item = enumObj.UnsolicitedDialog
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CenterDialogType.UnsolicitedDialog" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CenterDialogType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerInitiatedDialog
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CenterDialogType.PlayerInitiatedDialog" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CenterDialogType
    if not enumObj then
        return false
    end
    local item = enumObj.ModalDialog
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CenterDialogType.ModalDialog" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CenterDialogType
    if not enumObj then
        return false
    end
    local item = enumObj.QuitDialog
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CenterDialogType.QuitDialog" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CharacterControlMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CharacterControlMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CharacterControlMode
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CharacterControlMode.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CharacterControlMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoCharacterController
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CharacterControlMode.NoCharacterController" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CharacterControlMode
    if not enumObj then
        return false
    end
    local item = enumObj.LuaCharacterController
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CharacterControlMode.LuaCharacterController" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatCallbackType
    if not enumObj then
        return false
    end
    local item = enumObj.OnCreatingChatWindow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatCallbackType.OnCreatingChatWindow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatCallbackType
    if not enumObj then
        return false
    end
    local item = enumObj.OnClientSendingMessage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatCallbackType.OnClientSendingMessage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatCallbackType
    if not enumObj then
        return false
    end
    local item = enumObj.OnClientFormattingMessage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatCallbackType.OnClientFormattingMessage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatCallbackType
    if not enumObj then
        return false
    end
    local item = enumObj.OnServerReceivingMessage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatCallbackType.OnServerReceivingMessage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatColor
    if not enumObj then
        return false
    end
    local item = enumObj.Blue
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatColor.Blue" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatColor
    if not enumObj then
        return false
    end
    local item = enumObj.Green
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatColor.Green" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatColor
    if not enumObj then
        return false
    end
    local item = enumObj.Red
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatColor.Red" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatColor
    if not enumObj then
        return false
    end
    local item = enumObj.White
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatColor.White" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatMode
    if not enumObj then
        return false
    end
    local item = enumObj.Menu
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatMode.Menu" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatMode
    if not enumObj then
        return false
    end
    local item = enumObj.TextAndMenu
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatMode.TextAndMenu" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatPrivacyMode
    if not enumObj then
        return false
    end
    local item = enumObj.AllUsers
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatPrivacyMode.AllUsers" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatPrivacyMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatPrivacyMode.NoOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatPrivacyMode
    if not enumObj then
        return false
    end
    local item = enumObj.Friends
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatPrivacyMode.Friends" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatRestrictionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatRestrictionStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatRestrictionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotRestricted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatRestrictionStatus.NotRestricted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatRestrictionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Restricted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatRestrictionStatus.Restricted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatStyle.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Bubble
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatStyle.Bubble" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatStyle
    if not enumObj then
        return false
    end
    local item = enumObj.ClassicAndBubble
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatStyle.ClassicAndBubble" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatVersion
    if not enumObj then
        return false
    end
    local item = enumObj.LegacyChatService
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatVersion.LegacyChatService" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ChatVersion
    if not enumObj then
        return false
    end
    local item = enumObj.TextChatService
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ChatVersion.TextChatService" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ClientAnimatorThrottlingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ClientAnimatorThrottlingMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ClientAnimatorThrottlingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ClientAnimatorThrottlingMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ClientAnimatorThrottlingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ClientAnimatorThrottlingMode.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CloseReason
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CloseReason.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CloseReason
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxMaintenance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CloseReason.RobloxMaintenance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CloseReason
    if not enumObj then
        return false
    end
    local item = enumObj.DeveloperShutdown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CloseReason.DeveloperShutdown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CloseReason
    if not enumObj then
        return false
    end
    local item = enumObj.DeveloperUpdate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CloseReason.DeveloperUpdate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CloseReason
    if not enumObj then
        return false
    end
    local item = enumObj.ServerEmpty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CloseReason.ServerEmpty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CloseReason
    if not enumObj then
        return false
    end
    local item = enumObj.OutOfMemory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CloseReason.OutOfMemory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CloseReason
    if not enumObj then
        return false
    end
    local item = enumObj.Moderation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CloseReason.Moderation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollaboratorStatus
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollaboratorStatus.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollaboratorStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Editing3D
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollaboratorStatus.Editing3D" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollaboratorStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Scripting
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollaboratorStatus.Scripting" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollaboratorStatus
    if not enumObj then
        return false
    end
    local item = enumObj.PrivateScripting
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollaboratorStatus.PrivateScripting" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollisionFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollisionFidelity.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollisionFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Hull
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollisionFidelity.Hull" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollisionFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Box
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollisionFidelity.Box" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollisionFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.PreciseConvexDecomposition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.CollisionFidelity.PreciseConvexDecomposition"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CollisionFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Scalable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CollisionFidelity.Scalable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CommandPermission
    if not enumObj then
        return false
    end
    local item = enumObj.Plugin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CommandPermission.Plugin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CommandPermission
    if not enumObj then
        return false
    end
    local item = enumObj.LocalUser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CommandPermission.LocalUser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompileTarget
    if not enumObj then
        return false
    end
    local item = enumObj.Client
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompileTarget.Client" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompileTarget
    if not enumObj then
        return false
    end
    local item = enumObj.CoreScript
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompileTarget.CoreScript" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompileTarget
    if not enumObj then
        return false
    end
    local item = enumObj.Studio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompileTarget.Studio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompileTarget
    if not enumObj then
        return false
    end
    local item = enumObj.CoreScriptRaw
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompileTarget.CoreScriptRaw" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionAcceptanceBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Insert
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionAcceptanceBehavior.Insert" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionAcceptanceBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Replace
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionAcceptanceBehavior.Replace" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionAcceptanceBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.ReplaceOnEnterInsertOnTab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.CompletionAcceptanceBehavior.ReplaceOnEnterInsertOnTab"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionAcceptanceBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.InsertOnEnterReplaceOnTab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.CompletionAcceptanceBehavior.InsertOnEnterReplaceOnTab"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Text
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Text" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Method
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Method" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Function
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Function" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Constructor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Constructor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Field
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Field" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Variable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Variable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Class
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Class" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Interface
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Interface" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Module
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Module" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Property
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Property" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Unit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Unit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Value
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Value" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Enum
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Enum" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Keyword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Keyword" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Snippet
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Snippet" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Color
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Color" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.File
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.File" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Reference
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Reference" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Folder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Folder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.EnumMember
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.EnumMember" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Constant
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Constant" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Struct
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Struct" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Event
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Event" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.Operator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.Operator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemKind
    if not enumObj then
        return false
    end
    local item = enumObj.TypeParameter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemKind.TypeParameter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.Deprecated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.Deprecated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.IncorrectIndexType
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.IncorrectIndexType" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.PluginPermissions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.PluginPermissions" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.CommandLinePermissions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.CommandLinePermissions" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxPermissions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.RobloxPermissions" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.AddParens
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.AddParens" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.PutCursorInParens
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.PutCursorInParens" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.TypeCorrect
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.TypeCorrect" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.ClientServerBoundaryViolation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.CompletionItemTag.ClientServerBoundaryViolation"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.Invalidated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.Invalidated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionItemTag
    if not enumObj then
        return false
    end
    local item = enumObj.PutCursorBeforeEnd
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionItemTag.PutCursorBeforeEnd" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionTriggerKind
    if not enumObj then
        return false
    end
    local item = enumObj.Invoked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionTriggerKind.Invoked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionTriggerKind
    if not enumObj then
        return false
    end
    local item = enumObj.TriggerCharacter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompletionTriggerKind.TriggerCharacter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompletionTriggerKind
    if not enumObj then
        return false
    end
    local item = enumObj.TriggerForIncompleteCompletions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.CompletionTriggerKind.TriggerForIncompleteCompletions"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.ColorRGB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.ColorRGB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.ColorHSV
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.ColorHSV" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.NumberRange
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.NumberRange" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.Rect
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.Rect" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.UDim
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.UDim" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.UDim2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.UDim2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.Vector2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.Vector2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompositeValueCurveType
    if not enumObj then
        return false
    end
    local item = enumObj.Vector3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompositeValueCurveType.Vector3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CompressionAlgorithm
    if not enumObj then
        return false
    end
    local item = enumObj.Zstd
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CompressionAlgorithm.Zstd" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerCameraMovementMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerCameraMovementMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Follow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerCameraMovementMode.Follow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Orbital
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerCameraMovementMode.Orbital" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.CameraToggle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerCameraMovementMode.CameraToggle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerMovementMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.KeyboardMouse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerMovementMode.KeyboardMouse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ComputerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.ClickToMove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ComputerMovementMode.ClickToMove" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConfigSnapshotErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConfigSnapshotErrorState.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConfigSnapshotErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.LoadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConfigSnapshotErrorState.LoadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.OK
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.OK" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.ConnectErrors
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.ConnectErrors" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.AlreadyConnected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.AlreadyConnected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.NoFreeIncomingConnections
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.NoFreeIncomingConnections" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.ConnectionBanned
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.ConnectionBanned" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidPassword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.InvalidPassword" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.IncompatibleProtocolVersion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.IncompatibleProtocolVersion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.IPRecentlyConnected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.IPRecentlyConnected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.OurSystemRequiresSecurity
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.OurSystemRequiresSecurity" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.SecurityKeyMismatch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.SecurityKeyMismatch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectErrors
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectErrors" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectBadhash
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectBadhash" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectSecurityKeyMismatch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ConnectionError.DisconnectSecurityKeyMismatch"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectProtocolMismatch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectProtocolMismatch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectReceivePacketError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ConnectionError.DisconnectReceivePacketError"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectReceivePacketStreamError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectReceivePacketStreamError"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectSendPacketError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectSendPacketError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectIllegalTeleport
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectIllegalTeleport" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectDuplicatePlayer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectDuplicatePlayer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectDuplicateTicket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectDuplicateTicket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectTimeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectTimeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectLuaKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectLuaKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectOnRemoteSysStats
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectOnRemoteSysStats" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectHashTimeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectHashTimeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCloudEditKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectCloudEditKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectPlayerless
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectPlayerless" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectNewSecurityKeyMismatch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectNewSecurityKeyMismatch"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectEvicted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectEvicted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectDevMaintenance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectDevMaintenance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRobloxMaintenance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectRobloxMaintenance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRejoin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectRejoin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectConnectionLost
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectConnectionLost" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectIdle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectIdle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRaknetErrors
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectRaknetErrors" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectWrongVersion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectWrongVersion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectBySecurityPolicy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectBySecurityPolicy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectBlockedIP
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectBlockedIP" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectClientFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectClientFailure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectClientRequest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectClientRequest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectPrivateServerKickout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ConnectionError.DisconnectPrivateServerKickout"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectModeratedGame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectModeratedGame" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.ServerShutdown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.ServerShutdown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.ReplicatorTimeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.ReplicatorTimeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerRemoved
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlayerRemoved" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectOutOfMemoryKeepPlayingLeave
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectOutOfMemoryKeepPlayingLeave"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRomarkEndOfTest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectRomarkEndOfTest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorPermissionRevoked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorPermissionRevoked"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorUnderage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ConnectionError.DisconnectCollaboratorUnderage"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.NetworkInternal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.NetworkInternal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.NetworkSend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.NetworkSend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.NetworkTimeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.NetworkTimeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.NetworkMisbehavior
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.NetworkMisbehavior" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.NetworkSecurity
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.NetworkSecurity" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.ReplacementReady
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.ReplacementReady" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.ServerEmpty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.ServerEmpty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PhantomFreeze
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PhantomFreeze" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.AndroidAnticheatKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.AndroidAnticheatKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.AndroidEmulatorKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.AndroidEmulatorKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.AndroidRootedKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.AndroidRootedKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.ScreentimeLockoutKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.ScreentimeLockoutKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectionNotification
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.DisconnectionNotification" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectVerboselyModeratedGame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectVerboselyModeratedGame"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorNotAgeVerified
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorNotAgeVerified"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorTrustedConnectionsRequired
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorTrustedConnectionsRequired"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorOwnerActionRequired
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorOwnerActionRequired"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorTooManyCollaborators
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorTooManyCollaborators"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorUnknownError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorUnknownError"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorRequestedEviction
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorRequestedEviction"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectCollaboratorTrustedConnectionsRequiredPC
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectCollaboratorTrustedConnectionsRequiredPC"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRemoteAttestationUnsupported
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectRemoteAttestationUnsupported"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRemoteAttestationGeneralFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectRemoteAttestationGeneralFailure"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRemoteAttestationTimeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectRemoteAttestationTimeout"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRemoteAttestationOSOutOfDate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectRemoteAttestationOSOutOfDate"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.DisconnectRemoteAttestationBootValidationFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.DisconnectRemoteAttestationBootValidationFailure"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchErrors
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchErrors" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchDisabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchDisabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchGameEnded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchGameEnded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchGameFull
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchGameFull" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchUserLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchUserLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchRestricted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchRestricted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchUnauthorized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchUnauthorized" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchFlooded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchFlooded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchHashExpired
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchHashExpired" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchHashException
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchHashException" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchPartyCannotFit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchPartyCannotFit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchHttpError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchHttpError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchUserPrivacyUnauthorized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.PlacelaunchUserPrivacyUnauthorized"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchVipOwnerNotPresent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ConnectionError.PlacelaunchVipOwnerNotPresent"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchAgeVerificationRequired
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.PlacelaunchAgeVerificationRequired"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchParentalApprovalRequired
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.PlacelaunchParentalApprovalRequired"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchCoreGated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchCoreGated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchCollaborationCoreGated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ConnectionError.PlacelaunchCollaborationCoreGated"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchCreatorBan
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchCreatorBan" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchDeviceBlock
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchDeviceBlock" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchCustomMessage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchCustomMessage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.PlacelaunchOtherError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.PlacelaunchOtherError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportErrors
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportErrors" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportFailure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportGameNotFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportGameNotFound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportGameEnded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportGameEnded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportGameFull
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportGameFull" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportUnauthorized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportUnauthorized" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportFlooded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportFlooded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionError
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportIsTeleporting
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionError.TeleportIsTeleporting" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionState
    if not enumObj then
        return false
    end
    local item = enumObj.Connected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionState.Connected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ConnectionState
    if not enumObj then
        return false
    end
    local item = enumObj.Disconnected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ConnectionState.Disconnected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContentSourceType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContentSourceType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContentSourceType
    if not enumObj then
        return false
    end
    local item = enumObj.Uri
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContentSourceType.Uri" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContentSourceType
    if not enumObj then
        return false
    end
    local item = enumObj.Object
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContentSourceType.Object" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContentSourceType
    if not enumObj then
        return false
    end
    local item = enumObj.Opaque
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContentSourceType.Opaque" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContextActionPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Low
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContextActionPriority.Low" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContextActionPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContextActionPriority.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContextActionPriority
    if not enumObj then
        return false
    end
    local item = enumObj.High
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContextActionPriority.High" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContextActionResult
    if not enumObj then
        return false
    end
    local item = enumObj.Sink
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContextActionResult.Sink" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ContextActionResult
    if not enumObj then
        return false
    end
    local item = enumObj.Pass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ContextActionResult.Pass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ControlMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ControlMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ControlMode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseLockSwitch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ControlMode.MouseLockSwitch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerList
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.PlayerList" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.Health
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.Health" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.Backpack
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.Backpack" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.Chat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.Chat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.EmotesMenu
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.EmotesMenu" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.SelfView
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.SelfView" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.Captures
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.Captures" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarSwitcher
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.AvatarSwitcher" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CoreGuiType
    if not enumObj then
        return false
    end
    local item = enumObj.ExperienceShop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CoreGuiType.ExperienceShop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateAssetResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.PermissionDenied
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateAssetResult.PermissionDenied" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateAssetResult.UploadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateAssetResult.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateContentResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateContentResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateContentResult
    if not enumObj then
        return false
    end
    local item = enumObj.PermissionDenied
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateContentResult.PermissionDenied" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateContentResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateContentResult.UploadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateContentResult
    if not enumObj then
        return false
    end
    local item = enumObj.StorageLimitExceeded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateContentResult.StorageLimitExceeded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateContentResult
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateContentResult.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateOutfitFailure
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateOutfitFailure.InvalidName" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateOutfitFailure
    if not enumObj then
        return false
    end
    local item = enumObj.OutfitLimitReached
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateOutfitFailure.OutfitLimitReached" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreateOutfitFailure
    if not enumObj then
        return false
    end
    local item = enumObj.Other
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreateOutfitFailure.Other" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreatorType
    if not enumObj then
        return false
    end
    local item = enumObj.User
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreatorType.User" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreatorType
    if not enumObj then
        return false
    end
    local item = enumObj.Group
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreatorType.Group" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreatorTypeFilter
    if not enumObj then
        return false
    end
    local item = enumObj.User
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreatorTypeFilter.User" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreatorTypeFilter
    if not enumObj then
        return false
    end
    local item = enumObj.Group
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreatorTypeFilter.Group" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CreatorTypeFilter
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CreatorTypeFilter.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CurrencyType
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CurrencyType.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CurrencyType
    if not enumObj then
        return false
    end
    local item = enumObj.Robux
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CurrencyType.Robux" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CurrencyType
    if not enumObj then
        return false
    end
    local item = enumObj.Tix
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CurrencyType.Tix" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CustomCameraMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CustomCameraMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CustomCameraMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CustomCameraMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.CustomCameraMode
    if not enumObj then
        return false
    end
    local item = enumObj.Follow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.CustomCameraMode.Follow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataModelExtractorFileType
    if not enumObj then
        return false
    end
    local item = enumObj.PlaceFile
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataModelExtractorFileType.PlaceFile" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataModelExtractorFileType
    if not enumObj then
        return false
    end
    local item = enumObj.FirstSlice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataModelExtractorFileType.FirstSlice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataModelExtractorFileType
    if not enumObj then
        return false
    end
    local item = enumObj.NonFirstSlice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataModelExtractorFileType.NonFirstSlice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.GetAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.GetAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.SetIncrementAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.SetIncrementAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.UpdateAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.UpdateAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.GetSortedAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.GetSortedAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.SetIncrementSortedAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.DataStoreRequestType.SetIncrementSortedAsync"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.OnUpdate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.OnUpdate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.ListAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.ListAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.GetVersionAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.GetVersionAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.RemoveVersionAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.RemoveVersionAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.StandardRead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.StandardRead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.StandardWrite
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.StandardWrite" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.StandardList
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.StandardList" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.StandardRemove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.StandardRemove" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.OrderedRead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.OrderedRead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.OrderedWrite
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.OrderedWrite" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.OrderedList
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.OrderedList" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DataStoreRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.OrderedRemove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DataStoreRequestType.OrderedRemove" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebugBreakModeType
    if not enumObj then
        return false
    end
    local item = enumObj.Never
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebugBreakModeType.Never" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebugBreakModeType
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebugBreakModeType.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebugBreakModeType
    if not enumObj then
        return false
    end
    local item = enumObj.Unhandled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebugBreakModeType.Unhandled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.ClientRequest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.ClientRequest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidHost
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.InvalidHost" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.Disconnected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.Disconnected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.ServerShutdown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.ServerShutdown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.ServerProtocolMismatch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.ServerProtocolMismatch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.ConfigurationFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.ConfigurationFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerEndReason
    if not enumObj then
        return false
    end
    local item = enumObj.RpcError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerEndReason.RpcError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerExceptionBreakMode
    if not enumObj then
        return false
    end
    local item = enumObj.Never
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerExceptionBreakMode.Never" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerExceptionBreakMode
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerExceptionBreakMode.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerExceptionBreakMode
    if not enumObj then
        return false
    end
    local item = enumObj.Unhandled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerExceptionBreakMode.Unhandled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerFrameType
    if not enumObj then
        return false
    end
    local item = enumObj.C
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerFrameType.C" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerFrameType
    if not enumObj then
        return false
    end
    local item = enumObj.Lua
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerFrameType.Lua" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerPauseReason
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerPauseReason.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerPauseReason
    if not enumObj then
        return false
    end
    local item = enumObj.Requested
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerPauseReason.Requested" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerPauseReason
    if not enumObj then
        return false
    end
    local item = enumObj.Breakpoint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerPauseReason.Breakpoint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerPauseReason
    if not enumObj then
        return false
    end
    local item = enumObj.Exception
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerPauseReason.Exception" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerPauseReason
    if not enumObj then
        return false
    end
    local item = enumObj.SingleStep
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerPauseReason.SingleStep" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerPauseReason
    if not enumObj then
        return false
    end
    local item = enumObj.Entrypoint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerPauseReason.Entrypoint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerResumeType
    if not enumObj then
        return false
    end
    local item = enumObj.StepInto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerResumeType.StepInto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerResumeType
    if not enumObj then
        return false
    end
    local item = enumObj.StepOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerResumeType.StepOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerResumeType
    if not enumObj then
        return false
    end
    local item = enumObj.StepOver
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerResumeType.StepOver" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerResumeType
    if not enumObj then
        return false
    end
    local item = enumObj.Resume
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerResumeType.Resume" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ConnectionLost
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.ConnectionLost" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidResponse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.InvalidResponse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InternalError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.InternalError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidState
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.InvalidState" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.RpcError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.RpcError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidArgument
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.InvalidArgument" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DebuggerStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ConnectionClosed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DebuggerStatus.ConnectionClosed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DefaultScriptSyncFileType
    if not enumObj then
        return false
    end
    local item = enumObj.Lua
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DefaultScriptSyncFileType.Lua" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DefaultScriptSyncFileType
    if not enumObj then
        return false
    end
    local item = enumObj.Luau
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DefaultScriptSyncFileType.Luau" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevCameraOcclusionMode
    if not enumObj then
        return false
    end
    local item = enumObj.Zoom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevCameraOcclusionMode.Zoom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevCameraOcclusionMode
    if not enumObj then
        return false
    end
    local item = enumObj.Invisicam
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevCameraOcclusionMode.Invisicam" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.UserChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerCameraMovementMode.UserChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerCameraMovementMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Follow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerCameraMovementMode.Follow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Orbital
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerCameraMovementMode.Orbital" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.CameraToggle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerCameraMovementMode.CameraToggle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.UserChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerMovementMode.UserChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.KeyboardMouse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerMovementMode.KeyboardMouse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.ClickToMove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerMovementMode.ClickToMove" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevComputerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Scriptable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevComputerMovementMode.Scriptable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.UserChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchCameraMovementMode.UserChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchCameraMovementMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Follow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchCameraMovementMode.Follow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Orbital
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchCameraMovementMode.Orbital" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.UserChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchMovementMode.UserChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchMovementMode.Thumbstick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.DPad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchMovementMode.DPad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbpad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchMovementMode.Thumbpad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.ClickToMove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchMovementMode.ClickToMove" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Scriptable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchMovementMode.Scriptable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DevTouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicThumbstick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DevTouchMovementMode.DynamicThumbstick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Internal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Internal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.HttpCache
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.HttpCache" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Instances
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Instances" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Signals
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Signals" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.LuaHeap
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.LuaHeap" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Script
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Script" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.PhysicsCollision
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.PhysicsCollision" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.BaseParts
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.BaseParts" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsSolidModels
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsSolidModels" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsMeshParts
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsMeshParts" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsParticles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsParticles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsParts
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsParts" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsSpatialHash
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsSpatialHash" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsTerrain
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsTerrain" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsTexture
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsTexture" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsTextureCharacter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsTextureCharacter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Sounds
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Sounds" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.StreamingSounds
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.StreamingSounds" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.TerrainVoxels
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.TerrainVoxels" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Gui
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Gui" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Animation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Animation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.Navigation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.Navigation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GeometryCSG
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GeometryCSG" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeveloperMemoryTag
    if not enumObj then
        return false
    end
    local item = enumObj.GraphicsSlimModels
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeveloperMemoryTag.GraphicsSlimModels" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceFeatureType
    if not enumObj then
        return false
    end
    local item = enumObj.DeviceCapture
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceFeatureType.DeviceCapture" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceFeatureType
    if not enumObj then
        return false
    end
    local item = enumObj.InExperienceFAE
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceFeatureType.InExperienceFAE" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceForm
    if not enumObj then
        return false
    end
    local item = enumObj.Console
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceForm.Console" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceForm
    if not enumObj then
        return false
    end
    local item = enumObj.Phone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceForm.Phone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceForm
    if not enumObj then
        return false
    end
    local item = enumObj.Tablet
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceForm.Tablet" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceForm
    if not enumObj then
        return false
    end
    local item = enumObj.Desktop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceForm.Desktop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceForm
    if not enumObj then
        return false
    end
    local item = enumObj.VR
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceForm.VR" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Low
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceLevel.Low" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceLevel.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceLevel
    if not enumObj then
        return false
    end
    local item = enumObj.High
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceLevel.High" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceSimulatorScalingMode
    if not enumObj then
        return false
    end
    local item = enumObj.ScaleToPhysicalSize
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.DeviceSimulatorScalingMode.ScaleToPhysicalSize"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceSimulatorScalingMode
    if not enumObj then
        return false
    end
    local item = enumObj.ActualResolution
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceSimulatorScalingMode.ActualResolution" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceSimulatorScalingMode
    if not enumObj then
        return false
    end
    local item = enumObj.FitToWindow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceSimulatorScalingMode.FitToWindow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceType.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.Desktop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceType.Desktop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.Tablet
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceType.Tablet" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.Phone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceType.Phone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.TV
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DeviceType.TV" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogBehaviorType
    if not enumObj then
        return false
    end
    local item = enumObj.SinglePlayer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogBehaviorType.SinglePlayer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogBehaviorType
    if not enumObj then
        return false
    end
    local item = enumObj.MultiplePlayers
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogBehaviorType.MultiplePlayers" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogPurpose
    if not enumObj then
        return false
    end
    local item = enumObj.Quest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogPurpose.Quest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogPurpose
    if not enumObj then
        return false
    end
    local item = enumObj.Help
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogPurpose.Help" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogPurpose
    if not enumObj then
        return false
    end
    local item = enumObj.Shop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogPurpose.Shop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogTone
    if not enumObj then
        return false
    end
    local item = enumObj.Neutral
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogTone.Neutral" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogTone
    if not enumObj then
        return false
    end
    local item = enumObj.Friendly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogTone.Friendly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DialogTone
    if not enumObj then
        return false
    end
    local item = enumObj.Enemy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DialogTone.Enemy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DigitsRigDescriptionSide
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DigitsRigDescriptionSide.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DigitsRigDescriptionSide
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DigitsRigDescriptionSide.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DigitsRigDescriptionSide
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DigitsRigDescriptionSide.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DiscountType
    if not enumObj then
        return false
    end
    local item = enumObj.Uncategorized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DiscountType.Uncategorized" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DisplayScalingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DisplayScalingMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DisplayScalingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DisplayScalingMode.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DisplayScalingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Responsive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DisplayScalingMode.Responsive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DisplaySize
    if not enumObj then
        return false
    end
    local item = enumObj.Small
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DisplaySize.Small" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DisplaySize
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DisplaySize.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DisplaySize
    if not enumObj then
        return false
    end
    local item = enumObj.Large
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DisplaySize.Large" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DomainType
    if not enumObj then
        return false
    end
    local item = enumObj.EXPERIENCE
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DomainType.EXPERIENCE" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DomainType
    if not enumObj then
        return false
    end
    local item = enumObj.OAUTH
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DomainType.OAUTH" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DominantAxis
    if not enumObj then
        return false
    end
    local item = enumObj.Width
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DominantAxis.Width" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DominantAxis
    if not enumObj then
        return false
    end
    local item = enumObj.Height
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DominantAxis.Height" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraftStatusCode
    if not enumObj then
        return false
    end
    local item = enumObj.OK
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraftStatusCode.OK" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraftStatusCode
    if not enumObj then
        return false
    end
    local item = enumObj.DraftOutdated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraftStatusCode.DraftOutdated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraftStatusCode
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptRemoved
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraftStatusCode.ScriptRemoved" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraftStatusCode
    if not enumObj then
        return false
    end
    local item = enumObj.DraftCommitted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraftStatusCode.DraftCommitted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.TranslateLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.TranslateLine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.TranslatePlane
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.TranslatePlane" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.TranslatePlaneOrLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.TranslatePlaneOrLine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.TranslateLineOrPlane
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.TranslateLineOrPlane" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.TranslateViewPlane
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.TranslateViewPlane" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RotateAxis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.RotateAxis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RotateTrackball
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.RotateTrackball" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Scriptable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.Scriptable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.BestForDevice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorDragStyle.BestForDevice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorPermissionPolicy
    if not enumObj then
        return false
    end
    local item = enumObj.Nobody
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorPermissionPolicy.Nobody" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorPermissionPolicy
    if not enumObj then
        return false
    end
    local item = enumObj.Everybody
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorPermissionPolicy.Everybody" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorPermissionPolicy
    if not enumObj then
        return false
    end
    local item = enumObj.Scriptable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorPermissionPolicy.Scriptable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorResponseStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Geometric
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorResponseStyle.Geometric" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorResponseStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Physical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorResponseStyle.Physical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DragDetectorResponseStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DragDetectorResponseStyle.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraggerCoordinateSpace
    if not enumObj then
        return false
    end
    local item = enumObj.Object
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraggerCoordinateSpace.Object" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraggerCoordinateSpace
    if not enumObj then
        return false
    end
    local item = enumObj.World
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraggerCoordinateSpace.World" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraggerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Geometric
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraggerMovementMode.Geometric" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraggerMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Physical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraggerMovementMode.Physical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraggingScrollBar
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraggingScrollBar.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraggingScrollBar
    if not enumObj then
        return false
    end
    local item = enumObj.Horizontal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraggingScrollBar.Horizontal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.DraggingScrollBar
    if not enumObj then
        return false
    end
    local item = enumObj.Vertical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.DraggingScrollBar.Vertical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.In
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingDirection.In" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Out
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingDirection.Out" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.InOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingDirection.InOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Linear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Linear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Sine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Sine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Back
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Back" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Quad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Quad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Quart
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Quart" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Quint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Quint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Bounce
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Bounce" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Elastic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Elastic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Exponential
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Exponential" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Circular
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Circular" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Cubic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EasingStyle.Cubic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EditableStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EditableStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EditableStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Allowed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EditableStatus.Allowed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EditableStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Disallowed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EditableStatus.Disallowed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ElasticBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.WhenScrollable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ElasticBehavior.WhenScrollable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ElasticBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ElasticBehavior.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ElasticBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Never
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ElasticBehavior.Never" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngagementLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngagementLevel.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngagementLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Inactive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngagementLevel.Inactive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngagementLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Low
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngagementLevel.Low" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngagementLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngagementLevel.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngagementLevel
    if not enumObj then
        return false
    end
    local item = enumObj.High
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngagementLevel.High" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngineFolder
    if not enumObj then
        return false
    end
    local item = enumObj.Screenshots
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngineFolder.Screenshots" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngineFolder
    if not enumObj then
        return false
    end
    local item = enumObj.Videos
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngineFolder.Videos" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EngineFolder
    if not enumObj then
        return false
    end
    local item = enumObj.Logs
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EngineFolder.Logs" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EnviromentalPhysicsThrottle
    if not enumObj then
        return false
    end
    local item = enumObj.DefaultAuto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EnviromentalPhysicsThrottle.DefaultAuto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EnviromentalPhysicsThrottle
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EnviromentalPhysicsThrottle.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EnviromentalPhysicsThrottle
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EnviromentalPhysicsThrottle.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EnviromentalPhysicsThrottle
    if not enumObj then
        return false
    end
    local item = enumObj.Skip2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EnviromentalPhysicsThrottle.Skip2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EnviromentalPhysicsThrottle
    if not enumObj then
        return false
    end
    local item = enumObj.Skip4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EnviromentalPhysicsThrottle.Skip4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EnviromentalPhysicsThrottle
    if not enumObj then
        return false
    end
    local item = enumObj.Skip8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EnviromentalPhysicsThrottle.Skip8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.EnviromentalPhysicsThrottle
    if not enumObj then
        return false
    end
    local item = enumObj.Skip16
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.EnviromentalPhysicsThrottle.Skip16" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceActivationStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.New
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceActivationStatus.New" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Active
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceActivationStatus.Active" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Lapsed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceActivationStatus.Lapsed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Reactivated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceActivationStatus.Reactivated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceAuthScope
    if not enumObj then
        return false
    end
    local item = enumObj.DefaultScope
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceAuthScope.DefaultScope" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceAuthScope
    if not enumObj then
        return false
    end
    local item = enumObj.CreatorAssetsCreate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceAuthScope.CreatorAssetsCreate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceEventStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Active
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceEventStatus.Active" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceEventStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Cancelled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceEventStatus.Cancelled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceEventStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Moderated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceEventStatus.Moderated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceEventStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unpublished
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceEventStatus.Unpublished" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceEventStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceEventStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateCaptureSelectionMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceStateCaptureSelectionMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateCaptureSelectionMode
    if not enumObj then
        return false
    end
    local item = enumObj.SafetyHighlightMode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ExperienceStateCaptureSelectionMode.SafetyHighlightMode"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingLoadMode
    if not enumObj then
        return false
    end
    local item = enumObj.NewReplay
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceStateRecordingLoadMode.NewReplay" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingLoadMode
    if not enumObj then
        return false
    end
    local item = enumObj.ContiguousSlice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ExperienceStateRecordingLoadMode.ContiguousSlice"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingLoadMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoncontiguousSlice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ExperienceStateRecordingLoadMode.NoncontiguousSlice"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingLoadSourceType
    if not enumObj then
        return false
    end
    local item = enumObj.S3Url
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ExperienceStateRecordingLoadSourceType.S3Url"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingLoadSourceType
    if not enumObj then
        return false
    end
    local item = enumObj.File
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExperienceStateRecordingLoadSourceType.File" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingPlaybackMode
    if not enumObj then
        return false
    end
    local item = enumObj.Undefined
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ExperienceStateRecordingPlaybackMode.Undefined"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingPlaybackMode
    if not enumObj then
        return false
    end
    local item = enumObj.Stopped
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ExperienceStateRecordingPlaybackMode.Stopped"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingPlaybackMode
    if not enumObj then
        return false
    end
    local item = enumObj.Playing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ExperienceStateRecordingPlaybackMode.Playing"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExperienceStateRecordingPlaybackMode
    if not enumObj then
        return false
    end
    local item = enumObj.Rewinding
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ExperienceStateRecordingPlaybackMode.Rewinding"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExplosionType
    if not enumObj then
        return false
    end
    local item = enumObj.NoCraters
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExplosionType.NoCraters" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExplosionType
    if not enumObj then
        return false
    end
    local item = enumObj.Craters
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExplosionType.Craters" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExternalEditorMode
    if not enumObj then
        return false
    end
    local item = enumObj.SystemDefault
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExternalEditorMode.SystemDefault" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ExternalEditorMode
    if not enumObj then
        return false
    end
    local item = enumObj.UserSelectedEditor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ExternalEditorMode.UserSelectedEditor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FACSDataLod
    if not enumObj then
        return false
    end
    local item = enumObj.LOD0
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FACSDataLod.LOD0" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FACSDataLod
    if not enumObj then
        return false
    end
    local item = enumObj.LOD1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FACSDataLod.LOD1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FACSDataLod
    if not enumObj then
        return false
    end
    local item = enumObj.LODCount
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FACSDataLod.LODCount" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAgeEstimationResultType
    if not enumObj then
        return false
    end
    local item = enumObj.Complete
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAgeEstimationResultType.Complete" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAgeEstimationResultType
    if not enumObj then
        return false
    end
    local item = enumObj.Cancel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAgeEstimationResultType.Cancel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAgeEstimationResultType
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAgeEstimationResultType.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAnimationStreamingState
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAnimationStreamingState.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAnimationStreamingState
    if not enumObj then
        return false
    end
    local item = enumObj.Audio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAnimationStreamingState.Audio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAnimationStreamingState
    if not enumObj then
        return false
    end
    local item = enumObj.Video
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAnimationStreamingState.Video" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAnimationStreamingState
    if not enumObj then
        return false
    end
    local item = enumObj.Place
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAnimationStreamingState.Place" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacialAnimationStreamingState
    if not enumObj then
        return false
    end
    local item = enumObj.Server
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacialAnimationStreamingState.Server" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.ChinRaiserUpperLip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.ChinRaiserUpperLip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.ChinRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.ChinRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.FlatPucker
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.FlatPucker" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.Funneler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.Funneler" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LowerLipSuck
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LowerLipSuck" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LipPresser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LipPresser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LipsTogether
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LipsTogether" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.MouthLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.MouthLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.MouthRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.MouthRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.Pucker
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.Pucker" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.UpperLipSuck
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.UpperLipSuck" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftCheekPuff
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftCheekPuff" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftDimpler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftDimpler" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLipCornerDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftLipCornerDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLowerLipDepressor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftLowerLipDepressor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLipCornerPuller
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftLipCornerPuller" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLipStretcher
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftLipStretcher" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftUpperLipRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftUpperLipRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightCheekPuff
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightCheekPuff" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightDimpler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightDimpler" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightLipCornerDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightLipCornerDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightLowerLipDepressor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightLowerLipDepressor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightLipCornerPuller
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightLipCornerPuller" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightLipStretcher
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightLipStretcher" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightUpperLipRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightUpperLipRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.JawDrop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.JawDrop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.JawLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.JawLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.JawRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.JawRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.Corrugator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.Corrugator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftBrowLowerer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftBrowLowerer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftOuterBrowRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftOuterBrowRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftNoseWrinkler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftNoseWrinkler" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftInnerBrowRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftInnerBrowRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightBrowLowerer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightBrowLowerer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightOuterBrowRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightOuterBrowRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightInnerBrowRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightInnerBrowRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightNoseWrinkler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightNoseWrinkler" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.EyesLookDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.EyesLookDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.EyesLookLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.EyesLookLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.EyesLookUp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.EyesLookUp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.EyesLookRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.EyesLookRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftCheekRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftCheekRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftEyeUpperLidRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftEyeUpperLidRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.LeftEyeClosed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.LeftEyeClosed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightCheekRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightCheekRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightEyeUpperLidRaiser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightEyeUpperLidRaiser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.RightEyeClosed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.RightEyeClosed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.TongueDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.TongueDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.TongueOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.TongueOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FacsActionUnit
    if not enumObj then
        return false
    end
    local item = enumObj.TongueUp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FacsActionUnit.TongueUp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FeatureRestrictionAbuseVector
    if not enumObj then
        return false
    end
    local item = enumObj.ExperienceChat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.FeatureRestrictionAbuseVector.ExperienceChat"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FeatureRestrictionAbuseVector
    if not enumObj then
        return false
    end
    local item = enumObj.Communication
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FeatureRestrictionAbuseVector.Communication" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FeedbackType
    if not enumObj then
        return false
    end
    local item = enumObj.Feedback
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FeedbackType.Feedback" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FeedbackType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerSupport
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FeedbackType.PlayerSupport" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FieldOfViewMode
    if not enumObj then
        return false
    end
    local item = enumObj.Vertical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FieldOfViewMode.Vertical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FieldOfViewMode
    if not enumObj then
        return false
    end
    local item = enumObj.Diagonal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FieldOfViewMode.Diagonal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FieldOfViewMode
    if not enumObj then
        return false
    end
    local item = enumObj.MaxAxis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FieldOfViewMode.MaxAxis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FillDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Horizontal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FillDirection.Horizontal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FillDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Vertical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FillDirection.Vertical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.BackslashNotEscapingAnything
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.FilterErrorType.BackslashNotEscapingAnything"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.BadBespokeFilter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.BadBespokeFilter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.BadName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.BadName" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.IncompleteOr
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.IncompleteOr" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.IncompleteParenthesis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.IncompleteParenthesis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidDoubleStar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.InvalidDoubleStar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidTilde
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.InvalidTilde" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.PropertyBadOperator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.PropertyBadOperator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.PropertyDoesNotExist
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.PropertyDoesNotExist" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.PropertyInvalidField
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.PropertyInvalidField" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.PropertyInvalidValue
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.PropertyInvalidValue" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.PropertyUnsupportedFields
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.PropertyUnsupportedFields" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.PropertyUnsupportedProperty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.PropertyUnsupportedProperty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.UnexpectedNameIndex
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.UnexpectedNameIndex" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.UnexpectedToken
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.UnexpectedToken" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.UnfinishedBinaryOperator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.UnfinishedBinaryOperator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.UnfinishedQuote
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.UnfinishedQuote" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.UnknownBespokeFilter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.UnknownBespokeFilter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterErrorType
    if not enumObj then
        return false
    end
    local item = enumObj.WildcardInProperty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterErrorType.WildcardInProperty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterResult
    if not enumObj then
        return false
    end
    local item = enumObj.Accepted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterResult.Accepted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterResult
    if not enumObj then
        return false
    end
    local item = enumObj.Rejected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterResult.Rejected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Exclude
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterType.Exclude" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Include
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FilterType.Include" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FinishRecordingOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Cancel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FinishRecordingOperation.Cancel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FinishRecordingOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Commit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FinishRecordingOperation.Commit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FinishRecordingOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Append
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FinishRecordingOperation.Append" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FluidFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FluidFidelity.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FluidFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.UseCollisionGeometry
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FluidFidelity.UseCollisionGeometry" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FluidFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.UsePreciseGeometry
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FluidFidelity.UsePreciseGeometry" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FluidForces
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FluidForces.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FluidForces
    if not enumObj then
        return false
    end
    local item = enumObj.Experimental
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FluidForces.Experimental" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Arial
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Arial" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.ArialBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.ArialBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.SourceSans
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.SourceSans" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.SourceSansBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.SourceSansBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.SourceSansLight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.SourceSansLight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.SourceSansItalic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.SourceSansItalic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Bodoni
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Bodoni" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Garamond
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Garamond" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Cartoon
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Cartoon" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Code
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Code" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Highway
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Highway" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.SciFi
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.SciFi" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Arcade
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Arcade" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Fantasy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Fantasy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Antique
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Antique" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.SourceSansSemibold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.SourceSansSemibold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Gotham
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Gotham" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.GothamMedium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.GothamMedium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.GothamBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.GothamBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.GothamBlack
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.GothamBlack" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.AmaticSC
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.AmaticSC" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Bangers
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Bangers" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Creepster
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Creepster" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.DenkOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.DenkOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Fondamento
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Fondamento" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.FredokaOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.FredokaOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.GrenzeGotisch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.GrenzeGotisch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.IndieFlower
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.IndieFlower" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.JosefinSans
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.JosefinSans" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Jura
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Jura" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Kalam
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Kalam" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.LuckiestGuy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.LuckiestGuy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Merriweather
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Merriweather" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Michroma
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Michroma" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Nunito
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Nunito" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Oswald
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Oswald" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.PatrickHand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.PatrickHand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.PermanentMarker
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.PermanentMarker" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Roboto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Roboto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.RobotoCondensed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.RobotoCondensed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.RobotoMono
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.RobotoMono" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Sarpanch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Sarpanch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.SpecialElite
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.SpecialElite" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.TitilliumWeb
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.TitilliumWeb" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Ubuntu
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Ubuntu" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.BuilderSans
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.BuilderSans" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.BuilderSansMedium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.BuilderSansMedium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.BuilderSansBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.BuilderSansBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.BuilderSansExtraBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.BuilderSansExtraBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Arimo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Arimo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.ArimoBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.ArimoBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Font
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Font.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size9
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size9" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size10
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size10" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size11
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size11" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size12
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size12" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size14
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size14" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size18
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size18" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size24
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size24" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size36
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size36" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size48
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size48" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size28
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size28" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size32
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size32" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size42
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size42" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size60
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size60" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size96
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontSize.Size96" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Normal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontStyle.Normal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Italic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontStyle.Italic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.Thin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.Thin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.ExtraLight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.ExtraLight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.Light
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.Light" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.Regular
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.Regular" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.SemiBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.SemiBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.Bold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.Bold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.ExtraBold
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.ExtraBold" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FontWeight
    if not enumObj then
        return false
    end
    local item = enumObj.Heavy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FontWeight.Heavy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ForceLimitMode
    if not enumObj then
        return false
    end
    local item = enumObj.Magnitude
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ForceLimitMode.Magnitude" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ForceLimitMode
    if not enumObj then
        return false
    end
    local item = enumObj.PerAxis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ForceLimitMode.PerAxis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FormFactor
    if not enumObj then
        return false
    end
    local item = enumObj.Symmetric
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FormFactor.Symmetric" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FormFactor
    if not enumObj then
        return false
    end
    local item = enumObj.Brick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FormFactor.Brick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FormFactor
    if not enumObj then
        return false
    end
    local item = enumObj.Plate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FormFactor.Plate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FormFactor
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FormFactor.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FrameStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FrameStyle.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FrameStyle
    if not enumObj then
        return false
    end
    local item = enumObj.ChatBlue
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FrameStyle.ChatBlue" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FrameStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxSquare
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FrameStyle.RobloxSquare" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FrameStyle
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxRound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FrameStyle.RobloxRound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FrameStyle
    if not enumObj then
        return false
    end
    local item = enumObj.ChatGreen
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FrameStyle.ChatGreen" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FrameStyle
    if not enumObj then
        return false
    end
    local item = enumObj.ChatRed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FrameStyle.ChatRed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FrameStyle
    if not enumObj then
        return false
    end
    local item = enumObj.DropShadow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FrameStyle.DropShadow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FramerateManagerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FramerateManagerMode.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FramerateManagerMode
    if not enumObj then
        return false
    end
    local item = enumObj.On
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FramerateManagerMode.On" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FramerateManagerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Off
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FramerateManagerMode.Off" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendRequestEvent
    if not enumObj then
        return false
    end
    local item = enumObj.Issue
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendRequestEvent.Issue" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendRequestEvent
    if not enumObj then
        return false
    end
    local item = enumObj.Revoke
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendRequestEvent.Revoke" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendRequestEvent
    if not enumObj then
        return false
    end
    local item = enumObj.Accept
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendRequestEvent.Accept" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendRequestEvent
    if not enumObj then
        return false
    end
    local item = enumObj.Deny
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendRequestEvent.Deny" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotFriend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendStatus.NotFriend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Friend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendStatus.Friend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FriendRequestSent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendStatus.FriendRequestSent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FriendStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FriendRequestReceived
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FriendStatus.FriendRequestReceived" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FunctionalTestResult
    if not enumObj then
        return false
    end
    local item = enumObj.Passed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FunctionalTestResult.Passed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FunctionalTestResult
    if not enumObj then
        return false
    end
    local item = enumObj.Warning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FunctionalTestResult.Warning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.FunctionalTestResult
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.FunctionalTestResult.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GameAvatarType
    if not enumObj then
        return false
    end
    local item = enumObj.R6
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GameAvatarType.R6" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GameAvatarType
    if not enumObj then
        return false
    end
    local item = enumObj.R15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GameAvatarType.R15" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GameAvatarType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerChoice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GameAvatarType.PlayerChoice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GamepadType
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GamepadType.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GamepadType
    if not enumObj then
        return false
    end
    local item = enumObj.PS4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GamepadType.PS4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GamepadType
    if not enumObj then
        return false
    end
    local item = enumObj.PS5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GamepadType.PS5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GamepadType
    if not enumObj then
        return false
    end
    local item = enumObj.XboxOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GamepadType.XboxOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearGenreSetting
    if not enumObj then
        return false
    end
    local item = enumObj.AllGenres
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearGenreSetting.AllGenres" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearGenreSetting
    if not enumObj then
        return false
    end
    local item = enumObj.MatchingGenreOnly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearGenreSetting.MatchingGenreOnly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.MeleeWeapons
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.MeleeWeapons" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.RangedWeapons
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.RangedWeapons" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.Explosives
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.Explosives" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.PowerUps
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.PowerUps" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.NavigationEnhancers
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.NavigationEnhancers" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.MusicalInstruments
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.MusicalInstruments" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.SocialItems
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.SocialItems" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.BuildingTools
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.BuildingTools" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GearType
    if not enumObj then
        return false
    end
    local item = enumObj.Transport
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GearType.Transport" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.TownAndCity
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.TownAndCity" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Fantasy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Fantasy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.SciFi
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.SciFi" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Ninja
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Ninja" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Scary
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Scary" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Pirate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Pirate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Adventure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Adventure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Sports
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Sports" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Funny
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Funny" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.WildWest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.WildWest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.War
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.War" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.SkatePark
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.SkatePark" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Genre
    if not enumObj then
        return false
    end
    local item = enumObj.Tutorial
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Genre.Tutorial" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GradientTileMode
    if not enumObj then
        return false
    end
    local item = enumObj.Clamp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GradientTileMode.Clamp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GradientTileMode
    if not enumObj then
        return false
    end
    local item = enumObj.Repeat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GradientTileMode.Repeat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GradientTileMode
    if not enumObj then
        return false
    end
    local item = enumObj.Mirror
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GradientTileMode.Mirror" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GradientType
    if not enumObj then
        return false
    end
    local item = enumObj.Linear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GradientType.Linear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GradientType
    if not enumObj then
        return false
    end
    local item = enumObj.Radial
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GradientType.Radial" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GradientType
    if not enumObj then
        return false
    end
    local item = enumObj.Conical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GradientType.Conical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsMode
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsMode.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsMode
    if not enumObj then
        return false
    end
    local item = enumObj.Direct3D11
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsMode.Direct3D11" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsMode
    if not enumObj then
        return false
    end
    local item = enumObj.OpenGL
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsMode.OpenGL" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsMode
    if not enumObj then
        return false
    end
    local item = enumObj.Metal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsMode.Metal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsMode
    if not enumObj then
        return false
    end
    local item = enumObj.Vulkan
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsMode.Vulkan" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoGraphics
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsMode.NoGraphics" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsOptimizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Performance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsOptimizationMode.Performance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsOptimizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Balanced
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsOptimizationMode.Balanced" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GraphicsOptimizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Quality
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GraphicsOptimizationMode.Quality" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GroupMembershipStatus
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GroupMembershipStatus.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GroupMembershipStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Joined
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GroupMembershipStatus.Joined" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GroupMembershipStatus
    if not enumObj then
        return false
    end
    local item = enumObj.JoinRequestPending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GroupMembershipStatus.JoinRequestPending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GroupMembershipStatus
    if not enumObj then
        return false
    end
    local item = enumObj.AlreadyMember
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GroupMembershipStatus.AlreadyMember" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiState
    if not enumObj then
        return false
    end
    local item = enumObj.Idle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiState.Idle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiState
    if not enumObj then
        return false
    end
    local item = enumObj.Hover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiState.Hover" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiState
    if not enumObj then
        return false
    end
    local item = enumObj.Press
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiState.Press" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiState
    if not enumObj then
        return false
    end
    local item = enumObj.NonInteractable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiState.NonInteractable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiType
    if not enumObj then
        return false
    end
    local item = enumObj.Core
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiType.Core" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiType
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiType.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerNameplates
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiType.PlayerNameplates" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiType
    if not enumObj then
        return false
    end
    local item = enumObj.CustomBillboards
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiType.CustomBillboards" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.GuiType
    if not enumObj then
        return false
    end
    local item = enumObj.CoreBillboards
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.GuiType.CoreBillboards" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HandlesStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Resize
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HandlesStyle.Resize" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HandlesStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Movement
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HandlesStyle.Movement" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HapticEffectType
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HapticEffectType.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HapticEffectType
    if not enumObj then
        return false
    end
    local item = enumObj.UIHover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HapticEffectType.UIHover" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HapticEffectType
    if not enumObj then
        return false
    end
    local item = enumObj.UIClick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HapticEffectType.UIClick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HapticEffectType
    if not enumObj then
        return false
    end
    local item = enumObj.UINotification
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HapticEffectType.UINotification" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HapticEffectType
    if not enumObj then
        return false
    end
    local item = enumObj.GameplayExplosion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HapticEffectType.GameplayExplosion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HapticEffectType
    if not enumObj then
        return false
    end
    local item = enumObj.GameplayCollision
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HapticEffectType.GameplayCollision" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HashAlgorithm
    if not enumObj then
        return false
    end
    local item = enumObj.Blake2b
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HashAlgorithm.Blake2b" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HashAlgorithm
    if not enumObj then
        return false
    end
    local item = enumObj.Blake3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HashAlgorithm.Blake3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HashAlgorithm
    if not enumObj then
        return false
    end
    local item = enumObj.Md5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HashAlgorithm.Md5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HashAlgorithm
    if not enumObj then
        return false
    end
    local item = enumObj.Sha1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HashAlgorithm.Sha1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HashAlgorithm
    if not enumObj then
        return false
    end
    local item = enumObj.Sha256
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HashAlgorithm.Sha256" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HighlightDepthMode
    if not enumObj then
        return false
    end
    local item = enumObj.AlwaysOnTop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HighlightDepthMode.AlwaysOnTop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HighlightDepthMode
    if not enumObj then
        return false
    end
    local item = enumObj.Occluded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HighlightDepthMode.Occluded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HorizontalAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HorizontalAlignment.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HorizontalAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HorizontalAlignment.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HorizontalAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HorizontalAlignment.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HoverAnimateSpeed
    if not enumObj then
        return false
    end
    local item = enumObj.VerySlow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HoverAnimateSpeed.VerySlow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HoverAnimateSpeed
    if not enumObj then
        return false
    end
    local item = enumObj.Slow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HoverAnimateSpeed.Slow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HoverAnimateSpeed
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HoverAnimateSpeed.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HoverAnimateSpeed
    if not enumObj then
        return false
    end
    local item = enumObj.Fast
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HoverAnimateSpeed.Fast" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HoverAnimateSpeed
    if not enumObj then
        return false
    end
    local item = enumObj.VeryFast
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HoverAnimateSpeed.VeryFast" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpCachePolicy
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpCachePolicy.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpCachePolicy
    if not enumObj then
        return false
    end
    local item = enumObj.Full
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpCachePolicy.Full" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpCachePolicy
    if not enumObj then
        return false
    end
    local item = enumObj.DataOnly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpCachePolicy.DataOnly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpCachePolicy
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpCachePolicy.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpCachePolicy
    if not enumObj then
        return false
    end
    local item = enumObj.InternalRedirectRefresh
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpCachePolicy.InternalRedirectRefresh" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpCompression
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpCompression.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpCompression
    if not enumObj then
        return false
    end
    local item = enumObj.Gzip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpCompression.Gzip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpContentType
    if not enumObj then
        return false
    end
    local item = enumObj.ApplicationJson
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpContentType.ApplicationJson" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpContentType
    if not enumObj then
        return false
    end
    local item = enumObj.ApplicationXml
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpContentType.ApplicationXml" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpContentType
    if not enumObj then
        return false
    end
    local item = enumObj.ApplicationUrlEncoded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpContentType.ApplicationUrlEncoded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpContentType
    if not enumObj then
        return false
    end
    local item = enumObj.TextPlain
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpContentType.TextPlain" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpContentType
    if not enumObj then
        return false
    end
    local item = enumObj.TextXml
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpContentType.TextXml" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.OK
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.OK" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidUrl
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.InvalidUrl" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.DnsResolve
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.DnsResolve" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.ConnectFail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.ConnectFail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.OutOfMemory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.OutOfMemory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.TimedOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.TimedOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.TooManyRedirects
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.TooManyRedirects" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidRedirect
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.InvalidRedirect" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.NetFail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.NetFail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.Aborted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.Aborted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.SslConnectFail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.SslConnectFail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.SslVerificationFail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.SslVerificationFail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.ConnectionClosed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.ConnectionClosed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.ServerProtocolError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.ServerProtocolError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.CreatorEnvironmentsNotSupportedByService
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.HttpError.CreatorEnvironmentsNotSupportedByService"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.InactivityTimeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.InactivityTimeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.TooManyOutstandingRequests
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.TooManyOutstandingRequests" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpError
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidRangeResponse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpError.InvalidRangeResponse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpRequestType.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.MarketplaceService
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpRequestType.MarketplaceService" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Players
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpRequestType.Players" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Chat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpRequestType.Chat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Avatar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpRequestType.Avatar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Analytics
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpRequestType.Analytics" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HttpRequestType
    if not enumObj then
        return false
    end
    local item = enumObj.Localization
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HttpRequestType.Localization" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidCollisionType
    if not enumObj then
        return false
    end
    local item = enumObj.OuterBox
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidCollisionType.OuterBox" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidCollisionType
    if not enumObj then
        return false
    end
    local item = enumObj.InnerBox
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidCollisionType.InnerBox" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidDisplayDistanceType
    if not enumObj then
        return false
    end
    local item = enumObj.Viewer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidDisplayDistanceType.Viewer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidDisplayDistanceType
    if not enumObj then
        return false
    end
    local item = enumObj.Subject
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidDisplayDistanceType.Subject" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidDisplayDistanceType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidDisplayDistanceType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidHealthDisplayType
    if not enumObj then
        return false
    end
    local item = enumObj.DisplayWhenDamaged
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.HumanoidHealthDisplayType.DisplayWhenDamaged"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidHealthDisplayType
    if not enumObj then
        return false
    end
    local item = enumObj.AlwaysOn
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidHealthDisplayType.AlwaysOn" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidHealthDisplayType
    if not enumObj then
        return false
    end
    local item = enumObj.AlwaysOff
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidHealthDisplayType.AlwaysOff" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidRigType
    if not enumObj then
        return false
    end
    local item = enumObj.R6
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidRigType.R6" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidRigType
    if not enumObj then
        return false
    end
    local item = enumObj.R15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidRigType.R15" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.FallingDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.FallingDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Ragdoll
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Ragdoll" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.GettingUp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.GettingUp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Jumping
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Jumping" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Swimming
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Swimming" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Freefall
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Freefall" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Flying
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Flying" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Landed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Landed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Running
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Running" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.RunningNoPhysics
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.RunningNoPhysics" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.StrafingNoPhysics
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.StrafingNoPhysics" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Climbing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Climbing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Seated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Seated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.PlatformStanding
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.PlatformStanding" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Dead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Dead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.Physics
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.Physics" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.HumanoidStateType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.HumanoidStateType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKCollisionsMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoCollisions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKCollisionsMode.NoCollisions" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKCollisionsMode
    if not enumObj then
        return false
    end
    local item = enumObj.OtherMechanismsAnchored
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKCollisionsMode.OtherMechanismsAnchored" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKCollisionsMode
    if not enumObj then
        return false
    end
    local item = enumObj.IncludeContactedMechanisms
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKCollisionsMode.IncludeContactedMechanisms" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKControlConstraintSupport
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKControlConstraintSupport.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKControlConstraintSupport
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKControlConstraintSupport.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKControlConstraintSupport
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKControlConstraintSupport.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKControlType
    if not enumObj then
        return false
    end
    local item = enumObj.Transform
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKControlType.Transform" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKControlType
    if not enumObj then
        return false
    end
    local item = enumObj.Position
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKControlType.Position" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKControlType
    if not enumObj then
        return false
    end
    local item = enumObj.Rotation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKControlType.Rotation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IKControlType
    if not enumObj then
        return false
    end
    local item = enumObj.LookAt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IKControlType.LookAt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IXPLoadingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IXPLoadingStatus.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IXPLoadingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Pending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IXPLoadingStatus.Pending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IXPLoadingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Initialized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IXPLoadingStatus.Initialized" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IXPLoadingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorInvalidUser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IXPLoadingStatus.ErrorInvalidUser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IXPLoadingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorConnection
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IXPLoadingStatus.ErrorConnection" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IXPLoadingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorJsonParse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IXPLoadingStatus.ErrorJsonParse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IXPLoadingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorTimedOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.IXPLoadingStatus.ErrorTimedOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageAlphaType
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageAlphaType.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageAlphaType
    if not enumObj then
        return false
    end
    local item = enumObj.LockCanvasAlpha
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageAlphaType.LockCanvasAlpha" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageAlphaType
    if not enumObj then
        return false
    end
    local item = enumObj.LockCanvasColor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageAlphaType.LockCanvasColor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageCombineType
    if not enumObj then
        return false
    end
    local item = enumObj.BlendSourceOver
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageCombineType.BlendSourceOver" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageCombineType
    if not enumObj then
        return false
    end
    local item = enumObj.Overwrite
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageCombineType.Overwrite" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageCombineType
    if not enumObj then
        return false
    end
    local item = enumObj.Add
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageCombineType.Add" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageCombineType
    if not enumObj then
        return false
    end
    local item = enumObj.Multiply
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageCombineType.Multiply" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageCombineType
    if not enumObj then
        return false
    end
    local item = enumObj.AlphaBlend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageCombineType.AlphaBlend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ImageCombineType
    if not enumObj then
        return false
    end
    local item = enumObj.NormalMapBlend
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ImageCombineType.NormalMapBlend" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InOut
    if not enumObj then
        return false
    end
    local item = enumObj.Edge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InOut.Edge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InOut
    if not enumObj then
        return false
    end
    local item = enumObj.Inset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InOut.Inset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InOut
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InOut.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InfoType
    if not enumObj then
        return false
    end
    local item = enumObj.Asset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InfoType.Asset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InfoType
    if not enumObj then
        return false
    end
    local item = enumObj.Product
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InfoType.Product" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InfoType
    if not enumObj then
        return false
    end
    local item = enumObj.GamePass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InfoType.GamePass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InfoType
    if not enumObj then
        return false
    end
    local item = enumObj.Subscription
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InfoType.Subscription" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InfoType
    if not enumObj then
        return false
    end
    local item = enumObj.Bundle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InfoType.Bundle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InitialDockState
    if not enumObj then
        return false
    end
    local item = enumObj.Top
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InitialDockState.Top" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InitialDockState
    if not enumObj then
        return false
    end
    local item = enumObj.Bottom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InitialDockState.Bottom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InitialDockState
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InitialDockState.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InitialDockState
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InitialDockState.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InitialDockState
    if not enumObj then
        return false
    end
    local item = enumObj.Float
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InitialDockState.Float" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Bool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputActionType.Bool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Direction1D
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputActionType.Direction1D" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Direction2D
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputActionType.Direction2D" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Direction3D
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputActionType.Direction3D" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputActionType
    if not enumObj then
        return false
    end
    local item = enumObj.ViewportPosition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputActionType.ViewportPosition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputSink
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputSink.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputSink
    if not enumObj then
        return false
    end
    local item = enumObj.Activate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputSink.Activate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputSink
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputSink.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputType
    if not enumObj then
        return false
    end
    local item = enumObj.NoInput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputType.NoInput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputType
    if not enumObj then
        return false
    end
    local item = enumObj.Constant
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputType.Constant" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InputType
    if not enumObj then
        return false
    end
    local item = enumObj.Sin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InputType.Sin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InstanceFileSyncStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotSynced
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InstanceFileSyncStatus.NotSynced" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InstanceFileSyncStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Errored
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InstanceFileSyncStatus.Errored" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InstanceFileSyncStatus
    if not enumObj then
        return false
    end
    local item = enumObj.SyncedAsRoot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InstanceFileSyncStatus.SyncedAsRoot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InstanceFileSyncStatus
    if not enumObj then
        return false
    end
    local item = enumObj.SyncedAsDescendant
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InstanceFileSyncStatus.SyncedAsDescendant" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InstanceFileSyncStatus
    if not enumObj then
        return false
    end
    local item = enumObj.AncestorErrored
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InstanceFileSyncStatus.AncestorErrored" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.IntermediateMeshGenerationResult
    if not enumObj then
        return false
    end
    local item = enumObj.HighQualityMesh
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.IntermediateMeshGenerationResult.HighQualityMesh"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InternalVideoUsage
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InternalVideoUsage.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InternalVideoUsage
    if not enumObj then
        return false
    end
    local item = enumObj.WatchPage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InternalVideoUsage.WatchPage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InternalVideoUsage
    if not enumObj then
        return false
    end
    local item = enumObj.HomeCarousel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InternalVideoUsage.HomeCarousel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InterpolationThrottlingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InterpolationThrottlingMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InterpolationThrottlingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InterpolationThrottlingMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InterpolationThrottlingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InterpolationThrottlingMode.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InviteState
    if not enumObj then
        return false
    end
    local item = enumObj.Placed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InviteState.Placed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InviteState
    if not enumObj then
        return false
    end
    local item = enumObj.Accepted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InviteState.Accepted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InviteState
    if not enumObj then
        return false
    end
    local item = enumObj.Declined
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InviteState.Declined" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.InviteState
    if not enumObj then
        return false
    end
    local item = enumObj.Missed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.InviteState.Missed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ItemLineAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ItemLineAlignment.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ItemLineAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Start
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ItemLineAlignment.Start" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ItemLineAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ItemLineAlignment.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ItemLineAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.End
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ItemLineAlignment.End" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ItemLineAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Stretch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ItemLineAlignment.Stretch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.JoinSource
    if not enumObj then
        return false
    end
    local item = enumObj.CreatedItemAttribution
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.JoinSource.CreatedItemAttribution" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.JointCreationMode
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.JointCreationMode.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.JointCreationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Surface
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.JointCreationMode.Surface" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.JointCreationMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.JointCreationMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Backspace
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Backspace" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Tab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Tab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Clear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Clear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Return
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Return" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Pause
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Pause" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Escape
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Escape" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Space
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Space" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.QuotedDouble
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.QuotedDouble" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Hash
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Hash" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Dollar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Dollar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Percent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Percent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Ampersand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Ampersand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Quote
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Quote" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftParenthesis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftParenthesis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightParenthesis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightParenthesis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Asterisk
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Asterisk" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Plus
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Plus" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Comma
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Comma" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Minus
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Minus" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Period
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Period" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Slash
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Slash" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Zero
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Zero" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.One
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.One" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Two
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Two" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Three
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Three" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Four
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Four" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Five
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Five" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Six
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Six" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Seven
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Seven" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Eight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Eight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Nine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Nine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Colon
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Colon" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Semicolon
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Semicolon" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LessThan
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LessThan" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Equals
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Equals" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.GreaterThan
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.GreaterThan" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Question
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Question" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.At
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.At" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftBracket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftBracket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.BackSlash
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.BackSlash" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightBracket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightBracket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Caret
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Caret" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Underscore
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Underscore" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Backquote
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Backquote" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.A
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.A" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.B
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.B" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.C
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.C" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.D
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.D" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.E
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.E" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.G
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.G" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.H
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.H" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.I
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.I" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.J
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.J" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.K
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.K" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.L
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.L" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.M
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.M" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.N
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.N" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.O
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.O" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.P
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.P" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Q
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Q" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.R
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.R" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.S
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.S" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.T
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.T" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.U
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.U" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.V
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.V" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.W
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.W" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.X
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.X" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Y
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Y" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Z
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Z" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftCurly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftCurly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Pipe
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Pipe" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightCurly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightCurly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Tilde
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Tilde" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Delete
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Delete" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World0
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World0" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World6
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World6" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World7
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World7" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World9
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World9" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World10
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World10" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World11
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World11" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World12
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World12" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World13
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World13" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World14
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World14" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World15" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World16
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World16" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World17
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World17" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World18
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World18" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World19
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World19" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World20
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World20" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World21
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World21" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World22
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World22" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World23
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World23" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World24
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World24" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World25
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World25" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World26
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World26" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World27
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World27" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World28
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World28" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World29
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World29" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World30
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World30" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World31
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World31" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World32
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World32" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World33
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World33" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World34
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World34" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World35
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World35" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World36
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World36" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World37
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World37" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World38
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World38" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World39
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World39" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World40
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World40" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World41
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World41" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World42
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World42" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World43
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World43" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World44
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World44" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World45
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World45" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World46
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World46" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World47
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World47" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World48
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World48" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World49
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World49" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World50
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World50" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World51
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World51" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World52
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World52" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World53
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World53" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World54
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World54" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World55
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World55" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World56
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World56" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World57
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World57" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World58
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World58" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World59
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World59" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World60
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World60" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World61
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World61" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World62
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World62" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World63
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World63" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World64
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World64" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World65
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World65" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World66
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World66" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World67
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World67" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World68
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World68" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World69
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World69" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World70
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World70" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World71
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World71" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World72
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World72" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World73
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World73" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World74
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World74" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World75
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World75" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World76
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World76" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World77
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World77" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World78
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World78" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World79
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World79" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World80
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World80" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World81
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World81" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World82
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World82" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World83
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World83" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World84
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World84" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World85
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World85" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World86
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World86" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World87
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World87" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World88
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World88" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World89
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World89" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World90
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World90" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World91
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World91" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World92
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World92" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World93
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World93" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World94
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World94" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.World95
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.World95" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadZero
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadZero" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadTwo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadTwo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadThree
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadThree" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadFour
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadFour" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadFive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadFive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadSix
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadSix" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadSeven
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadSeven" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadEight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadEight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadNine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadNine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadPeriod
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadPeriod" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadDivide
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadDivide" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadMultiply
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadMultiply" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadMinus
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadMinus" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadPlus
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadPlus" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadEnter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadEnter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.KeypadEquals
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.KeypadEquals" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Up
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Up" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Down
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Down" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Insert
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Insert" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Home
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Home" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.End
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.End" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.PageUp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.PageUp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.PageDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.PageDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F6
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F6" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F7
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F7" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F9
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F9" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F10
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F10" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F11
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F11" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F12
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F12" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F13
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F13" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F14
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F14" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.F15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.F15" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.NumLock
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.NumLock" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.CapsLock
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.CapsLock" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ScrollLock
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ScrollLock" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightShift
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightShift" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftShift
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftShift" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightControl
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightControl" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftControl
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftControl" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightAlt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightAlt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftAlt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftAlt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightMeta
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightMeta" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftMeta
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftMeta" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.LeftSuper
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.LeftSuper" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.RightSuper
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.RightSuper" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Mode
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Mode" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Compose
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Compose" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Help
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Help" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Print
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Print" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.SysReq
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.SysReq" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Break
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Break" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Menu
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Menu" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Power
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Power" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Euro
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Euro" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Undo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Undo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonA
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonA" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonB
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonB" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonR1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonR1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonL1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonL1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonR2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonR2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonL2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonL2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonR3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonR3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonL3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonL3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonStart
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonStart" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonSelect
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.ButtonSelect" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.DPadLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.DPadLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.DPadRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.DPadRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.DPadUp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.DPadUp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.DPadDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.DPadDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick1Up
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick1Up" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick1Down
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick1Down" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick1Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick1Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick1Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick1Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick2Up
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick2Up" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick2Down
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick2Down" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick2Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick2Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick2Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.Thumbstick2Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseLeftButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseLeftButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseRightButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseRightButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseMiddleButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseMiddleButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseBackButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseBackButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseNoButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseNoButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MousePosition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MousePosition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.TouchPosition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.TouchPosition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseWheel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseWheel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.TrackpadPan
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.TrackpadPan" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.TrackpadPinch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.TrackpadPinch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.MouseDelta
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.MouseDelta" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.TouchDelta
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.TouchDelta" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyCode
    if not enumObj then
        return false
    end
    local item = enumObj.TouchPinch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyCode.TouchPinch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyInterpolationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Constant
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyInterpolationMode.Constant" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyInterpolationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Linear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyInterpolationMode.Linear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeyInterpolationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Cubic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeyInterpolationMode.Cubic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeywordFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Include
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeywordFilterType.Include" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.KeywordFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Exclude
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.KeywordFilterType.Exclude" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Language
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Language.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LeftRight
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LeftRight.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LeftRight
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LeftRight.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LeftRight
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LeftRight.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Eof
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Eof" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Name
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Name" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.QuotedString
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.QuotedString" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Number
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Number" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.And
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.And" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Or
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Or" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Equal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Equal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.TildeEqual
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.TildeEqual" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.GreaterThan
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.GreaterThan" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.GreaterThanEqual
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.GreaterThanEqual" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.LessThan
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.LessThan" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.LessThanEqual
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.LessThanEqual" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Colon
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Colon" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Dot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Dot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.LeftParenthesis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.LeftParenthesis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.RightParenthesis
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.RightParenthesis" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.Star
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.Star" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.DoubleStar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.DoubleStar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LexemeType
    if not enumObj then
        return false
    end
    local item = enumObj.ReservedSpecial
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LexemeType.ReservedSpecial" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LightingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Realistic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LightingStyle.Realistic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LightingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Soft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LightingStyle.Soft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Limb
    if not enumObj then
        return false
    end
    local item = enumObj.Head
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Limb.Head" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Limb
    if not enumObj then
        return false
    end
    local item = enumObj.Torso
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Limb.Torso" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Limb
    if not enumObj then
        return false
    end
    local item = enumObj.LeftArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Limb.LeftArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Limb
    if not enumObj then
        return false
    end
    local item = enumObj.RightArm
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Limb.RightArm" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Limb
    if not enumObj then
        return false
    end
    local item = enumObj.LeftLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Limb.LeftLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Limb
    if not enumObj then
        return false
    end
    local item = enumObj.RightLeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Limb.RightLeg" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Limb
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Limb.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LineJoinMode
    if not enumObj then
        return false
    end
    local item = enumObj.Round
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LineJoinMode.Round" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LineJoinMode
    if not enumObj then
        return false
    end
    local item = enumObj.Bevel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LineJoinMode.Bevel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LineJoinMode
    if not enumObj then
        return false
    end
    local item = enumObj.Miter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LineJoinMode.Miter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListDisplayMode
    if not enumObj then
        return false
    end
    local item = enumObj.Horizontal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListDisplayMode.Horizontal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListDisplayMode
    if not enumObj then
        return false
    end
    local item = enumObj.Vertical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListDisplayMode.Vertical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerLocation
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerLocation.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerLocation
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerLocation.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerLocation
    if not enumObj then
        return false
    end
    local item = enumObj.Character
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerLocation.Character" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerLocation
    if not enumObj then
        return false
    end
    local item = enumObj.Camera
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerLocation.Camera" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerType
    if not enumObj then
        return false
    end
    local item = enumObj.Camera
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerType.Camera" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerType
    if not enumObj then
        return false
    end
    local item = enumObj.CFrame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerType.CFrame" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerType
    if not enumObj then
        return false
    end
    local item = enumObj.ObjectPosition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerType.ObjectPosition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ListenerType
    if not enumObj then
        return false
    end
    local item = enumObj.ObjectCFrame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ListenerType.ObjectCFrame" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LiveEditingAtomicUpdateResponse
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LiveEditingAtomicUpdateResponse.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LiveEditingAtomicUpdateResponse
    if not enumObj then
        return false
    end
    local item = enumObj.FailureGuidNotFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.LiveEditingAtomicUpdateResponse.FailureGuidNotFound"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LiveEditingAtomicUpdateResponse
    if not enumObj then
        return false
    end
    local item = enumObj.FailureHashMismatch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.LiveEditingAtomicUpdateResponse.FailureHashMismatch"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LiveEditingAtomicUpdateResponse
    if not enumObj then
        return false
    end
    local item = enumObj.FailureOperationIllegal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.LiveEditingAtomicUpdateResponse.FailureOperationIllegal"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LiveEditingBroadcastMessageType
    if not enumObj then
        return false
    end
    local item = enumObj.Normal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LiveEditingBroadcastMessageType.Normal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LiveEditingBroadcastMessageType
    if not enumObj then
        return false
    end
    local item = enumObj.Warning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LiveEditingBroadcastMessageType.Warning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LiveEditingBroadcastMessageType
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LiveEditingBroadcastMessageType.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LoadCharacterLayeredClothing
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LoadCharacterLayeredClothing.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LoadCharacterLayeredClothing
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LoadCharacterLayeredClothing.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LoadCharacterLayeredClothing
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LoadCharacterLayeredClothing.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LoadDynamicHeads
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LoadDynamicHeads.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LoadDynamicHeads
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LoadDynamicHeads.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LoadDynamicHeads
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LoadDynamicHeads.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LocationType
    if not enumObj then
        return false
    end
    local item = enumObj.Character
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LocationType.Character" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LocationType
    if not enumObj then
        return false
    end
    local item = enumObj.Camera
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LocationType.Camera" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LocationType
    if not enumObj then
        return false
    end
    local item = enumObj.ObjectPosition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LocationType.ObjectPosition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LuauTypeCheckMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LuauTypeCheckMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LuauTypeCheckMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoCheck
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LuauTypeCheckMode.NoCheck" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LuauTypeCheckMode
    if not enumObj then
        return false
    end
    local item = enumObj.Nonstrict
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LuauTypeCheckMode.Nonstrict" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.LuauTypeCheckMode
    if not enumObj then
        return false
    end
    local item = enumObj.Strict
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.LuauTypeCheckMode.Strict" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MakeupType
    if not enumObj then
        return false
    end
    local item = enumObj.Face
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MakeupType.Face" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MakeupType
    if not enumObj then
        return false
    end
    local item = enumObj.Lip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MakeupType.Lip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MakeupType
    if not enumObj then
        return false
    end
    local item = enumObj.Eye
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MakeupType.Eye" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceBulkPurchasePromptStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Completed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.MarketplaceBulkPurchasePromptStatus.Completed"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceBulkPurchasePromptStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Aborted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceBulkPurchasePromptStatus.Aborted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceBulkPurchasePromptStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceBulkPurchasePromptStatus.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.SystemError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.SystemError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.AlreadyOwned
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.AlreadyOwned" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InsufficientRobux
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.InsufficientRobux"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.QuantityLimitExceeded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.MarketplaceItemPurchaseStatus.QuantityLimitExceeded"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.QuotaExceeded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.QuotaExceeded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotForSale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.NotForSale" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotAvailableForPurchaser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.MarketplaceItemPurchaseStatus.NotAvailableForPurchaser"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.PriceMismatch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.PriceMismatch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.SoldOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.SoldOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.PurchaserIsSeller
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.PurchaserIsSeller"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InsufficientMembership
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.MarketplaceItemPurchaseStatus.InsufficientMembership"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceItemPurchaseStatus
    if not enumObj then
        return false
    end
    local item = enumObj.PlaceInvalid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceItemPurchaseStatus.PlaceInvalid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceProductType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarAsset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceProductType.AvatarAsset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarketplaceProductType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarBundle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarketplaceProductType.AvatarBundle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarkupKind
    if not enumObj then
        return false
    end
    local item = enumObj.PlainText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarkupKind.PlainText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MarkupKind
    if not enumObj then
        return false
    end
    local item = enumObj.Markdown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MarkupKind.Markdown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MatchmakingType
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MatchmakingType.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MatchmakingType
    if not enumObj then
        return false
    end
    local item = enumObj.XboxOnly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MatchmakingType.XboxOnly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MatchmakingType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayStationOnly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MatchmakingType.PlayStationOnly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Plastic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Plastic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.SmoothPlastic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.SmoothPlastic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Neon
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Neon" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Wood
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Wood" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.WoodPlanks
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.WoodPlanks" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Marble
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Marble" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Basalt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Basalt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Slate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Slate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.CrackedLava
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.CrackedLava" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Concrete
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Concrete" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Limestone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Limestone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Granite
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Granite" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Pavement
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Pavement" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Brick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Brick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Pebble
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Pebble" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Cobblestone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Cobblestone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Rock
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Rock" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Sandstone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Sandstone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.CorrodedMetal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.CorrodedMetal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.DiamondPlate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.DiamondPlate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Foil
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Foil" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Metal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Metal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Grass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Grass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.LeafyGrass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.LeafyGrass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Sand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Sand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Fabric
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Fabric" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Snow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Snow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Mud
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Mud" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Ground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Ground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Asphalt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Asphalt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Salt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Salt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Ice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Ice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Glacier
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Glacier" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Glass
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Glass" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.ForceField
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.ForceField" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Air
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Air" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Water
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Water" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Cardboard
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Cardboard" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Carpet
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Carpet" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.CeramicTiles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.CeramicTiles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.ClayRoofTiles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.ClayRoofTiles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.RoofShingles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.RoofShingles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Leather
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Leather" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Plaster
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Plaster" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Material
    if not enumObj then
        return false
    end
    local item = enumObj.Rubber
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Material.Rubber" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MaterialPattern
    if not enumObj then
        return false
    end
    local item = enumObj.Regular
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MaterialPattern.Regular" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MaterialPattern
    if not enumObj then
        return false
    end
    local item = enumObj.Organic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MaterialPattern.Organic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MembershipType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MembershipType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MembershipType
    if not enumObj then
        return false
    end
    local item = enumObj.BuildersClub
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MembershipType.BuildersClub" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MembershipType
    if not enumObj then
        return false
    end
    local item = enumObj.TurboBuildersClub
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MembershipType.TurboBuildersClub" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MembershipType
    if not enumObj then
        return false
    end
    local item = enumObj.OutrageousBuildersClub
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MembershipType.OutrageousBuildersClub" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MembershipType
    if not enumObj then
        return false
    end
    local item = enumObj.Premium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MembershipType.Premium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.DistanceBased
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.DistanceBased" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level00
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level00" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level01
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level01" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level02
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level02" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level03
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level03" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level04
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level04" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level05
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level05" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level06
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level06" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level07
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level07" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level08
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level08" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartDetailLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level09
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartDetailLevel.Level09" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartHeadsAndAccessories
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartHeadsAndAccessories.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartHeadsAndAccessories
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartHeadsAndAccessories.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshPartHeadsAndAccessories
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshPartHeadsAndAccessories.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshScaleUnit
    if not enumObj then
        return false
    end
    local item = enumObj.Stud
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshScaleUnit.Stud" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshScaleUnit
    if not enumObj then
        return false
    end
    local item = enumObj.Meter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshScaleUnit.Meter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshScaleUnit
    if not enumObj then
        return false
    end
    local item = enumObj.CM
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshScaleUnit.CM" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshScaleUnit
    if not enumObj then
        return false
    end
    local item = enumObj.MM
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshScaleUnit.MM" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshScaleUnit
    if not enumObj then
        return false
    end
    local item = enumObj.Foot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshScaleUnit.Foot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshScaleUnit
    if not enumObj then
        return false
    end
    local item = enumObj.Inch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshScaleUnit.Inch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Head
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Head" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Torso
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Torso" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Wedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Wedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Sphere
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Sphere" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Cylinder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Cylinder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.FileMesh
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.FileMesh" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Brick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Brick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Prism
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Prism" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.Pyramid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.Pyramid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.ParallelRamp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.ParallelRamp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.RightAngleRamp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.RightAngleRamp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MeshType
    if not enumObj then
        return false
    end
    local item = enumObj.CornerWedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MeshType.CornerWedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MessageType
    if not enumObj then
        return false
    end
    local item = enumObj.MessageOutput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MessageType.MessageOutput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MessageType
    if not enumObj then
        return false
    end
    local item = enumObj.MessageInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MessageType.MessageInfo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MessageType
    if not enumObj then
        return false
    end
    local item = enumObj.MessageWarning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MessageType.MessageWarning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MessageType
    if not enumObj then
        return false
    end
    local item = enumObj.MessageError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MessageType.MessageError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelLevelOfDetail
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelLevelOfDetail.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelLevelOfDetail
    if not enumObj then
        return false
    end
    local item = enumObj.StreamingMesh
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelLevelOfDetail.StreamingMesh" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelLevelOfDetail
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelLevelOfDetail.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelLevelOfDetail
    if not enumObj then
        return false
    end
    local item = enumObj.SLIM
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelLevelOfDetail.SLIM" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingBehavior.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingBehavior.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Improved
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingBehavior.Improved" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Atomic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingMode.Atomic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Persistent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingMode.Persistent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingMode
    if not enumObj then
        return false
    end
    local item = enumObj.PersistentPerPlayer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingMode.PersistentPerPlayer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModelStreamingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Nonatomic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModelStreamingMode.Nonatomic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultCategory
    if not enumObj then
        return false
    end
    local item = enumObj.ViolationDetected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationResultCategory.ViolationDetected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultCategory
    if not enumObj then
        return false
    end
    local item = enumObj.Borderline
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationResultCategory.Borderline" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultCategory
    if not enumObj then
        return false
    end
    local item = enumObj.NoViolationDetected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ModerationResultCategory.NoViolationDetected"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.ChildExploitation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationResultLabel.ChildExploitation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.SuicideSelfInjuryAndHarmfulBehavior
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ModerationResultLabel.SuicideSelfInjuryAndHarmfulBehavior"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.ThreatsBullyingAndHarassment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ModerationResultLabel.ThreatsBullyingAndHarassment"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.TerrorismAndViolentExtremism
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ModerationResultLabel.TerrorismAndViolentExtremism"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.DiscriminationSlursAndHateSpeech
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ModerationResultLabel.DiscriminationSlursAndHateSpeech"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RealWorldSensitiveEvents
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ModerationResultLabel.RealWorldSensitiveEvents"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.ViolentContentAndGore
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationResultLabel.ViolentContentAndGore" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RomanticAndSexualContent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ModerationResultLabel.RomanticAndSexualContent"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.IllegalAndRegulatedGoodsAndActivities
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.ModerationResultLabel.IllegalAndRegulatedGoodsAndActivities"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Profanity
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationResultLabel.Profanity" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationResultLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Other
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationResultLabel.Other" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ReviewedApproved
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationStatus.ReviewedApproved" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ReviewedRejected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationStatus.ReviewedRejected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotReviewed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationStatus.NotReviewed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotApplicable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationStatus.NotApplicable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModerationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Invalid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModerationStatus.Invalid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModifierKey
    if not enumObj then
        return false
    end
    local item = enumObj.Shift
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModifierKey.Shift" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModifierKey
    if not enumObj then
        return false
    end
    local item = enumObj.Ctrl
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModifierKey.Ctrl" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModifierKey
    if not enumObj then
        return false
    end
    local item = enumObj.Alt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModifierKey.Alt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ModifierKey
    if not enumObj then
        return false
    end
    local item = enumObj.Meta
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ModifierKey.Meta" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MouseBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MouseBehavior.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MouseBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.LockCenter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MouseBehavior.LockCenter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MouseBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.LockCurrentPosition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MouseBehavior.LockCurrentPosition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MoveState
    if not enumObj then
        return false
    end
    local item = enumObj.Stopped
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MoveState.Stopped" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MoveState
    if not enumObj then
        return false
    end
    local item = enumObj.Coasting
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MoveState.Coasting" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MoveState
    if not enumObj then
        return false
    end
    local item = enumObj.Pushing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MoveState.Pushing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MoveState
    if not enumObj then
        return false
    end
    local item = enumObj.Stopping
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MoveState.Stopping" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MoveState
    if not enumObj then
        return false
    end
    local item = enumObj.AirFree
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MoveState.AirFree" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MuteState
    if not enumObj then
        return false
    end
    local item = enumObj.Unmuted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MuteState.Unmuted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.MuteState
    if not enumObj then
        return false
    end
    local item = enumObj.Muted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.MuteState.Muted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NameOcclusion
    if not enumObj then
        return false
    end
    local item = enumObj.NoOcclusion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NameOcclusion.NoOcclusion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NameOcclusion
    if not enumObj then
        return false
    end
    local item = enumObj.EnemyOcclusion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NameOcclusion.EnemyOcclusion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NameOcclusion
    if not enumObj then
        return false
    end
    local item = enumObj.OccludeAll
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NameOcclusion.OccludeAll" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NegateOperationHiddenHistory
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NegateOperationHiddenHistory.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NegateOperationHiddenHistory
    if not enumObj then
        return false
    end
    local item = enumObj.NegatedUnion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NegateOperationHiddenHistory.NegatedUnion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NegateOperationHiddenHistory
    if not enumObj then
        return false
    end
    local item = enumObj.NegatedIntersection
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.NegateOperationHiddenHistory.NegatedIntersection"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NetworkOwnership
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NetworkOwnership.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NetworkOwnership
    if not enumObj then
        return false
    end
    local item = enumObj.Manual
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NetworkOwnership.Manual" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NetworkOwnership
    if not enumObj then
        return false
    end
    local item = enumObj.OnContact
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NetworkOwnership.OnContact" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NetworkStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NetworkStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NetworkStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Connected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NetworkStatus.Connected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NetworkStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Disconnected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NetworkStatus.Disconnected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NoiseType
    if not enumObj then
        return false
    end
    local item = enumObj.SimplexGabor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NoiseType.SimplexGabor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NormalId
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NormalId.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NormalId
    if not enumObj then
        return false
    end
    local item = enumObj.Top
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NormalId.Top" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NormalId
    if not enumObj then
        return false
    end
    local item = enumObj.Back
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NormalId.Back" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NormalId
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NormalId.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NormalId
    if not enumObj then
        return false
    end
    local item = enumObj.Bottom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NormalId.Bottom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NormalId
    if not enumObj then
        return false
    end
    local item = enumObj.Front
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NormalId.Front" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NotificationButtonType
    if not enumObj then
        return false
    end
    local item = enumObj.Primary
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NotificationButtonType.Primary" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.NotificationButtonType
    if not enumObj then
        return false
    end
    local item = enumObj.Secondary
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.NotificationButtonType.Secondary" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OperationType
    if not enumObj then
        return false
    end
    local item = enumObj.Null
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OperationType.Null" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OperationType
    if not enumObj then
        return false
    end
    local item = enumObj.Union
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OperationType.Union" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OperationType
    if not enumObj then
        return false
    end
    local item = enumObj.Subtraction
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OperationType.Subtraction" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OperationType
    if not enumObj then
        return false
    end
    local item = enumObj.Intersection
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OperationType.Intersection" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OperationType
    if not enumObj then
        return false
    end
    local item = enumObj.Primitive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OperationType.Primitive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OrientationAlignmentMode
    if not enumObj then
        return false
    end
    local item = enumObj.OneAttachment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OrientationAlignmentMode.OneAttachment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OrientationAlignmentMode
    if not enumObj then
        return false
    end
    local item = enumObj.TwoAttachment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OrientationAlignmentMode.TwoAttachment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitSource
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitSource.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitSource
    if not enumObj then
        return false
    end
    local item = enumObj.Created
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitSource.Created" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitSource
    if not enumObj then
        return false
    end
    local item = enumObj.Purchased
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitSource.Purchased" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitType
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitType.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitType
    if not enumObj then
        return false
    end
    local item = enumObj.Avatar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitType.Avatar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitType
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicHead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitType.DynamicHead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitType
    if not enumObj then
        return false
    end
    local item = enumObj.Shoes
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitType.Shoes" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutfitType
    if not enumObj then
        return false
    end
    local item = enumObj.Makeup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutfitType.Makeup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutputLayoutMode
    if not enumObj then
        return false
    end
    local item = enumObj.Horizontal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutputLayoutMode.Horizontal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OutputLayoutMode
    if not enumObj then
        return false
    end
    local item = enumObj.Vertical
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OutputLayoutMode.Vertical" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OverrideMouseIconBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OverrideMouseIconBehavior.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OverrideMouseIconBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.ForceShow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OverrideMouseIconBehavior.ForceShow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.OverrideMouseIconBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.ForceHide
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.OverrideMouseIconBehavior.ForceHide" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PackagePermission
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PackagePermission.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PackagePermission
    if not enumObj then
        return false
    end
    local item = enumObj.NoAccess
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PackagePermission.NoAccess" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PackagePermission
    if not enumObj then
        return false
    end
    local item = enumObj.Revoked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PackagePermission.Revoked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PackagePermission
    if not enumObj then
        return false
    end
    local item = enumObj.UseView
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PackagePermission.UseView" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PackagePermission
    if not enumObj then
        return false
    end
    local item = enumObj.Edit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PackagePermission.Edit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PackagePermission
    if not enumObj then
        return false
    end
    local item = enumObj.Own
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PackagePermission.Own" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PartType
    if not enumObj then
        return false
    end
    local item = enumObj.Ball
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PartType.Ball" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PartType
    if not enumObj then
        return false
    end
    local item = enumObj.Block
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PartType.Block" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PartType
    if not enumObj then
        return false
    end
    local item = enumObj.Cylinder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PartType.Cylinder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PartType
    if not enumObj then
        return false
    end
    local item = enumObj.Wedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PartType.Wedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PartType
    if not enumObj then
        return false
    end
    local item = enumObj.CornerWedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PartType.CornerWedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShape
    if not enumObj then
        return false
    end
    local item = enumObj.Box
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShape.Box" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShape
    if not enumObj then
        return false
    end
    local item = enumObj.Sphere
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShape.Sphere" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShape
    if not enumObj then
        return false
    end
    local item = enumObj.Cylinder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShape.Cylinder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShape
    if not enumObj then
        return false
    end
    local item = enumObj.Disc
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShape.Disc" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShapeInOut
    if not enumObj then
        return false
    end
    local item = enumObj.Outward
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShapeInOut.Outward" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShapeInOut
    if not enumObj then
        return false
    end
    local item = enumObj.Inward
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShapeInOut.Inward" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShapeInOut
    if not enumObj then
        return false
    end
    local item = enumObj.InAndOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShapeInOut.InAndOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShapeStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Volume
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShapeStyle.Volume" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleEmitterShapeStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Surface
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleEmitterShapeStyle.Surface" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookLayout
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookLayout.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Grid2x2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookLayout.Grid2x2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Grid4x4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookLayout.Grid4x4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Grid8x8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookLayout.Grid8x8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookLayout.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookMode
    if not enumObj then
        return false
    end
    local item = enumObj.Loop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookMode.Loop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookMode
    if not enumObj then
        return false
    end
    local item = enumObj.OneShot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookMode.OneShot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookMode
    if not enumObj then
        return false
    end
    local item = enumObj.PingPong
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookMode.PingPong" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookMode
    if not enumObj then
        return false
    end
    local item = enumObj.Random
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookMode.Random" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookTextureCompatible
    if not enumObj then
        return false
    end
    local item = enumObj.NotCompatible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ParticleFlipbookTextureCompatible.NotCompatible"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookTextureCompatible
    if not enumObj then
        return false
    end
    local item = enumObj.Compatible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ParticleFlipbookTextureCompatible.Compatible"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleFlipbookTextureCompatible
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleFlipbookTextureCompatible.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.FacingCamera
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleOrientation.FacingCamera" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.FacingCameraWorldUp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleOrientation.FacingCameraWorldUp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.VelocityParallel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleOrientation.VelocityParallel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ParticleOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.VelocityPerpendicular
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ParticleOrientation.VelocityPerpendicular" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathStatus.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ClosestNoPath
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathStatus.ClosestNoPath" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ClosestOutOfRange
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathStatus.ClosestOutOfRange" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FailStartNotEmpty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathStatus.FailStartNotEmpty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FailFinishNotEmpty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathStatus.FailFinishNotEmpty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NoPath
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathStatus.NoPath" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathWaypointAction
    if not enumObj then
        return false
    end
    local item = enumObj.Walk
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathWaypointAction.Walk" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathWaypointAction
    if not enumObj then
        return false
    end
    local item = enumObj.Jump
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathWaypointAction.Jump" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathWaypointAction
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathWaypointAction.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathfindingUseImprovedSearch
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathfindingUseImprovedSearch.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathfindingUseImprovedSearch
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathfindingUseImprovedSearch.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PathfindingUseImprovedSearch
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PathfindingUseImprovedSearch.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PeoplePageLayout
    if not enumObj then
        return false
    end
    local item = enumObj.Card
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PeoplePageLayout.Card" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PeoplePageLayout
    if not enumObj then
        return false
    end
    local item = enumObj.List
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PeoplePageLayout.List" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PerformanceOverlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.Overdraw
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PerformanceOverlayMode.Overdraw" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PerformanceOverlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.Transparent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PerformanceOverlayMode.Transparent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PerformanceOverlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.Decals
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PerformanceOverlayMode.Decals" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PerformanceOverlayMode
    if not enumObj then
        return false
    end
    local item = enumObj.Lights
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PerformanceOverlayMode.Lights" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PermissionLevelShown
    if not enumObj then
        return false
    end
    local item = enumObj.Game
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PermissionLevelShown.Game" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PermissionLevelShown
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxGame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PermissionLevelShown.RobloxGame" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PermissionLevelShown
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxScript
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PermissionLevelShown.RobloxScript" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PermissionLevelShown
    if not enumObj then
        return false
    end
    local item = enumObj.Studio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PermissionLevelShown.Studio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PermissionLevelShown
    if not enumObj then
        return false
    end
    local item = enumObj.Roblox
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PermissionLevelShown.Roblox" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicalConstraintType
    if not enumObj then
        return false
    end
    local item = enumObj.AnimationConstraint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicalConstraintType.AnimationConstraint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicalConstraintType
    if not enumObj then
        return false
    end
    local item = enumObj.Motor6D
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicalConstraintType.Motor6D" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicsSimulationRate
    if not enumObj then
        return false
    end
    local item = enumObj.Fixed240Hz
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicsSimulationRate.Fixed240Hz" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicsSimulationRate
    if not enumObj then
        return false
    end
    local item = enumObj.Fixed120Hz
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicsSimulationRate.Fixed120Hz" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicsSimulationRate
    if not enumObj then
        return false
    end
    local item = enumObj.Fixed60Hz
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicsSimulationRate.Fixed60Hz" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicsSteppingMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicsSteppingMethod.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicsSteppingMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Fixed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicsSteppingMethod.Fixed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PhysicsSteppingMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Adaptive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PhysicsSteppingMethod.Adaptive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaceContentPreference.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaceContentPreference.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.MentionsAndReplies
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaceContentPreference.MentionsAndReplies" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaceContentPreference
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaceContentPreference.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlacePublishType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlacePublishType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlacePublishType
    if not enumObj then
        return false
    end
    local item = enumObj.Publish
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlacePublishType.Publish" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlacePublishType
    if not enumObj then
        return false
    end
    local item = enumObj.Save
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlacePublishType.Save" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.Windows
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.Windows" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.OSX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.OSX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.IOS
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.IOS" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.Android
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.Android" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.XBoxOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.XBoxOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.PS4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.PS4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.PS3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.PS3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.XBox360
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.XBox360" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.WiiU
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.WiiU" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.NX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.NX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.Ouya
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.Ouya" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.AndroidTV
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.AndroidTV" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.Chromecast
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.Chromecast" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.Linux
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.Linux" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.SteamOS
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.SteamOS" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.WebOS
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.WebOS" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.DOS
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.DOS" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.BeOS
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.BeOS" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.UWP
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.UWP" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.PS5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.PS5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.MetaOS
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.MetaOS" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.Web
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.Web" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Platform
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Platform.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaybackState
    if not enumObj then
        return false
    end
    local item = enumObj.Begin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaybackState.Begin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaybackState
    if not enumObj then
        return false
    end
    local item = enumObj.Delayed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaybackState.Delayed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaybackState
    if not enumObj then
        return false
    end
    local item = enumObj.Playing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaybackState.Playing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaybackState
    if not enumObj then
        return false
    end
    local item = enumObj.Paused
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaybackState.Paused" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaybackState
    if not enumObj then
        return false
    end
    local item = enumObj.Completed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaybackState.Completed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlaybackState
    if not enumObj then
        return false
    end
    local item = enumObj.Cancelled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlaybackState.Cancelled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerActions
    if not enumObj then
        return false
    end
    local item = enumObj.CharacterForward
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerActions.CharacterForward" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerActions
    if not enumObj then
        return false
    end
    local item = enumObj.CharacterBackward
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerActions.CharacterBackward" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerActions
    if not enumObj then
        return false
    end
    local item = enumObj.CharacterLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerActions.CharacterLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerActions
    if not enumObj then
        return false
    end
    local item = enumObj.CharacterRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerActions.CharacterRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerActions
    if not enumObj then
        return false
    end
    local item = enumObj.CharacterJump
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerActions.CharacterJump" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerCharacterDestroyBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerCharacterDestroyBehavior.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerCharacterDestroyBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerCharacterDestroyBehavior.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerCharacterDestroyBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerCharacterDestroyBehavior.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerChatType
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerChatType.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerChatType
    if not enumObj then
        return false
    end
    local item = enumObj.Team
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerChatType.Team" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerChatType
    if not enumObj then
        return false
    end
    local item = enumObj.Whisper
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerChatType.Whisper" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerDataErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.LoadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerDataErrorState.LoadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerDataErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.FlushFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerDataErrorState.FlushFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerDataErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.ReleaseFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerDataErrorState.ReleaseFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerDataErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerDataErrorState.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerDataLoadFailureBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Failure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerDataLoadFailureBehavior.Failure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerDataLoadFailureBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.FallbackToDefault
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.PlayerDataLoadFailureBehavior.FallbackToDefault"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerDataLoadFailureBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Kick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerDataLoadFailureBehavior.Kick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerExitReason
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerExitReason.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerExitReason
    if not enumObj then
        return false
    end
    local item = enumObj.PlatformKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerExitReason.PlatformKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerExitReason
    if not enumObj then
        return false
    end
    local item = enumObj.CreatorKick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerExitReason.CreatorKick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformActivationStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.New
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformActivationStatus.New" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Active
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformActivationStatus.Active" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Lapsed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformActivationStatus.Lapsed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformActivationStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Reactivated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformActivationStatus.Reactivated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformSpenderStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformSpenderStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformSpenderStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Active
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformSpenderStatus.Active" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PlayerPlatformSpenderStatus
    if not enumObj then
        return false
    end
    local item = enumObj.OtherPayer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PlayerPlatformSpenderStatus.OtherPayer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PluginConnectionTargetType
    if not enumObj then
        return false
    end
    local item = enumObj.Edit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PluginConnectionTargetType.Edit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PluginConnectionTargetType
    if not enumObj then
        return false
    end
    local item = enumObj.Test
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PluginConnectionTargetType.Test" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.In
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingDirection.In" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Out
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingDirection.Out" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.InOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingDirection.InOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Linear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingStyle.Linear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Constant
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingStyle.Constant" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Elastic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingStyle.Elastic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Cubic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingStyle.Cubic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Bounce
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingStyle.Bounce" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PoseEasingStyle
    if not enumObj then
        return false
    end
    local item = enumObj.CubicV2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PoseEasingStyle.CubicV2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PositionAlignmentMode
    if not enumObj then
        return false
    end
    local item = enumObj.OneAttachment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PositionAlignmentMode.OneAttachment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PositionAlignmentMode
    if not enumObj then
        return false
    end
    local item = enumObj.TwoAttachment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PositionAlignmentMode.TwoAttachment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PredictionMode
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PredictionMode.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PredictionMode
    if not enumObj then
        return false
    end
    local item = enumObj.On
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PredictionMode.On" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PredictionMode
    if not enumObj then
        return false
    end
    local item = enumObj.Off
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PredictionMode.Off" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PredictionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Authoritative
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PredictionStatus.Authoritative" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PredictionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Predicted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PredictionStatus.Predicted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PredictionStatus
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PredictionStatus.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PreferredInput
    if not enumObj then
        return false
    end
    local item = enumObj.KeyboardAndMouse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PreferredInput.KeyboardAndMouse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PreferredInput
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PreferredInput.Gamepad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PreferredInput
    if not enumObj then
        return false
    end
    local item = enumObj.Touch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PreferredInput.Touch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PreferredTextSize
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PreferredTextSize.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PreferredTextSize
    if not enumObj then
        return false
    end
    local item = enumObj.Large
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PreferredTextSize.Large" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PreferredTextSize
    if not enumObj then
        return false
    end
    local item = enumObj.Larger
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PreferredTextSize.Larger" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PreferredTextSize
    if not enumObj then
        return false
    end
    local item = enumObj.Largest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PreferredTextSize.Largest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrefetchDownloadStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotStarted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrefetchDownloadStatus.NotStarted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrefetchDownloadStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InProgress
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrefetchDownloadStatus.InProgress" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrefetchDownloadStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Completed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrefetchDownloadStatus.Completed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrefetchDownloadStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Failed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrefetchDownloadStatus.Failed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimalPhysicsSolver
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimalPhysicsSolver.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimalPhysicsSolver
    if not enumObj then
        return false
    end
    local item = enumObj.Experimental
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimalPhysicsSolver.Experimental" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimalPhysicsSolver
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimalPhysicsSolver.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.Null
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimitiveType.Null" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.Ball
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimitiveType.Ball" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.Cylinder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimitiveType.Cylinder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.Block
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimitiveType.Block" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.Wedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimitiveType.Wedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.CornerWedge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrimitiveType.CornerWedge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrivilegeType
    if not enumObj then
        return false
    end
    local item = enumObj.Banned
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrivilegeType.Banned" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrivilegeType
    if not enumObj then
        return false
    end
    local item = enumObj.Visitor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrivilegeType.Visitor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrivilegeType
    if not enumObj then
        return false
    end
    local item = enumObj.Member
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrivilegeType.Member" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrivilegeType
    if not enumObj then
        return false
    end
    local item = enumObj.Admin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrivilegeType.Admin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PrivilegeType
    if not enumObj then
        return false
    end
    local item = enumObj.Owner
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PrivilegeType.Owner" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductLocationRestriction
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarShop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductLocationRestriction.AvatarShop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductLocationRestriction
    if not enumObj then
        return false
    end
    local item = enumObj.AllowedGames
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductLocationRestriction.AllowedGames" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductLocationRestriction
    if not enumObj then
        return false
    end
    local item = enumObj.AllGames
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductLocationRestriction.AllGames" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductPurchaseChannel
    if not enumObj then
        return false
    end
    local item = enumObj.InExperience
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductPurchaseChannel.InExperience" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductPurchaseChannel
    if not enumObj then
        return false
    end
    local item = enumObj.ExperienceDetailsPage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ProductPurchaseChannel.ExperienceDetailsPage"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductPurchaseChannel
    if not enumObj then
        return false
    end
    local item = enumObj.AdReward
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductPurchaseChannel.AdReward" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductPurchaseChannel
    if not enumObj then
        return false
    end
    local item = enumObj.CommerceProduct
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductPurchaseChannel.CommerceProduct" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductPurchaseDecision
    if not enumObj then
        return false
    end
    local item = enumObj.NotProcessedYet
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductPurchaseDecision.NotProcessedYet" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProductPurchaseDecision
    if not enumObj then
        return false
    end
    local item = enumObj.PurchaseGranted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProductPurchaseDecision.PurchaseGranted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.PermissionDenied
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.PermissionDenied" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.UploadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoUserInput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.NoUserInput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.UnknownFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.UnknownFailure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.UGCValidationFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.UGCValidationFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.ModeratedName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.ModeratedName" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.PurchaseFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.PurchaseFailure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.TokenInvalid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAssetResult.TokenInvalid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.PermissionDenied
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.PermissionDenied" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.UploadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoUserInput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.NoUserInput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidHumanoidDescription
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.PromptCreateAvatarResult.InvalidHumanoidDescription"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.UGCValidationFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.PromptCreateAvatarResult.UGCValidationFailed"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.ModeratedName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.ModeratedName" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.MaxOutfits
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.MaxOutfits" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.PurchaseFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.PurchaseFailure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.UnknownFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.UnknownFailure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptCreateAvatarResult
    if not enumObj then
        return false
    end
    local item = enumObj.TokenInvalid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptCreateAvatarResult.TokenInvalid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptExperienceDetailsResult
    if not enumObj then
        return false
    end
    local item = enumObj.PromptClosed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptExperienceDetailsResult.PromptClosed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptExperienceDetailsResult
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportAttempted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.PromptExperienceDetailsResult.TeleportAttempted"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptLinkSharingResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptLinkSharingResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptLinkSharingResult
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptLinkSharingResult.PlayerLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptLinkSharingResult
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidLaunchData
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptLinkSharingResult.InvalidLaunchData" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptPublishAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptPublishAssetResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptPublishAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.PermissionDenied
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptPublishAssetResult.PermissionDenied" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptPublishAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptPublishAssetResult.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptPublishAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptPublishAssetResult.UploadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptPublishAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoUserInput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptPublishAssetResult.NoUserInput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PromptPublishAssetResult
    if not enumObj then
        return false
    end
    local item = enumObj.UnknownFailure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PromptPublishAssetResult.UnknownFailure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PropertyStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Ok
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PropertyStatus.Ok" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PropertyStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Warning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PropertyStatus.Warning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PropertyStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PropertyStatus.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptExclusivity
    if not enumObj then
        return false
    end
    local item = enumObj.OnePerButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptExclusivity.OnePerButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptExclusivity
    if not enumObj then
        return false
    end
    local item = enumObj.OneGlobally
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptExclusivity.OneGlobally" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptExclusivity
    if not enumObj then
        return false
    end
    local item = enumObj.AlwaysShow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptExclusivity.AlwaysShow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Keyboard
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptInputType.Keyboard" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptInputType.Gamepad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Touch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptInputType.Touch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptStyle.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ProximityPromptStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ProximityPromptStyle.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PurchaseOption
    if not enumObj then
        return false
    end
    local item = enumObj.TimedOption
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PurchaseOption.TimedOption" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.PurchaseOption
    if not enumObj then
        return false
    end
    local item = enumObj.Permanent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.PurchaseOption.Permanent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level01
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level01" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level02
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level02" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level03
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level03" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level04
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level04" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level05
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level05" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level06
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level06" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level07
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level07" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level08
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level08" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level09
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level09" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level10
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level10" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level11
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level11" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level12
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level12" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level13
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level13" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level14
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level14" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level15" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level16
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level16" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level17
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level17" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level18
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level18" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level19
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level19" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level20
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level20" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.QualityLevel
    if not enumObj then
        return false
    end
    local item = enumObj.Level21
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.QualityLevel.Level21" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.R15CollisionType
    if not enumObj then
        return false
    end
    local item = enumObj.OuterBox
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.R15CollisionType.OuterBox" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.R15CollisionType
    if not enumObj then
        return false
    end
    local item = enumObj.InnerBox
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.R15CollisionType.InnerBox" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RaycastFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Exclude
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RaycastFilterType.Exclude" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RaycastFilterType
    if not enumObj then
        return false
    end
    local item = enumObj.Include
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RaycastFilterType.Include" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReadCapturesFromGalleryResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReadCapturesFromGalleryResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReadCapturesFromGalleryResult
    if not enumObj then
        return false
    end
    local item = enumObj.NeedPermission
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.ReadCapturesFromGalleryResult.NeedPermission"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReceiptDecision
    if not enumObj then
        return false
    end
    local item = enumObj.NotProcessedYet
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReceiptDecision.NotProcessedYet" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReceiptDecision
    if not enumObj then
        return false
    end
    local item = enumObj.Processed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReceiptDecision.Processed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReceiptType
    if not enumObj then
        return false
    end
    local item = enumObj.DeveloperProduct
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReceiptType.DeveloperProduct" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReceiptType
    if not enumObj then
        return false
    end
    local item = enumObj.RobuxTransferSender
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReceiptType.RobuxTransferSender" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReceiptType
    if not enumObj then
        return false
    end
    local item = enumObj.RobuxTransferReceiver
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReceiptType.RobuxTransferReceiver" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationActionType
    if not enumObj then
        return false
    end
    local item = enumObj.AddReaction
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationActionType.AddReaction" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationActionType
    if not enumObj then
        return false
    end
    local item = enumObj.RemoveReaction
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationActionType.RemoveReaction" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Share
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationActionType.Share" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Report
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationActionType.Report" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Comment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationActionType.Comment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Play
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationActionType.Play" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationActionType
    if not enumObj then
        return false
    end
    local item = enumObj.Purchase
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationActionType.Purchase" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationDepartureIntent
    if not enumObj then
        return false
    end
    local item = enumObj.Neutral
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationDepartureIntent.Neutral" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationDepartureIntent
    if not enumObj then
        return false
    end
    local item = enumObj.Positive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationDepartureIntent.Positive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationDepartureIntent
    if not enumObj then
        return false
    end
    local item = enumObj.Negative
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationDepartureIntent.Negative" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationImpressionType
    if not enumObj then
        return false
    end
    local item = enumObj.View
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationImpressionType.View" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationImpressionType
    if not enumObj then
        return false
    end
    local item = enumObj.NotViewable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationImpressionType.NotViewable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationItemContentType
    if not enumObj then
        return false
    end
    local item = enumObj.Static
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationItemContentType.Static" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationItemContentType
    if not enumObj then
        return false
    end
    local item = enumObj.Dynamic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationItemContentType.Dynamic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationItemContentType
    if not enumObj then
        return false
    end
    local item = enumObj.Interactive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationItemContentType.Interactive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationItemVisibility
    if not enumObj then
        return false
    end
    local item = enumObj.Private
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationItemVisibility.Private" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationItemVisibility
    if not enumObj then
        return false
    end
    local item = enumObj.Public
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationItemVisibility.Public" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationPreferenceTargetType
    if not enumObj then
        return false
    end
    local item = enumObj.User
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationPreferenceTargetType.User" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationPreferenceTargetType
    if not enumObj then
        return false
    end
    local item = enumObj.Universe
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationPreferenceTargetType.Universe" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationPreferenceTargetType
    if not enumObj then
        return false
    end
    local item = enumObj.CustomTag
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.RecommendationPreferenceTargetType.CustomTag"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationPreferenceType
    if not enumObj then
        return false
    end
    local item = enumObj.AddFollow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationPreferenceType.AddFollow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationPreferenceType
    if not enumObj then
        return false
    end
    local item = enumObj.RemoveFollow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationPreferenceType.RemoveFollow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationPreferenceType
    if not enumObj then
        return false
    end
    local item = enumObj.AddMute
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationPreferenceType.AddMute" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RecommendationPreferenceType
    if not enumObj then
        return false
    end
    local item = enumObj.RemoveMute
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RecommendationPreferenceType.RemoveMute" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RejectCharacterDeletions
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RejectCharacterDeletions.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RejectCharacterDeletions
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RejectCharacterDeletions.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RejectCharacterDeletions
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RejectCharacterDeletions.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderFidelity.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Precise
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderFidelity.Precise" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderFidelity
    if not enumObj then
        return false
    end
    local item = enumObj.Performance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderFidelity.Performance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderPriority
    if not enumObj then
        return false
    end
    local item = enumObj.First
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderPriority.First" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Input
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderPriority.Input" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Camera
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderPriority.Camera" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Character
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderPriority.Character" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Last
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderPriority.Last" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderingCacheOptimizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderingCacheOptimizationMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderingCacheOptimizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderingCacheOptimizationMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderingCacheOptimizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderingCacheOptimizationMode.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderingTestComparisonMethod
    if not enumObj then
        return false
    end
    local item = enumObj.psnr
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderingTestComparisonMethod.psnr" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RenderingTestComparisonMethod
    if not enumObj then
        return false
    end
    local item = enumObj.diff
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RenderingTestComparisonMethod.diff" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReplicateInstanceDestroySetting
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReplicateInstanceDestroySetting.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReplicateInstanceDestroySetting
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReplicateInstanceDestroySetting.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReplicateInstanceDestroySetting
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReplicateInstanceDestroySetting.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ResamplerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ResamplerMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ResamplerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Pixelated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ResamplerMode.Pixelated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReservedHighlightId
    if not enumObj then
        return false
    end
    local item = enumObj.Standard
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReservedHighlightId.Standard" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReservedHighlightId
    if not enumObj then
        return false
    end
    local item = enumObj.Active
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReservedHighlightId.Active" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReservedHighlightId
    if not enumObj then
        return false
    end
    local item = enumObj.Hover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReservedHighlightId.Hover" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReservedHighlightId
    if not enumObj then
        return false
    end
    local item = enumObj.Selection
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReservedHighlightId.Selection" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReservedHighlightId
    if not enumObj then
        return false
    end
    local item = enumObj.NegatedPart
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReservedHighlightId.NegatedPart" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RestPose
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RestPose.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RestPose
    if not enumObj then
        return false
    end
    local item = enumObj.RotationsReset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RestPose.RotationsReset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RestPose
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RestPose.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RestPoseModel
    if not enumObj then
        return false
    end
    local item = enumObj.FromRigInACE
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RestPoseModel.FromRigInACE" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RestPoseModel
    if not enumObj then
        return false
    end
    local item = enumObj.FromRigInFile
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RestPoseModel.FromRigInFile" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RestPoseModel
    if not enumObj then
        return false
    end
    local item = enumObj.FromRigInFileZeroedRotations
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RestPoseModel.FromRigInFileZeroedRotations" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RestPoseModel
    if not enumObj then
        return false
    end
    local item = enumObj.FromCustomClip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RestPoseModel.FromCustomClip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReturnKeyType
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReturnKeyType.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReturnKeyType
    if not enumObj then
        return false
    end
    local item = enumObj.Done
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReturnKeyType.Done" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReturnKeyType
    if not enumObj then
        return false
    end
    local item = enumObj.Go
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReturnKeyType.Go" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReturnKeyType
    if not enumObj then
        return false
    end
    local item = enumObj.Next
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReturnKeyType.Next" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReturnKeyType
    if not enumObj then
        return false
    end
    local item = enumObj.Search
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReturnKeyType.Search" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReturnKeyType
    if not enumObj then
        return false
    end
    local item = enumObj.Send
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReturnKeyType.Send" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.NoReverb
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.NoReverb" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.GenericReverb
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.GenericReverb" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.PaddedCell
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.PaddedCell" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Room
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Room" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Bathroom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Bathroom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.LivingRoom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.LivingRoom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.StoneRoom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.StoneRoom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Auditorium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Auditorium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.ConcertHall
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.ConcertHall" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Cave
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Cave" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Arena
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Arena" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Hangar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Hangar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.CarpettedHallway
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.CarpettedHallway" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Hallway
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Hallway" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.StoneCorridor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.StoneCorridor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Alley
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Alley" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Forest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Forest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.City
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.City" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Mountains
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Mountains" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Quarry
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Quarry" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.Plain
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.Plain" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.ParkingLot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.ParkingLot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.SewerPipe
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.SewerPipe" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReverbType
    if not enumObj then
        return false
    end
    local item = enumObj.UnderWater
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReverbType.UnderWater" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReviewableContentState
    if not enumObj then
        return false
    end
    local item = enumObj.Pending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReviewableContentState.Pending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReviewableContentState
    if not enumObj then
        return false
    end
    local item = enumObj.Completed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReviewableContentState.Completed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ReviewableContentState
    if not enumObj then
        return false
    end
    local item = enumObj.Failed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ReviewableContentState.Failed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.Select
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.Select" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.Scale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.Scale" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.Rotate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.Rotate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.Move
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.Move" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.Transform
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.Transform" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.ColorPicker
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.ColorPicker" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.MaterialPicker
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.MaterialPicker" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.Group
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.Group" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.Ungroup
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.Ungroup" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RibbonTool
    if not enumObj then
        return false
    end
    local item = enumObj.PivotEditor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RibbonTool.PivotEditor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Invalid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Invalid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Root
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Root" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftHip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftHip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftKnee
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftKnee" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftAnkle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftAnkle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightHip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightHip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightKnee
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightKnee" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightAnkle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightAnkle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Waist
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Waist" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftShoulder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftShoulder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftElbow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftElbow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftWrist
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftWrist" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightShoulder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightShoulder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightElbow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightElbow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightWrist
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightWrist" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Neck
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Neck" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Spine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Spine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Chest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Chest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.HeadBase
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.HeadBase" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftClavicle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftClavicle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightClavicle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightClavicle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.LeftToeBase
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.LeftToeBase" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.RightToeBase
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.RightToeBase" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Thumb1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Thumb1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Thumb2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Thumb2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Thumb3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Thumb3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Index1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Index1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Index2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Index2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Index3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Index3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Middle1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Middle1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Middle2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Middle2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Middle3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Middle3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Ring1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Ring1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Ring2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Ring2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Ring3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Ring3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Pinky1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Pinky1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Pinky2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Pinky2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigLabel
    if not enumObj then
        return false
    end
    local item = enumObj.Pinky3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigLabel.Pinky3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigScale
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigScale.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigScale
    if not enumObj then
        return false
    end
    local item = enumObj.Rthro
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigScale.Rthro" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigScale
    if not enumObj then
        return false
    end
    local item = enumObj.RthroNarrow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigScale.RthroNarrow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigType
    if not enumObj then
        return false
    end
    local item = enumObj.R15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigType.R15" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigType
    if not enumObj then
        return false
    end
    local item = enumObj.CustomHumanoid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigType.CustomHumanoid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigType
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigType.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RigType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RigType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RollOffMode
    if not enumObj then
        return false
    end
    local item = enumObj.Inverse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RollOffMode.Inverse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RollOffMode
    if not enumObj then
        return false
    end
    local item = enumObj.Linear
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RollOffMode.Linear" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RollOffMode
    if not enumObj then
        return false
    end
    local item = enumObj.LinearSquare
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RollOffMode.LinearSquare" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RollOffMode
    if not enumObj then
        return false
    end
    local item = enumObj.InverseTapered
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RollOffMode.InverseTapered" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RolloutState
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RolloutState.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RolloutState
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RolloutState.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RolloutState
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RolloutState.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationOrder
    if not enumObj then
        return false
    end
    local item = enumObj.XYZ
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationOrder.XYZ" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationOrder
    if not enumObj then
        return false
    end
    local item = enumObj.XZY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationOrder.XZY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationOrder
    if not enumObj then
        return false
    end
    local item = enumObj.YZX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationOrder.YZX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationOrder
    if not enumObj then
        return false
    end
    local item = enumObj.YXZ
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationOrder.YXZ" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationOrder
    if not enumObj then
        return false
    end
    local item = enumObj.ZXY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationOrder.ZXY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationOrder
    if not enumObj then
        return false
    end
    local item = enumObj.ZYX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationOrder.ZYX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationType
    if not enumObj then
        return false
    end
    local item = enumObj.MovementRelative
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationType.MovementRelative" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RotationType
    if not enumObj then
        return false
    end
    local item = enumObj.CameraRelative
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RotationType.CameraRelative" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RsvpStatus
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RsvpStatus.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RsvpStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Going
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RsvpStatus.Going" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RsvpStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotGoing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RsvpStatus.NotGoing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RtlTextSupport
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RtlTextSupport.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RtlTextSupport
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RtlTextSupport.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RtlTextSupport
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RtlTextSupport.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RunContext
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RunContext.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RunContext
    if not enumObj then
        return false
    end
    local item = enumObj.Server
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RunContext.Server" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RunContext
    if not enumObj then
        return false
    end
    local item = enumObj.Client
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RunContext.Client" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RunContext
    if not enumObj then
        return false
    end
    local item = enumObj.Plugin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RunContext.Plugin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RunState
    if not enumObj then
        return false
    end
    local item = enumObj.Stopped
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RunState.Stopped" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RunState
    if not enumObj then
        return false
    end
    local item = enumObj.Running
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RunState.Running" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RunState
    if not enumObj then
        return false
    end
    local item = enumObj.Paused
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RunState.Paused" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RuntimeUndoBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Aggregate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RuntimeUndoBehavior.Aggregate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RuntimeUndoBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Snapshot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RuntimeUndoBehavior.Snapshot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.RuntimeUndoBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Hybrid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.RuntimeUndoBehavior.Hybrid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SafeAreaCompatibility
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SafeAreaCompatibility.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SafeAreaCompatibility
    if not enumObj then
        return false
    end
    local item = enumObj.FullscreenExtension
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SafeAreaCompatibility.FullscreenExtension" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SalesTypeFilter
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SalesTypeFilter.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SalesTypeFilter
    if not enumObj then
        return false
    end
    local item = enumObj.Collectibles
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SalesTypeFilter.Collectibles" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SalesTypeFilter
    if not enumObj then
        return false
    end
    local item = enumObj.Premium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SalesTypeFilter.Premium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SalesTypeFilter
    if not enumObj then
        return false
    end
    local item = enumObj.TimedOptions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SalesTypeFilter.TimedOptions" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SandboxedInstanceMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SandboxedInstanceMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SandboxedInstanceMode
    if not enumObj then
        return false
    end
    local item = enumObj.Experimental
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SandboxedInstanceMode.Experimental" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveAvatarThumbnailCustomizationFailure
    if not enumObj then
        return false
    end
    local item = enumObj.BadThumbnailType
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.SaveAvatarThumbnailCustomizationFailure.BadThumbnailType"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveAvatarThumbnailCustomizationFailure
    if not enumObj then
        return false
    end
    local item = enumObj.BadYRotDeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.SaveAvatarThumbnailCustomizationFailure.BadYRotDeg"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveAvatarThumbnailCustomizationFailure
    if not enumObj then
        return false
    end
    local item = enumObj.BadFieldOfViewDeg
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.SaveAvatarThumbnailCustomizationFailure.BadFieldOfViewDeg"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveAvatarThumbnailCustomizationFailure
    if not enumObj then
        return false
    end
    local item = enumObj.BadDistanceScale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.SaveAvatarThumbnailCustomizationFailure.BadDistanceScale"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveAvatarThumbnailCustomizationFailure
    if not enumObj then
        return false
    end
    local item = enumObj.Other
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.SaveAvatarThumbnailCustomizationFailure.Other"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveAvatarThumbnailCustomizationFailure
    if not enumObj then
        return false
    end
    local item = enumObj.Throttled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.SaveAvatarThumbnailCustomizationFailure.Throttled"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveFilter
    if not enumObj then
        return false
    end
    local item = enumObj.SaveWorld
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SaveFilter.SaveWorld" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveFilter
    if not enumObj then
        return false
    end
    local item = enumObj.SaveGame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SaveFilter.SaveGame" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SaveFilter
    if not enumObj then
        return false
    end
    local item = enumObj.SaveAll
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SaveFilter.SaveAll" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel6
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel6" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel7
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel7" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel9
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel9" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SavedQualitySetting
    if not enumObj then
        return false
    end
    local item = enumObj.QualityLevel10
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SavedQualitySetting.QualityLevel10" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScaleType
    if not enumObj then
        return false
    end
    local item = enumObj.Stretch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScaleType.Stretch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScaleType
    if not enumObj then
        return false
    end
    local item = enumObj.Slice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScaleType.Slice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScaleType
    if not enumObj then
        return false
    end
    local item = enumObj.Tile
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScaleType.Tile" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScaleType
    if not enumObj then
        return false
    end
    local item = enumObj.Fit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScaleType.Fit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScaleType
    if not enumObj then
        return false
    end
    local item = enumObj.Crop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScaleType.Crop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.ConsentAccepted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.ConsentAccepted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidScopes
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.InvalidScopes" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.Timeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.Timeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoUserInput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.NoUserInput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.BackendError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.BackendError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.UnexpectedError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.UnexpectedError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidArgument
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.InvalidArgument" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScopeCheckResult
    if not enumObj then
        return false
    end
    local item = enumObj.ConsentDenied
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScopeCheckResult.ConsentDenied" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenInsets
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenInsets.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenInsets
    if not enumObj then
        return false
    end
    local item = enumObj.DeviceSafeInsets
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenInsets.DeviceSafeInsets" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenInsets
    if not enumObj then
        return false
    end
    local item = enumObj.CoreUISafeInsets
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenInsets.CoreUISafeInsets" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenInsets
    if not enumObj then
        return false
    end
    local item = enumObj.TopbarSafeInsets
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenInsets.TopbarSafeInsets" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.LandscapeLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenOrientation.LandscapeLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.LandscapeRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenOrientation.LandscapeRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.LandscapeSensor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenOrientation.LandscapeSensor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.Portrait
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenOrientation.Portrait" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenOrientation
    if not enumObj then
        return false
    end
    local item = enumObj.Sensor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenOrientation.Sensor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenshotCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenshotCaptureResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenshotCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.OtherError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenshotCaptureResult.OtherError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenshotCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoDeviceSupport
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenshotCaptureResult.NoDeviceSupport" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScreenshotCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoSpaceOnDevice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScreenshotCaptureResult.NoSpaceOnDevice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptStoppedReason
    if not enumObj then
        return false
    end
    local item = enumObj.Breakpoint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptStoppedReason.Breakpoint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptStoppedReason
    if not enumObj then
        return false
    end
    local item = enumObj.Exception
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptStoppedReason.Exception" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptStoppedReason
    if not enumObj then
        return false
    end
    local item = enumObj.Pause
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptStoppedReason.Pause" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptStoppedReason
    if not enumObj then
        return false
    end
    local item = enumObj.Step
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptStoppedReason.Step" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptStoppedReason
    if not enumObj then
        return false
    end
    local item = enumObj.Entry
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptStoppedReason.Entry" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptVariableScope
    if not enumObj then
        return false
    end
    local item = enumObj.Local
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptVariableScope.Local" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptVariableScope
    if not enumObj then
        return false
    end
    local item = enumObj.Upvalue
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptVariableScope.Upvalue" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScriptVariableScope
    if not enumObj then
        return false
    end
    local item = enumObj.Global
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScriptVariableScope.Global" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScrollBarInset
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScrollBarInset.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScrollBarInset
    if not enumObj then
        return false
    end
    local item = enumObj.ScrollBar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScrollBarInset.ScrollBar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScrollBarInset
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScrollBarInset.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScrollingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.X
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScrollingDirection.X" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScrollingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Y
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScrollingDirection.Y" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ScrollingDirection
    if not enumObj then
        return false
    end
    local item = enumObj.XY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ScrollingDirection.XY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.RunClientScript
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.RunClientScript" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.RunServerScript
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.RunServerScript" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.AccessOutsideWrite
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.AccessOutsideWrite" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.AssetRequire
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.AssetRequire" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.LoadString
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.LoadString" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptGlobals
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.ScriptGlobals" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.CreateInstances
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.CreateInstances" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Basic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Basic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Audio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Audio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.DataStore
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.DataStore" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Network
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Network" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Physics
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Physics" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.UI
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.UI" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.CSG
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.CSG" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Chat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Chat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Animation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Animation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Avatar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Avatar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Input
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Input" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Environment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Environment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.RemoteEvent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.RemoteEvent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.LegacySound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.LegacySound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Players
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Players" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.CapabilityControl
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.CapabilityControl" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Plugin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Plugin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.LocalUser
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.LocalUser" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.WritePlayer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.WritePlayer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxScript
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.RobloxScript" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxEngine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.RobloxEngine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Unassigned
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Unassigned" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.InternalTest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.InternalTest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.PluginOrOpenCloud
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.PluginOrOpenCloud" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Assistant
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Assistant" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.RemoteCommand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.RemoteCommand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.AssetRead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.AssetRead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.AssetManagement
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.AssetManagement" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicGeneration
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.DynamicGeneration" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.PlatformAvatarEditing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.PlatformAvatarEditing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.AssetCreateUpdate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.AssetCreateUpdate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Capture
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Capture" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.SensitiveInput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.SensitiveInput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Monetization
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Monetization" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.LoadOwnedAsset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.LoadOwnedAsset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Social
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Social" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.ServerCommunication
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.ServerCommunication" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Logging
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Logging" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.PromptExternalPurchase
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.PromptExternalPurchase" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Groups
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Groups" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Teleport
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Teleport" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Consequences
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Consequences" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.Material
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.Material" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarBehavior
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.AvatarBehavior" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarAppearance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.AvatarAppearance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SecurityCapability
    if not enumObj then
        return false
    end
    local item = enumObj.LoadUnownedAsset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SecurityCapability.LoadUnownedAsset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelectionBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Escape
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelectionBehavior.Escape" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelectionBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Stop
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelectionBehavior.Stop" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelectionRenderMode
    if not enumObj then
        return false
    end
    local item = enumObj.Outlines
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelectionRenderMode.Outlines" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelectionRenderMode
    if not enumObj then
        return false
    end
    local item = enumObj.BoundingBoxes
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelectionRenderMode.BoundingBoxes" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelectionRenderMode
    if not enumObj then
        return false
    end
    local item = enumObj.Both
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelectionRenderMode.Both" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelfViewPosition
    if not enumObj then
        return false
    end
    local item = enumObj.LastPosition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelfViewPosition.LastPosition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelfViewPosition
    if not enumObj then
        return false
    end
    local item = enumObj.TopLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelfViewPosition.TopLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelfViewPosition
    if not enumObj then
        return false
    end
    local item = enumObj.TopRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelfViewPosition.TopRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelfViewPosition
    if not enumObj then
        return false
    end
    local item = enumObj.BottomLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelfViewPosition.BottomLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SelfViewPosition
    if not enumObj then
        return false
    end
    local item = enumObj.BottomRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SelfViewPosition.BottomRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SensorMode
    if not enumObj then
        return false
    end
    local item = enumObj.Floor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SensorMode.Floor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SensorMode
    if not enumObj then
        return false
    end
    local item = enumObj.Ladder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SensorMode.Ladder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SensorMode
    if not enumObj then
        return false
    end
    local item = enumObj.ClassicFloor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SensorMode.ClassicFloor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SensorMode
    if not enumObj then
        return false
    end
    local item = enumObj.ClassicLadder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SensorMode.ClassicLadder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SensorUpdateType
    if not enumObj then
        return false
    end
    local item = enumObj.OnRead
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SensorUpdateType.OnRead" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SensorUpdateType
    if not enumObj then
        return false
    end
    local item = enumObj.Manual
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SensorUpdateType.Manual" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ServerLiveEditingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Uninitialized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ServerLiveEditingMode.Uninitialized" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ServerLiveEditingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ServerLiveEditingMode.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ServerLiveEditingMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ServerLiveEditingMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ServiceVisibility
    if not enumObj then
        return false
    end
    local item = enumObj.Always
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ServiceVisibility.Always" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ServiceVisibility
    if not enumObj then
        return false
    end
    local item = enumObj.Off
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ServiceVisibility.Off" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ServiceVisibility
    if not enumObj then
        return false
    end
    local item = enumObj.WithChildren
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ServiceVisibility.WithChildren" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Severity
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Severity.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Severity
    if not enumObj then
        return false
    end
    local item = enumObj.Warning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Severity.Warning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Severity
    if not enumObj then
        return false
    end
    local item = enumObj.Information
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Severity.Information" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Severity
    if not enumObj then
        return false
    end
    local item = enumObj.Hint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Severity.Hint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ShowAdResult
    if not enumObj then
        return false
    end
    local item = enumObj.ShowCompleted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ShowAdResult.ShowCompleted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ShowAdResult
    if not enumObj then
        return false
    end
    local item = enumObj.AdNotReady
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ShowAdResult.AdNotReady" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ShowAdResult
    if not enumObj then
        return false
    end
    local item = enumObj.AdAlreadyShowing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ShowAdResult.AdAlreadyShowing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ShowAdResult
    if not enumObj then
        return false
    end
    local item = enumObj.InternalError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ShowAdResult.InternalError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ShowAdResult
    if not enumObj then
        return false
    end
    local item = enumObj.ShowInterrupted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ShowAdResult.ShowInterrupted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ShowAdResult
    if not enumObj then
        return false
    end
    local item = enumObj.InsufficientMemory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ShowAdResult.InsufficientMemory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SignalBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SignalBehavior.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SignalBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Immediate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SignalBehavior.Immediate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SignalBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Deferred
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SignalBehavior.Deferred" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SignalBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.AncestryDeferred
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SignalBehavior.AncestryDeferred" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SizeConstraint
    if not enumObj then
        return false
    end
    local item = enumObj.RelativeXY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SizeConstraint.RelativeXY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SizeConstraint
    if not enumObj then
        return false
    end
    local item = enumObj.RelativeXX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SizeConstraint.RelativeXX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SizeConstraint
    if not enumObj then
        return false
    end
    local item = enumObj.RelativeYY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SizeConstraint.RelativeYY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SlimTintMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SlimTintMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SlimTintMode
    if not enumObj then
        return false
    end
    local item = enumObj.LOD
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SlimTintMode.LOD" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SlimTintMode
    if not enumObj then
        return false
    end
    local item = enumObj.Meshes
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SlimTintMode.Meshes" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SlimTintMode
    if not enumObj then
        return false
    end
    local item = enumObj.MeshResourcePtr
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SlimTintMode.MeshResourcePtr" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SlimTintMode
    if not enumObj then
        return false
    end
    local item = enumObj.ContentId
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SlimTintMode.ContentId" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SlimTintMode
    if not enumObj then
        return false
    end
    local item = enumObj.Zone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SlimTintMode.Zone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolidPrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.Capsule
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SolidPrimitiveType.Capsule" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolidPrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.Cone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SolidPrimitiveType.Cone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolidPrimitiveType
    if not enumObj then
        return false
    end
    local item = enumObj.RoundedBox
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SolidPrimitiveType.RoundedBox" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolverConvergenceMetricType
    if not enumObj then
        return false
    end
    local item = enumObj.IterationBased
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SolverConvergenceMetricType.IterationBased" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolverConvergenceMetricType
    if not enumObj then
        return false
    end
    local item = enumObj.AlgorithmAgnostic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.SolverConvergenceMetricType.AlgorithmAgnostic"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolverConvergenceVisualizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SolverConvergenceVisualizationMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolverConvergenceVisualizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.PerIsland
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.SolverConvergenceVisualizationMode.PerIsland"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SolverConvergenceVisualizationMode
    if not enumObj then
        return false
    end
    local item = enumObj.PerEdge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SolverConvergenceVisualizationMode.PerEdge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SortDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Ascending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SortDirection.Ascending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SortDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Descending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SortDirection.Descending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SortOrder
    if not enumObj then
        return false
    end
    local item = enumObj.Name
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SortOrder.Name" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SortOrder
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SortOrder.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SortOrder
    if not enumObj then
        return false
    end
    local item = enumObj.LayoutOrder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SortOrder.LayoutOrder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SpecialKey
    if not enumObj then
        return false
    end
    local item = enumObj.Insert
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SpecialKey.Insert" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SpecialKey
    if not enumObj then
        return false
    end
    local item = enumObj.Home
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SpecialKey.Home" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SpecialKey
    if not enumObj then
        return false
    end
    local item = enumObj.End
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SpecialKey.End" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SpecialKey
    if not enumObj then
        return false
    end
    local item = enumObj.PageUp
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SpecialKey.PageUp" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SpecialKey
    if not enumObj then
        return false
    end
    local item = enumObj.PageDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SpecialKey.PageDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SpecialKey
    if not enumObj then
        return false
    end
    local item = enumObj.ChatHotkey
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SpecialKey.ChatHotkey" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StartCorner
    if not enumObj then
        return false
    end
    local item = enumObj.TopLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StartCorner.TopLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StartCorner
    if not enumObj then
        return false
    end
    local item = enumObj.TopRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StartCorner.TopRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StartCorner
    if not enumObj then
        return false
    end
    local item = enumObj.BottomLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StartCorner.BottomLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StartCorner
    if not enumObj then
        return false
    end
    local item = enumObj.BottomRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StartCorner.BottomRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.Boolean
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.Boolean" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.CFrame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.CFrame" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.Color3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.Color3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.Float
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.Float" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.Instance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.Instance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.Random
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.Random" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.Vector2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.Vector2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.Vector3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.Vector3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StateObjectFieldType
    if not enumObj then
        return false
    end
    local item = enumObj.INVALID
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StateObjectFieldType.INVALID" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Status
    if not enumObj then
        return false
    end
    local item = enumObj.Poison
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Status.Poison" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Status
    if not enumObj then
        return false
    end
    local item = enumObj.Confusion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Status.Confusion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StepFrequency
    if not enumObj then
        return false
    end
    local item = enumObj.Hz60
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StepFrequency.Hz60" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StepFrequency
    if not enumObj then
        return false
    end
    local item = enumObj.Hz30
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StepFrequency.Hz30" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StepFrequency
    if not enumObj then
        return false
    end
    local item = enumObj.Hz15
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StepFrequency.Hz15" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StepFrequency
    if not enumObj then
        return false
    end
    local item = enumObj.Hz10
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StepFrequency.Hz10" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StepFrequency
    if not enumObj then
        return false
    end
    local item = enumObj.Hz5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StepFrequency.Hz5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StepFrequency
    if not enumObj then
        return false
    end
    local item = enumObj.Hz1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StepFrequency.Hz1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamOutBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamOutBehavior.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamOutBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.LowMemory
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamOutBehavior.LowMemory" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamOutBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Opportunistic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamOutBehavior.Opportunistic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamingIntegrityMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamingIntegrityMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamingIntegrityMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamingIntegrityMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamingIntegrityMode
    if not enumObj then
        return false
    end
    local item = enumObj.MinimumRadiusPause
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamingIntegrityMode.MinimumRadiusPause" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamingIntegrityMode
    if not enumObj then
        return false
    end
    local item = enumObj.PauseOutsideLoadedArea
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StreamingIntegrityMode.PauseOutsideLoadedArea"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamingPauseMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamingPauseMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamingPauseMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamingPauseMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StreamingPauseMode
    if not enumObj then
        return false
    end
    local item = enumObj.ClientPhysicsPause
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StreamingPauseMode.ClientPhysicsPause" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StrokeSizingMode
    if not enumObj then
        return false
    end
    local item = enumObj.FixedSize
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StrokeSizingMode.FixedSize" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StrokeSizingMode
    if not enumObj then
        return false
    end
    local item = enumObj.ScaledSize
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StrokeSizingMode.ScaledSize" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioAction
    if not enumObj then
        return false
    end
    local item = enumObj.Undo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioAction.Undo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioAction
    if not enumObj then
        return false
    end
    local item = enumObj.Redo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioAction.Redo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioAction
    if not enumObj then
        return false
    end
    local item = enumObj.SelectAll
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioAction.SelectAll" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioAction
    if not enumObj then
        return false
    end
    local item = enumObj.DeleteSelected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioAction.DeleteSelected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioAction
    if not enumObj then
        return false
    end
    local item = enumObj.Copy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioAction.Copy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioAction
    if not enumObj then
        return false
    end
    local item = enumObj.Cut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioAction.Cut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioAction
    if not enumObj then
        return false
    end
    local item = enumObj.Paste
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioAction.Paste" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCaptureBufferStatus
    if not enumObj then
        return false
    end
    local item = enumObj.NotStarted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCaptureBufferStatus.NotStarted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCaptureBufferStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Pending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCaptureBufferStatus.Pending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCaptureBufferStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Ready
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCaptureBufferStatus.Ready" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCaptureBufferStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCaptureBufferStatus.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCaptureScreenshotFormat
    if not enumObj then
        return false
    end
    local item = enumObj.RGBA8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCaptureScreenshotFormat.RGBA8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCaptureScreenshotFormat
    if not enumObj then
        return false
    end
    local item = enumObj.PNG
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCaptureScreenshotFormat.PNG" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCloseMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCloseMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCloseMode
    if not enumObj then
        return false
    end
    local item = enumObj.CloseStudio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCloseMode.CloseStudio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCloseMode
    if not enumObj then
        return false
    end
    local item = enumObj.CloseDoc
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCloseMode.CloseDoc" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioCloseMode
    if not enumObj then
        return false
    end
    local item = enumObj.LogOut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioCloseMode.LogOut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioDataModelType
    if not enumObj then
        return false
    end
    local item = enumObj.Edit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioDataModelType.Edit" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioDataModelType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayClient
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioDataModelType.PlayClient" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioDataModelType
    if not enumObj then
        return false
    end
    local item = enumObj.PlayServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioDataModelType.PlayServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioDataModelType
    if not enumObj then
        return false
    end
    local item = enumObj.Standalone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioDataModelType.Standalone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioDataModelType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioDataModelType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioPlaceUpdateFailureReason
    if not enumObj then
        return false
    end
    local item = enumObj.Other
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioPlaceUpdateFailureReason.Other" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioPlaceUpdateFailureReason
    if not enumObj then
        return false
    end
    local item = enumObj.TeamCreateConflict
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioPlaceUpdateFailureReason.TeamCreateConflict"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Operator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Operator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Number
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Number" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.String
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.String" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Comment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Comment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Keyword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Keyword" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Builtin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Builtin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Method
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Method" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Property
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Property" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Nil
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Nil" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Bool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Bool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Function
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Function" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Local
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Local" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Self
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Self" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.LuauKeyword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorCategories.LuauKeyword"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.FunctionName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorCategories.FunctionName"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.TODO
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.TODO" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Background
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Background"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.SelectionText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorCategories.SelectionText"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.SelectionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.SelectionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.FindSelectionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.FindSelectionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MatchingWordBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MatchingWordBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Warning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Warning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Info
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Info" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Hint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Hint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Whitespace
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Whitespace"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.ActiveLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorCategories.ActiveLine"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.DebuggerCurrentLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.DebuggerCurrentLine"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.DebuggerErrorLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.DebuggerErrorLine"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Ruler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Ruler" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Bracket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Bracket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.Type
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorCategories.Type" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuPrimaryText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MenuPrimaryText"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuSecondaryText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MenuSecondaryText"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuSelectedText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MenuSelectedText"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MenuBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuSelectedBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MenuSelectedBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuScrollbarBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MenuScrollbarBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuScrollbarHandle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.MenuScrollbarHandle"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.MenuBorder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorCategories.MenuBorder"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.DocViewCodeBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.DocViewCodeBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.AICOOverlayText"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayButtonBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.AICOOverlayButtonBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayButtonBackgroundHover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.AICOOverlayButtonBackgroundHover"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayButtonBackgroundPressed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.AICOOverlayButtonBackgroundPressed"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorCategories
    if not enumObj then
        return false
    end
    local item = enumObj.IndentationRuler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioScriptEditorColorCategories.IndentationRuler"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorPresets
    if not enumObj then
        return false
    end
    local item = enumObj.RobloxDefault
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioScriptEditorColorPresets.RobloxDefault"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorPresets
    if not enumObj then
        return false
    end
    local item = enumObj.Extra1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorPresets.Extra1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorPresets
    if not enumObj then
        return false
    end
    local item = enumObj.Extra2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorPresets.Extra2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioScriptEditorColorPresets
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioScriptEditorColorPresets.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.MainBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.MainBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Titlebar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Titlebar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Dropdown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Dropdown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Tooltip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Tooltip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Notification
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Notification" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScrollBar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScrollBar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScrollBarBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScrollBarBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.TabBar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.TabBar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Tab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Tab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.FilterButtonDefault
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.FilterButtonDefault" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.FilterButtonHover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.FilterButtonHover" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.FilterButtonChecked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.FilterButtonChecked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.FilterButtonAccent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.FilterButtonAccent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.FilterButtonBorder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.FilterButtonBorder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.FilterButtonBorderAlt
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.FilterButtonBorderAlt" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.RibbonTab
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.RibbonTab" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.RibbonTabTopBar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.RibbonTabTopBar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Button
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Button" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.MainButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.MainButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.RibbonButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.RibbonButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ViewPortBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ViewPortBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.InputFieldBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.InputFieldBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Item
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Item" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.TableItem
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.TableItem" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.CategoryItem
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.CategoryItem" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.GameSettingsTableItem
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.GameSettingsTableItem" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.GameSettingsTooltip
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.GameSettingsTooltip" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.EmulatorBar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.EmulatorBar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.EmulatorDropDown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.EmulatorDropDown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ColorPickerFrame
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ColorPickerFrame" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.CurrentMarker
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.CurrentMarker" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Border
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Border" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DropShadow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DropShadow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Shadow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Shadow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Light
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Light" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Dark
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Dark" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Mid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Mid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.MainText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.MainText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.SubText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.SubText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.TitlebarText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.TitlebarText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.BrightText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.BrightText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DimmedText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DimmedText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.LinkText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.LinkText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.WarningText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.WarningText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ErrorText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ErrorText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.InfoText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.InfoText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.SensitiveText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.SensitiveText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptSideWidget
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptSideWidget" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptSelectionText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptSelectionText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptSelectionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptSelectionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptFindSelectionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.ScriptFindSelectionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptMatchingWordSelectionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.ScriptMatchingWordSelectionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptOperator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptOperator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptNumber
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptNumber" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptString
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptString" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptComment
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptComment" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptKeyword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptKeyword" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptBuiltInFunction
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptBuiltInFunction" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptWarning
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptWarning" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptInformation
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptInformation" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptHint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptHint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptWhitespace
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptWhitespace" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptRuler
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptRuler" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DocViewCodeBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DocViewCodeBackground" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DebuggerCurrentLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DebuggerCurrentLine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DebuggerErrorLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DebuggerErrorLine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffFilePathText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffFilePathText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextHunkInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffTextHunkInfo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextNoChange
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffTextNoChange" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextAddition
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffTextAddition" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextDeletion
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffTextDeletion" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextSeparatorBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffTextSeparatorBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextNoChangeBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffTextNoChangeBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextAdditionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffTextAdditionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffTextDeletionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffTextDeletionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffLineNum
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffLineNum" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffLineNumSeparatorBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffLineNumSeparatorBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffLineNumNoChangeBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffLineNumNoChangeBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffLineNumAdditionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffLineNumAdditionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffLineNumDeletionBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffLineNumDeletionBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffFilePathBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioStyleGuideColor.DiffFilePathBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffFilePathBorder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffFilePathBorder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ChatIncomingBgColor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ChatIncomingBgColor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ChatIncomingTextColor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ChatIncomingTextColor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ChatOutgoingBgColor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ChatOutgoingBgColor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ChatOutgoingTextColor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ChatOutgoingTextColor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ChatModeratedMessageColor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioStyleGuideColor.ChatModeratedMessageColor"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Separator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Separator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonBorder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ButtonBorder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ButtonText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ButtonText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.InputFieldBorder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.InputFieldBorder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.CheckedFieldBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioStyleGuideColor.CheckedFieldBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.CheckedFieldBorder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.CheckedFieldBorder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.CheckedFieldIndicator
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.CheckedFieldIndicator" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.HeaderSection
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.HeaderSection" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.Midlight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.Midlight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.StatusBar
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.StatusBar" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DialogButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DialogButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DialogButtonText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DialogButtonText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DialogButtonBorder
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DialogButtonBorder" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DialogMainButton
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DialogMainButton" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DialogMainButtonText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DialogMainButtonText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.InfoBarWarningBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioStyleGuideColor.InfoBarWarningBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.InfoBarWarningText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.InfoBarWarningText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptEditorCurrentLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptEditorCurrentLine"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptMethod
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptMethod" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptProperty
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptProperty" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptNil
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptNil" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptBool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptBool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptFunction
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptFunction" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptLocal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptLocal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptSelf
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptSelf" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptLuauKeyword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptLuauKeyword" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptFunctionName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptFunctionName" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptTodo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptTodo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.ScriptBracket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.ScriptBracket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.AttributeCog
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.AttributeCog" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayText
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.AICOOverlayText" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayButtonBackground
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.AICOOverlayButtonBackground"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayButtonBackgroundHover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.AICOOverlayButtonBackgroundHover"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.AICOOverlayButtonBackgroundPressed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.AICOOverlayButtonBackgroundPressed"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.OnboardingCover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.OnboardingCover" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.OnboardingHighlight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.OnboardingHighlight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.OnboardingShadow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.OnboardingShadow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.BreakpointMarker
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.BreakpointMarker" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffLineNumHover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideColor.DiffLineNumHover" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideColor
    if not enumObj then
        return false
    end
    local item = enumObj.DiffLineNumSeparatorBackgroundHover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.StudioStyleGuideColor.DiffLineNumSeparatorBackgroundHover"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideModifier
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideModifier.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideModifier
    if not enumObj then
        return false
    end
    local item = enumObj.Selected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideModifier.Selected" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideModifier
    if not enumObj then
        return false
    end
    local item = enumObj.Pressed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideModifier.Pressed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideModifier
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideModifier.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.StudioStyleGuideModifier
    if not enumObj then
        return false
    end
    local item = enumObj.Hover
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.StudioStyleGuideModifier.Hover" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Style
    if not enumObj then
        return false
    end
    local item = enumObj.AlternatingSupports
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Style.AlternatingSupports" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Style
    if not enumObj then
        return false
    end
    local item = enumObj.BridgeStyleSupports
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Style.BridgeStyleSupports" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Style
    if not enumObj then
        return false
    end
    local item = enumObj.NoSupports
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Style.NoSupports" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionExpirationReason
    if not enumObj then
        return false
    end
    local item = enumObj.ProductInactive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.SubscriptionExpirationReason.ProductInactive"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionExpirationReason
    if not enumObj then
        return false
    end
    local item = enumObj.ProductDeleted
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionExpirationReason.ProductDeleted" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionExpirationReason
    if not enumObj then
        return false
    end
    local item = enumObj.SubscriberCancelled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.SubscriptionExpirationReason.SubscriberCancelled"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionExpirationReason
    if not enumObj then
        return false
    end
    local item = enumObj.SubscriberRefunded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.SubscriptionExpirationReason.SubscriberRefunded"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionExpirationReason
    if not enumObj then
        return false
    end
    local item = enumObj.Lapsed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionExpirationReason.Lapsed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionPaymentStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Paid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionPaymentStatus.Paid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionPaymentStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Refunded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionPaymentStatus.Refunded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionPeriod
    if not enumObj then
        return false
    end
    local item = enumObj.Month
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionPeriod.Month" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionState
    if not enumObj then
        return false
    end
    local item = enumObj.NeverSubscribed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionState.NeverSubscribed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionState
    if not enumObj then
        return false
    end
    local item = enumObj.SubscribedWillRenew
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionState.SubscribedWillRenew" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionState
    if not enumObj then
        return false
    end
    local item = enumObj.SubscribedWillNotRenew
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionState.SubscribedWillNotRenew" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionState
    if not enumObj then
        return false
    end
    local item = enumObj.SubscribedRenewalPaymentPending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.SubscriptionState.SubscribedRenewalPaymentPending"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SubscriptionState
    if not enumObj then
        return false
    end
    local item = enumObj.Expired
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SubscriptionState.Expired" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceConstraint
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceConstraint.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceConstraint
    if not enumObj then
        return false
    end
    local item = enumObj.Hinge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceConstraint.Hinge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceConstraint
    if not enumObj then
        return false
    end
    local item = enumObj.SteppingMotor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceConstraint.SteppingMotor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceConstraint
    if not enumObj then
        return false
    end
    local item = enumObj.Motor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceConstraint.Motor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceGuiShape
    if not enumObj then
        return false
    end
    local item = enumObj.Flat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceGuiShape.Flat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceGuiShape
    if not enumObj then
        return false
    end
    local item = enumObj.CurvedHorizontally
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceGuiShape.CurvedHorizontally" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceGuiSizingMode
    if not enumObj then
        return false
    end
    local item = enumObj.FixedSize
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceGuiSizingMode.FixedSize" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceGuiSizingMode
    if not enumObj then
        return false
    end
    local item = enumObj.PixelsPerStud
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceGuiSizingMode.PixelsPerStud" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Smooth
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Smooth" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Glue
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Glue" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Weld
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Weld" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Studs
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Studs" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Inlet
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Inlet" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Universal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Universal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Hinge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Hinge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.Motor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.Motor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.SteppingMotor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.SteppingMotor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SurfaceType
    if not enumObj then
        return false
    end
    local item = enumObj.SmoothNoOutlines
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SurfaceType.SmoothNoOutlines" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SwipeDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SwipeDirection.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SwipeDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SwipeDirection.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SwipeDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Up
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SwipeDirection.Up" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SwipeDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Down
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SwipeDirection.Down" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SwipeDirection
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SwipeDirection.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SystemThemeValue
    if not enumObj then
        return false
    end
    local item = enumObj.error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SystemThemeValue.error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SystemThemeValue
    if not enumObj then
        return false
    end
    local item = enumObj.light
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SystemThemeValue.light" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SystemThemeValue
    if not enumObj then
        return false
    end
    local item = enumObj.dark
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SystemThemeValue.dark" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SystemThemeValue
    if not enumObj then
        return false
    end
    local item = enumObj.systemLight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SystemThemeValue.systemLight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.SystemThemeValue
    if not enumObj then
        return false
    end
    local item = enumObj.systemDark
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.SystemThemeValue.systemDark" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TableMajorAxis
    if not enumObj then
        return false
    end
    local item = enumObj.RowMajor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TableMajorAxis.RowMajor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TableMajorAxis
    if not enumObj then
        return false
    end
    local item = enumObj.ColumnMajor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TableMajorAxis.ColumnMajor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeamCreateErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.PlaceSizeTooLarge
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeamCreateErrorState.PlaceSizeTooLarge" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeamCreateErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.PlaceSizeApproachingLimit
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.TeamCreateErrorState.PlaceSizeApproachingLimit"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeamCreateErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.PlaceUploadFailing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeamCreateErrorState.PlaceUploadFailing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeamCreateErrorState
    if not enumObj then
        return false
    end
    local item = enumObj.NoError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeamCreateErrorState.NoError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Technology
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Technology.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Technology
    if not enumObj then
        return false
    end
    local item = enumObj.Voxel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Technology.Voxel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Technology
    if not enumObj then
        return false
    end
    local item = enumObj.Compatibility
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Technology.Compatibility" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Technology
    if not enumObj then
        return false
    end
    local item = enumObj.ShadowMap
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Technology.ShadowMap" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Technology
    if not enumObj then
        return false
    end
    local item = enumObj.Future
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Technology.Future" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.Technology
    if not enumObj then
        return false
    end
    local item = enumObj.Unified
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.Technology.Unified" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.UNSPECIFIED
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.UNSPECIFIED" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.EventIngest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.EventIngest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.Points
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.Points" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.Teletune
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.Teletune" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.EphemeralCounter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.EphemeralCounter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.EphemeralStat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.EphemeralStat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.Counter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.Counter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryBackend
    if not enumObj then
        return false
    end
    local item = enumObj.Stat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryBackend.Stat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddDatacenterId
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddDatacenterId" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddPlaceId
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddPlaceId" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddUniverseId
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddUniverseId" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddPlaceInstanceId
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.TelemetryStandardizedField.AddPlaceInstanceId"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddPlaySessionId
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddPlaySessionId" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddCurrentContextName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TelemetryStandardizedField.AddCurrentContextName"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddOsInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddOsInfo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddArchitectureInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.TelemetryStandardizedField.AddArchitectureInfo"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddCpuInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddCpuInfo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddMemoryInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddMemoryInfo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TelemetryStandardizedField
    if not enumObj then
        return false
    end
    local item = enumObj.AddSessionInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TelemetryStandardizedField.AddSessionInfo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportToSpawnByName
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportMethod.TeleportToSpawnByName" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportToPlaceInstance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportMethod.TeleportToPlaceInstance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportToPrivateServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportMethod.TeleportToPrivateServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportPartyAsync
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportMethod.TeleportPartyAsync" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportToVIPServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportMethod.TeleportToVIPServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportToInstanceBack
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportMethod.TeleportToInstanceBack" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportMethod
    if not enumObj then
        return false
    end
    local item = enumObj.TeleportUnknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportMethod.TeleportUnknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.Failure
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.Failure" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.GameNotFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.GameNotFound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.GameEnded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.GameEnded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.GameFull
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.GameFull" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.Unauthorized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.Unauthorized" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.Flooded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.Flooded" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportResult
    if not enumObj then
        return false
    end
    local item = enumObj.IsTeleporting
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportResult.IsTeleporting" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportState
    if not enumObj then
        return false
    end
    local item = enumObj.RequestedFromServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportState.RequestedFromServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportState
    if not enumObj then
        return false
    end
    local item = enumObj.Started
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportState.Started" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportState
    if not enumObj then
        return false
    end
    local item = enumObj.WaitingForServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportState.WaitingForServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportState
    if not enumObj then
        return false
    end
    local item = enumObj.Failed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportState.Failed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportState
    if not enumObj then
        return false
    end
    local item = enumObj.InProgress
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportState.InProgress" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportType
    if not enumObj then
        return false
    end
    local item = enumObj.ToPlace
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportType.ToPlace" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportType
    if not enumObj then
        return false
    end
    local item = enumObj.ToInstance
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportType.ToInstance" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportType
    if not enumObj then
        return false
    end
    local item = enumObj.ToReservedServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportType.ToReservedServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportType
    if not enumObj then
        return false
    end
    local item = enumObj.ToVIPServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportType.ToVIPServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TeleportType
    if not enumObj then
        return false
    end
    local item = enumObj.ToInstanceBack
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TeleportType.ToInstanceBack" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Template
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.Template" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Generate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.Generate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Import
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.Import" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Convert
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.Convert" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.EditAddTool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.EditAddTool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.EditSeaLevelTool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.EditSeaLevelTool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.EditReplaceTool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.EditReplaceTool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.RegionFillTool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.RegionFillTool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.RegionPasteTool
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.RegionPasteTool" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainAcquisitionMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Other
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainAcquisitionMethod.Other" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainFace
    if not enumObj then
        return false
    end
    local item = enumObj.Top
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainFace.Top" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainFace
    if not enumObj then
        return false
    end
    local item = enumObj.Side
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainFace.Side" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainFace
    if not enumObj then
        return false
    end
    local item = enumObj.Bottom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainFace.Bottom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainLiquidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainLiquidMergeOperation.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainLiquidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Source
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainLiquidMergeOperation.Source" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainLiquidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Union
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainLiquidMergeOperation.Union" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainLiquidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Difference
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainLiquidMergeOperation.Difference" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainLiquidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Intersect
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainLiquidMergeOperation.Intersect" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Paint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Paint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Source
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Source" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Union
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Union" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Dig
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Dig" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Difference
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Difference" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Intersect
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Intersect" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Cut
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Cut" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TerrainSolidMergeOperation
    if not enumObj then
        return false
    end
    local item = enumObj.Place
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TerrainSolidMergeOperation.Place" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextChatMessageStatus.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextChatMessageStatus.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Sending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextChatMessageStatus.Sending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.TextFilterFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextChatMessageStatus.TextFilterFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Floodchecked
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextChatMessageStatus.Floodchecked" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidPrivacySettings
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.TextChatMessageStatus.InvalidPrivacySettings"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.InvalidTextChannelPermissions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TextChatMessageStatus.InvalidTextChannelPermissions"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.MessageTooLong
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextChatMessageStatus.MessageTooLong" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextChatMessageStatus
    if not enumObj then
        return false
    end
    local item = enumObj.ModerationTimeout
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextChatMessageStatus.ModerationTimeout" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Auto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextDirection.Auto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextDirection
    if not enumObj then
        return false
    end
    local item = enumObj.LeftToRight
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextDirection.LeftToRight" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextDirection
    if not enumObj then
        return false
    end
    local item = enumObj.RightToLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextDirection.RightToLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextFilterContext
    if not enumObj then
        return false
    end
    local item = enumObj.PublicChat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextFilterContext.PublicChat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextFilterContext
    if not enumObj then
        return false
    end
    local item = enumObj.PrivateChat
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextFilterContext.PrivateChat" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.NoSuggestions
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.NoSuggestions" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Number
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.Number" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Email
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.Email" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Phone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.Phone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Password
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.Password" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.PasswordShown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.PasswordShown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Username
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.Username" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.OneTimePassword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.OneTimePassword" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.NewPassword
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.NewPassword" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextInputType
    if not enumObj then
        return false
    end
    local item = enumObj.NewPasswordShown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextInputType.NewPasswordShown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextTruncate
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextTruncate.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextTruncate
    if not enumObj then
        return false
    end
    local item = enumObj.AtEnd
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextTruncate.AtEnd" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextTruncate
    if not enumObj then
        return false
    end
    local item = enumObj.SplitWord
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextTruncate.SplitWord" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextXAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextXAlignment.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextXAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextXAlignment.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextXAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextXAlignment.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextYAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Top
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextYAlignment.Top" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextYAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextYAlignment.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextYAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Bottom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextYAlignment.Bottom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextureMode
    if not enumObj then
        return false
    end
    local item = enumObj.Stretch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextureMode.Stretch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextureMode
    if not enumObj then
        return false
    end
    local item = enumObj.Wrap
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextureMode.Wrap" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextureMode
    if not enumObj then
        return false
    end
    local item = enumObj.Static
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextureMode.Static" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextureQueryType
    if not enumObj then
        return false
    end
    local item = enumObj.NonHumanoid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextureQueryType.NonHumanoid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextureQueryType
    if not enumObj then
        return false
    end
    local item = enumObj.NonHumanoidOrphaned
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextureQueryType.NonHumanoidOrphaned" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextureQueryType
    if not enumObj then
        return false
    end
    local item = enumObj.Humanoid
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextureQueryType.Humanoid" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TextureQueryType
    if not enumObj then
        return false
    end
    local item = enumObj.HumanoidOrphaned
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TextureQueryType.HumanoidOrphaned" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.Auto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.Auto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.Threads1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.Threads1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.Threads2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.Threads2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.Threads3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.Threads3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.Threads4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.Threads4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.Threads8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.Threads8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.Threads16
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.Threads16" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.PerCore1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.PerCore1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.PerCore2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.PerCore2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.PerCore3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.PerCore3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThreadPoolConfig
    if not enumObj then
        return false
    end
    local item = enumObj.PerCore4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThreadPoolConfig.PerCore4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThrottlingPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThrottlingPriority.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThrottlingPriority
    if not enumObj then
        return false
    end
    local item = enumObj.ElevatedOnServer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThrottlingPriority.ElevatedOnServer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThrottlingPriority
    if not enumObj then
        return false
    end
    local item = enumObj.Extreme
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThrottlingPriority.Extreme" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size48x48
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailSize.Size48x48" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size180x180
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailSize.Size180x180" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size420x420
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailSize.Size420x420" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size60x60
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailSize.Size60x60" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size100x100
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailSize.Size100x100" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size150x150
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailSize.Size150x150" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailSize
    if not enumObj then
        return false
    end
    local item = enumObj.Size352x352
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailSize.Size352x352" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailType
    if not enumObj then
        return false
    end
    local item = enumObj.HeadShot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailType.HeadShot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarBust
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailType.AvatarBust" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ThumbnailType
    if not enumObj then
        return false
    end
    local item = enumObj.AvatarThumbnail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ThumbnailType.AvatarThumbnail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TickCountSampleMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Fast
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TickCountSampleMethod.Fast" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TickCountSampleMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Benchmark
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TickCountSampleMethod.Benchmark" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TickCountSampleMethod
    if not enumObj then
        return false
    end
    local item = enumObj.Precise
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TickCountSampleMethod.Precise" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TonemapperPreset
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TonemapperPreset.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TonemapperPreset
    if not enumObj then
        return false
    end
    local item = enumObj.Retro
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TonemapperPreset.Retro" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TopBottom
    if not enumObj then
        return false
    end
    local item = enumObj.Top
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TopBottom.Top" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TopBottom
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TopBottom.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TopBottom
    if not enumObj then
        return false
    end
    local item = enumObj.Bottom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TopBottom.Bottom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchCameraMovementMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Classic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchCameraMovementMode.Classic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Follow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchCameraMovementMode.Follow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchCameraMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Orbital
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchCameraMovementMode.Orbital" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchMovementMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbstick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchMovementMode.Thumbstick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.DPad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchMovementMode.DPad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.Thumbpad
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchMovementMode.Thumbpad" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.ClickToMove
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchMovementMode.ClickToMove" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TouchMovementMode
    if not enumObj then
        return false
    end
    local item = enumObj.DynamicThumbstick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TouchMovementMode.DynamicThumbstick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.Ok
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.Ok" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.NoService
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.NoService" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.InitFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.InitFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.NoVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.NoVideo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.VideoError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.VideoError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.VideoNoPermission
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.VideoNoPermission" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.VideoUnsupported
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.VideoUnsupported" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.NoAudio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.NoAudio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.AudioError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.AudioError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.AudioNoPermission
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.AudioNoPermission" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerError
    if not enumObj then
        return false
    end
    local item = enumObj.UnsupportedDevice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerError.UnsupportedDevice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerExtrapolationFlagMode
    if not enumObj then
        return false
    end
    local item = enumObj.ForceDisabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerExtrapolationFlagMode.ForceDisabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerExtrapolationFlagMode
    if not enumObj then
        return false
    end
    local item = enumObj.ExtrapolateFacsAndPose
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TrackerExtrapolationFlagMode.ExtrapolateFacsAndPose"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerExtrapolationFlagMode
    if not enumObj then
        return false
    end
    local item = enumObj.ExtrapolateFacsOnly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TrackerExtrapolationFlagMode.ExtrapolateFacsOnly"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerExtrapolationFlagMode
    if not enumObj then
        return false
    end
    local item = enumObj.Auto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerExtrapolationFlagMode.Auto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerFaceTrackingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FaceTrackingSuccess
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.TrackerFaceTrackingStatus.FaceTrackingSuccess"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerFaceTrackingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FaceTrackingNoFaceFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TrackerFaceTrackingStatus.FaceTrackingNoFaceFound"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerFaceTrackingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FaceTrackingUnknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.TrackerFaceTrackingStatus.FaceTrackingUnknown"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerFaceTrackingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FaceTrackingLost
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerFaceTrackingStatus.FaceTrackingLost" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerFaceTrackingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FaceTrackingHasTrackingError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TrackerFaceTrackingStatus.FaceTrackingHasTrackingError"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerFaceTrackingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FaceTrackingIsOccluded
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TrackerFaceTrackingStatus.FaceTrackingIsOccluded"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerFaceTrackingStatus
    if not enumObj then
        return false
    end
    local item = enumObj.FaceTrackingUninitialized
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.TrackerFaceTrackingStatus.FaceTrackingUninitialized"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerLodFlagMode
    if not enumObj then
        return false
    end
    local item = enumObj.ForceFalse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerLodFlagMode.ForceFalse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerLodFlagMode
    if not enumObj then
        return false
    end
    local item = enumObj.ForceTrue
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerLodFlagMode.ForceTrue" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerLodFlagMode
    if not enumObj then
        return false
    end
    local item = enumObj.Auto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerLodFlagMode.Auto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerLodValueMode
    if not enumObj then
        return false
    end
    local item = enumObj.Force0
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerLodValueMode.Force0" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerLodValueMode
    if not enumObj then
        return false
    end
    local item = enumObj.Force1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerLodValueMode.Force1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerLodValueMode
    if not enumObj then
        return false
    end
    local item = enumObj.Auto
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerLodValueMode.Auto" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Audio
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerMode.Audio" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Video
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerMode.Video" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerMode
    if not enumObj then
        return false
    end
    local item = enumObj.AudioVideo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerMode.AudioVideo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerPromptEvent
    if not enumObj then
        return false
    end
    local item = enumObj.LODCameraRecommendDisable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.TrackerPromptEvent.LODCameraRecommendDisable"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerType
    if not enumObj then
        return false
    end
    local item = enumObj.Face
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerType.Face" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TrackerType
    if not enumObj then
        return false
    end
    local item = enumObj.UpperBody
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TrackerType.UpperBody" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TriStateBoolean
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TriStateBoolean.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TriStateBoolean
    if not enumObj then
        return false
    end
    local item = enumObj.True
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TriStateBoolean.True" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TriStateBoolean
    if not enumObj then
        return false
    end
    local item = enumObj.False
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TriStateBoolean.False" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TweenStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Canceled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TweenStatus.Canceled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.TweenStatus
    if not enumObj then
        return false
    end
    local item = enumObj.Completed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.TweenStatus.Completed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UICaptureMode
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UICaptureMode.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UICaptureMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UICaptureMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorBoundingBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorBoundingBehavior.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorBoundingBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.EntireObject
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorBoundingBehavior.EntireObject" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorBoundingBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.HitPoint
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorBoundingBehavior.HitPoint" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragRelativity
    if not enumObj then
        return false
    end
    local item = enumObj.Absolute
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragRelativity.Absolute" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragRelativity
    if not enumObj then
        return false
    end
    local item = enumObj.Relative
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragRelativity.Relative" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragSpace
    if not enumObj then
        return false
    end
    local item = enumObj.Parent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragSpace.Parent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragSpace
    if not enumObj then
        return false
    end
    local item = enumObj.LayerCollector
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragSpace.LayerCollector" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragSpace
    if not enumObj then
        return false
    end
    local item = enumObj.Reference
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragSpace.Reference" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.TranslatePlane
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragStyle.TranslatePlane" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.TranslateLine
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragStyle.TranslateLine" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Rotate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragStyle.Rotate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorDragStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Scriptable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorDragStyle.Scriptable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorResponseStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Offset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorResponseStyle.Offset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorResponseStyle
    if not enumObj then
        return false
    end
    local item = enumObj.Scale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorResponseStyle.Scale" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorResponseStyle
    if not enumObj then
        return false
    end
    local item = enumObj.CustomOffset
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorResponseStyle.CustomOffset" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragDetectorResponseStyle
    if not enumObj then
        return false
    end
    local item = enumObj.CustomScale
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragDetectorResponseStyle.CustomScale" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragSpeedAxisMapping
    if not enumObj then
        return false
    end
    local item = enumObj.XY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragSpeedAxisMapping.XY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragSpeedAxisMapping
    if not enumObj then
        return false
    end
    local item = enumObj.XX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragSpeedAxisMapping.XX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIDragSpeedAxisMapping
    if not enumObj then
        return false
    end
    local item = enumObj.YY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIDragSpeedAxisMapping.YY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexAlignment.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Fill
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexAlignment.Fill" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.SpaceAround
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexAlignment.SpaceAround" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.SpaceBetween
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexAlignment.SpaceBetween" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.SpaceEvenly
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexAlignment.SpaceEvenly" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexMode
    if not enumObj then
        return false
    end
    local item = enumObj.Grow
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexMode.Grow" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexMode
    if not enumObj then
        return false
    end
    local item = enumObj.Shrink
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexMode.Shrink" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexMode
    if not enumObj then
        return false
    end
    local item = enumObj.Fill
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexMode.Fill" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UIFlexMode
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UIFlexMode.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UITheme
    if not enumObj then
        return false
    end
    local item = enumObj.Light
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UITheme.Light" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UITheme
    if not enumObj then
        return false
    end
    local item = enumObj.Dark
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UITheme.Dark" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UiMessageType
    if not enumObj then
        return false
    end
    local item = enumObj.UiMessageError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UiMessageType.UiMessageError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UiMessageType
    if not enumObj then
        return false
    end
    local item = enumObj.UiMessageInfo
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UiMessageType.UiMessageInfo" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UpdateState
    if not enumObj then
        return false
    end
    local item = enumObj.UpdateNotAvailable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UpdateState.UpdateNotAvailable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UpdateState
    if not enumObj then
        return false
    end
    local item = enumObj.UpdateAvailable
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UpdateState.UpdateAvailable" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UpdateState
    if not enumObj then
        return false
    end
    local item = enumObj.UpdateInProgress
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UpdateState.UpdateInProgress" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UpdateState
    if not enumObj then
        return false
    end
    local item = enumObj.UpdateReady
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UpdateState.UpdateReady" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UpdateState
    if not enumObj then
        return false
    end
    local item = enumObj.UpdateFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UpdateState.UpdateFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.NeedPermission
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.NeedPermission" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.CaptureModerated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.CaptureModerated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.CaptureNotInGallery
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.CaptureNotInGallery" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.IneligibleCapture
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.IneligibleCapture" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadQuotaReached
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.UploadQuotaReached" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadPending
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.UploadPending" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UploadCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.UploadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UploadCaptureResult.UploadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UsageContext
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UsageContext.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UsageContext
    if not enumObj then
        return false
    end
    local item = enumObj.Preview
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UsageContext.Preview" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserCFrame
    if not enumObj then
        return false
    end
    local item = enumObj.Head
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserCFrame.Head" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserCFrame
    if not enumObj then
        return false
    end
    local item = enumObj.LeftHand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserCFrame.LeftHand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserCFrame
    if not enumObj then
        return false
    end
    local item = enumObj.RightHand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserCFrame.RightHand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserCFrame
    if not enumObj then
        return false
    end
    local item = enumObj.Floor
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserCFrame.Floor" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputState
    if not enumObj then
        return false
    end
    local item = enumObj.Begin
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputState.Begin" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputState
    if not enumObj then
        return false
    end
    local item = enumObj.Change
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputState.Change" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputState
    if not enumObj then
        return false
    end
    local item = enumObj.End
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputState.End" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputState
    if not enumObj then
        return false
    end
    local item = enumObj.Cancel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputState.Cancel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputState
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputState.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.MouseButton1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.MouseButton1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.MouseButton2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.MouseButton2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.MouseButton3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.MouseButton3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.MouseWheel
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.MouseWheel" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.MouseMovement
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.MouseMovement" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Touch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Touch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Keyboard
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Keyboard" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Focus
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Focus" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Accelerometer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Accelerometer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gyro
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gyro" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad1
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad1" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad2
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad2" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad3
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad3" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad4
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad4" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad5
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad5" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad6
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad6" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad7
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad7" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.Gamepad8
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.Gamepad8" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.TextInput
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.TextInput" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.InputMethod
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.InputMethod" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.UserInputType
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.UserInputType.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRComfortSetting
    if not enumObj then
        return false
    end
    local item = enumObj.Comfort
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRComfortSetting.Comfort" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRComfortSetting
    if not enumObj then
        return false
    end
    local item = enumObj.Normal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRComfortSetting.Normal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRComfortSetting
    if not enumObj then
        return false
    end
    local item = enumObj.Expert
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRComfortSetting.Expert" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRComfortSetting
    if not enumObj then
        return false
    end
    local item = enumObj.Custom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRComfortSetting.Custom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRControllerModelMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRControllerModelMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRControllerModelMode
    if not enumObj then
        return false
    end
    local item = enumObj.Transparent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRControllerModelMode.Transparent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRDeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRDeviceType.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRDeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.OculusRift
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRDeviceType.OculusRift" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRDeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.HTCVive
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRDeviceType.HTCVive" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRDeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.ValveIndex
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRDeviceType.ValveIndex" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRDeviceType
    if not enumObj then
        return false
    end
    local item = enumObj.OculusQuest
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRDeviceType.OculusQuest" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRLaserPointerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRLaserPointerMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRLaserPointerMode
    if not enumObj then
        return false
    end
    local item = enumObj.Pointer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRLaserPointerMode.Pointer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRLaserPointerMode
    if not enumObj then
        return false
    end
    local item = enumObj.DualPointer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRLaserPointerMode.DualPointer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSafetyBubbleMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSafetyBubbleMode.NoOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSafetyBubbleMode
    if not enumObj then
        return false
    end
    local item = enumObj.OnlyFriends
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSafetyBubbleMode.OnlyFriends" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSafetyBubbleMode
    if not enumObj then
        return false
    end
    local item = enumObj.Anyone
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSafetyBubbleMode.Anyone" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRScaling
    if not enumObj then
        return false
    end
    local item = enumObj.World
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRScaling.World" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRScaling
    if not enumObj then
        return false
    end
    local item = enumObj.Off
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRScaling.Off" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSessionState
    if not enumObj then
        return false
    end
    local item = enumObj.Undefined
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSessionState.Undefined" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSessionState
    if not enumObj then
        return false
    end
    local item = enumObj.Idle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSessionState.Idle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSessionState
    if not enumObj then
        return false
    end
    local item = enumObj.Visible
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSessionState.Visible" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSessionState
    if not enumObj then
        return false
    end
    local item = enumObj.Focused
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSessionState.Focused" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRSessionState
    if not enumObj then
        return false
    end
    local item = enumObj.Stopping
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRSessionState.Stopping" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRTouchpad
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRTouchpad.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRTouchpad
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRTouchpad.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRTouchpadMode
    if not enumObj then
        return false
    end
    local item = enumObj.Touch
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRTouchpadMode.Touch" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRTouchpadMode
    if not enumObj then
        return false
    end
    local item = enumObj.VirtualThumbstick
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRTouchpadMode.VirtualThumbstick" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VRTouchpadMode
    if not enumObj then
        return false
    end
    local item = enumObj.ABXY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VRTouchpadMode.ABXY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VelocityConstraintMode
    if not enumObj then
        return false
    end
    local item = enumObj.Line
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VelocityConstraintMode.Line" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VelocityConstraintMode
    if not enumObj then
        return false
    end
    local item = enumObj.Plane
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VelocityConstraintMode.Plane" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VelocityConstraintMode
    if not enumObj then
        return false
    end
    local item = enumObj.Vector
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VelocityConstraintMode.Vector" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VerticalAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Center
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VerticalAlignment.Center" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VerticalAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Top
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VerticalAlignment.Top" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VerticalAlignment
    if not enumObj then
        return false
    end
    local item = enumObj.Bottom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VerticalAlignment.Bottom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VerticalScrollBarPosition
    if not enumObj then
        return false
    end
    local item = enumObj.Right
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VerticalScrollBarPosition.Right" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VerticalScrollBarPosition
    if not enumObj then
        return false
    end
    local item = enumObj.Left
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VerticalScrollBarPosition.Left" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VibrationMotor
    if not enumObj then
        return false
    end
    local item = enumObj.Large
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VibrationMotor.Large" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VibrationMotor
    if not enumObj then
        return false
    end
    local item = enumObj.Small
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VibrationMotor.Small" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VibrationMotor
    if not enumObj then
        return false
    end
    local item = enumObj.LeftTrigger
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VibrationMotor.LeftTrigger" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VibrationMotor
    if not enumObj then
        return false
    end
    local item = enumObj.RightTrigger
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VibrationMotor.RightTrigger" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VibrationMotor
    if not enumObj then
        return false
    end
    local item = enumObj.LeftHand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VibrationMotor.LeftHand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VibrationMotor
    if not enumObj then
        return false
    end
    local item = enumObj.RightHand
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VibrationMotor.RightHand" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.OtherError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureResult.OtherError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.ScreenSizeChanged
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureResult.ScreenSizeChanged" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureResult
    if not enumObj then
        return false
    end
    local item = enumObj.TimeLimitReached
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureResult.TimeLimitReached" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureStartedResult
    if not enumObj then
        return false
    end
    local item = enumObj.Success
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureStartedResult.Success" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureStartedResult
    if not enumObj then
        return false
    end
    local item = enumObj.OtherError
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureStartedResult.OtherError" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureStartedResult
    if not enumObj then
        return false
    end
    local item = enumObj.CapturingAlready
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureStartedResult.CapturingAlready" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureStartedResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoDeviceSupport
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureStartedResult.NoDeviceSupport" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoCaptureStartedResult
    if not enumObj then
        return false
    end
    local item = enumObj.NoSpaceOnDevice
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoCaptureStartedResult.NoSpaceOnDevice" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoDeviceCaptureQuality
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoDeviceCaptureQuality.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoDeviceCaptureQuality
    if not enumObj then
        return false
    end
    local item = enumObj.Low
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoDeviceCaptureQuality.Low" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoDeviceCaptureQuality
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoDeviceCaptureQuality.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoDeviceCaptureQuality
    if not enumObj then
        return false
    end
    local item = enumObj.High
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoDeviceCaptureQuality.High" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.Ok
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.Ok" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.Eof
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.Eof" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.EAgain
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.EAgain" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.BadParameter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.BadParameter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.AllocFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.AllocFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.CodecInitFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.CodecInitFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.CodecCloseFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.CodecCloseFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.DecodeFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.DecodeFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.ParsingFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.ParsingFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.Unsupported
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.Unsupported" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.Generic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.Generic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.DownloadFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.DownloadFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.StreamNotFound
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.StreamNotFound" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.EncodeFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.EncodeFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.CreateFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.CreateFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.NoPermission
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.NoPermission" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.NoService
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.NoService" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.ReleaseFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.ReleaseFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoError
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoError.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoSampleSize
    if not enumObj then
        return false
    end
    local item = enumObj.Small
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoSampleSize.Small" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoSampleSize
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoSampleSize.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoSampleSize
    if not enumObj then
        return false
    end
    local item = enumObj.Large
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoSampleSize.Large" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VideoSampleSize
    if not enumObj then
        return false
    end
    local item = enumObj.Full
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VideoSampleSize.Full" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ViewMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ViewMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ViewMode
    if not enumObj then
        return false
    end
    local item = enumObj.GeometryComplexity
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ViewMode.GeometryComplexity" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ViewMode
    if not enumObj then
        return false
    end
    local item = enumObj.Transparent
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ViewMode.Transparent" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ViewMode
    if not enumObj then
        return false
    end
    local item = enumObj.Decal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ViewMode.Decal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VirtualCursorMode
    if not enumObj then
        return false
    end
    local item = enumObj.Default
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VirtualCursorMode.Default" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VirtualCursorMode
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VirtualCursorMode.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VirtualCursorMode
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VirtualCursorMode.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VirtualInputMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VirtualInputMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VirtualInputMode
    if not enumObj then
        return false
    end
    local item = enumObj.Recording
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VirtualInputMode.Recording" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VirtualInputMode
    if not enumObj then
        return false
    end
    local item = enumObj.Playing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VirtualInputMode.Playing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatDistanceAttenuationType
    if not enumObj then
        return false
    end
    local item = enumObj.Inverse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatDistanceAttenuationType.Inverse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatDistanceAttenuationType
    if not enumObj then
        return false
    end
    local item = enumObj.Legacy
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatDistanceAttenuationType.Legacy" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatState
    if not enumObj then
        return false
    end
    local item = enumObj.Idle
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatState.Idle" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatState
    if not enumObj then
        return false
    end
    local item = enumObj.Joining
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatState.Joining" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatState
    if not enumObj then
        return false
    end
    local item = enumObj.JoiningRetry
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatState.JoiningRetry" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatState
    if not enumObj then
        return false
    end
    local item = enumObj.Joined
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatState.Joined" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatState
    if not enumObj then
        return false
    end
    local item = enumObj.Leaving
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatState.Leaving" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatState
    if not enumObj then
        return false
    end
    local item = enumObj.Ended
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatState.Ended" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceChatState
    if not enumObj then
        return false
    end
    local item = enumObj.Failed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceChatState.Failed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.ClientNetworkDisconnected
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item)
        ~= "Enum.VoiceClientLeaveReasons.ClientNetworkDisconnected"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.PlayerLeft
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.PlayerLeft" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.ClientShutdown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.ClientShutdown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.PublishFailed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.PublishFailed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.RejoinReceived
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.RejoinReceived" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.VoiceReboot
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.VoiceReboot" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.ImguiDebugLeave
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.ImguiDebugLeave" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceClientLeaveReasons
    if not enumObj then
        return false
    end
    local item = enumObj.LuaInitiated
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceClientLeaveReasons.LuaInitiated" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceControlPath
    if not enumObj then
        return false
    end
    local item = enumObj.Publish
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceControlPath.Publish" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceControlPath
    if not enumObj then
        return false
    end
    local item = enumObj.Subscribe
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceControlPath.Subscribe" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceControlPath
    if not enumObj then
        return false
    end
    local item = enumObj.Join
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceControlPath.Join" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceRccReconnectReason
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceRccReconnectReason.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceRccReconnectReason
    if not enumObj then
        return false
    end
    local item = enumObj.Migration
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceRccReconnectReason.Migration" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceRccReconnectReason
    if not enumObj then
        return false
    end
    local item = enumObj.CloseRoom
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceRccReconnectReason.CloseRoom" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VoiceRccReconnectReason
    if not enumObj then
        return false
    end
    local item = enumObj.FAEUpdate
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VoiceRccReconnectReason.FAEUpdate" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VolumetricAudio
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VolumetricAudio.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VolumetricAudio
    if not enumObj then
        return false
    end
    local item = enumObj.Automatic
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VolumetricAudio.Automatic" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.VolumetricAudio
    if not enumObj then
        return false
    end
    local item = enumObj.Enabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.VolumetricAudio.Enabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterDirection
    if not enumObj then
        return false
    end
    local item = enumObj.NegX
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterDirection.NegX" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterDirection
    if not enumObj then
        return false
    end
    local item = enumObj.X
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterDirection.X" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterDirection
    if not enumObj then
        return false
    end
    local item = enumObj.NegY
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterDirection.NegY" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Y
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterDirection.Y" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterDirection
    if not enumObj then
        return false
    end
    local item = enumObj.NegZ
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterDirection.NegZ" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterDirection
    if not enumObj then
        return false
    end
    local item = enumObj.Z
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterDirection.Z" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterForce
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterForce.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterForce
    if not enumObj then
        return false
    end
    local item = enumObj.Small
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterForce.Small" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterForce
    if not enumObj then
        return false
    end
    local item = enumObj.Medium
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterForce.Medium" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterForce
    if not enumObj then
        return false
    end
    local item = enumObj.Strong
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterForce.Strong" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WaterForce
    if not enumObj then
        return false
    end
    local item = enumObj.Max
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WaterForce.Max" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebSocketState
    if not enumObj then
        return false
    end
    local item = enumObj.Connecting
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebSocketState.Connecting" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebSocketState
    if not enumObj then
        return false
    end
    local item = enumObj.Open
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebSocketState.Open" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebSocketState
    if not enumObj then
        return false
    end
    local item = enumObj.Closing
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebSocketState.Closing" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebSocketState
    if not enumObj then
        return false
    end
    local item = enumObj.Closed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebSocketState.Closed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebStreamClientState
    if not enumObj then
        return false
    end
    local item = enumObj.Connecting
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebStreamClientState.Connecting" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebStreamClientState
    if not enumObj then
        return false
    end
    local item = enumObj.Open
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebStreamClientState.Open" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebStreamClientState
    if not enumObj then
        return false
    end
    local item = enumObj.Error
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebStreamClientState.Error" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebStreamClientState
    if not enumObj then
        return false
    end
    local item = enumObj.Closed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebStreamClientState.Closed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebStreamClientType
    if not enumObj then
        return false
    end
    local item = enumObj.SSE
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebStreamClientType.SSE" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebStreamClientType
    if not enumObj then
        return false
    end
    local item = enumObj.RawStream
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebStreamClientType.RawStream" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WebStreamClientType
    if not enumObj then
        return false
    end
    local item = enumObj.WebSocket
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WebStreamClientType.WebSocket" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WeldConstraintPreserve
    if not enumObj then
        return false
    end
    local item = enumObj.All
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WeldConstraintPreserve.All" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WeldConstraintPreserve
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WeldConstraintPreserve.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WeldConstraintPreserve
    if not enumObj then
        return false
    end
    local item = enumObj.Touching
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WeldConstraintPreserve.Touching" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhenUserFirstPlayed
    if not enumObj then
        return false
    end
    local item = enumObj.Unknown
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhenUserFirstPlayed.Unknown" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhenUserFirstPlayed
    if not enumObj then
        return false
    end
    local item = enumObj.Days0To30
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhenUserFirstPlayed.Days0To30" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhenUserFirstPlayed
    if not enumObj then
        return false
    end
    local item = enumObj.Days31To90
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhenUserFirstPlayed.Days31To90" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhenUserFirstPlayed
    if not enumObj then
        return false
    end
    local item = enumObj.Days91To180
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhenUserFirstPlayed.Days91To180" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhenUserFirstPlayed
    if not enumObj then
        return false
    end
    local item = enumObj.Days181To365
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhenUserFirstPlayed.Days181To365" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhenUserFirstPlayed
    if not enumObj then
        return false
    end
    local item = enumObj.Days366Plus
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhenUserFirstPlayed.Days366Plus" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhisperChatPrivacyMode
    if not enumObj then
        return false
    end
    local item = enumObj.AllUsers
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhisperChatPrivacyMode.AllUsers" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WhisperChatPrivacyMode
    if not enumObj then
        return false
    end
    local item = enumObj.NoOne
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WhisperChatPrivacyMode.NoOne" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerAutoSkin
    if not enumObj then
        return false
    end
    local item = enumObj.Disabled
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerAutoSkin.Disabled" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerAutoSkin
    if not enumObj then
        return false
    end
    local item = enumObj.EnabledPreserve
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerAutoSkin.EnabledPreserve" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerAutoSkin
    if not enumObj then
        return false
    end
    local item = enumObj.EnabledOverride
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerAutoSkin.EnabledOverride" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.BoundCage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.BoundCage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.LayerCage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.LayerCage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.BoundCageAndLinks
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.BoundCageAndLinks" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.Reference
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.Reference" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.Rbf
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.Rbf" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.OuterCage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.OuterCage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.ReferenceMeshAfterMorph
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.ReferenceMeshAfterMorph" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.HSROuterDetail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.HSROuterDetail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.HSROuter
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.HSROuter" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.HSRInner
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.HSRInner" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.HSRInnerReverse
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.HSRInnerReverse" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.LayerCageFittedToBase
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.LayerCageFittedToBase" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.LayerCageFittedToPrev
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.LayerCageFittedToPrev" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.PreWrapDeformerOuterCage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.PreWrapDeformerOuterCage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapLayerDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.SkinningTransfer
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapLayerDebugMode.SkinningTransfer" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.None
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.None" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.TargetCageOriginal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.TargetCageOriginal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.TargetCageCompressed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.TargetCageCompressed" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.TargetCageInterface
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.TargetCageInterface" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.TargetLayerCageOriginal
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.TargetLayerCageOriginal" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.TargetLayerCageCompressed
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if
        tostring(item) ~= "Enum.WrapTargetDebugMode.TargetLayerCageCompressed"
    then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.TargetLayerInterface
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.TargetLayerInterface" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.Rbf
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.Rbf" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.OuterCageDetail
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.OuterCageDetail" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.WrapTargetDebugMode
    if not enumObj then
        return false
    end
    local item = enumObj.PreWrapDeformerCage
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.WrapTargetDebugMode.PreWrapDeformerCage" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ZIndexBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Global
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ZIndexBehavior.Global" then
        return false
    end
    return true
end)
addCheck(function()
    local enumObj = Enum.ZIndexBehavior
    if not enumObj then
        return false
    end
    local item = enumObj.Sibling
    if not item then
        return false
    end
    if item.Value == nil then
        return false
    end
    if tostring(item) ~= "Enum.ZIndexBehavior.Sibling" then
        return false
    end
    return true
end)
local passed = 0
local failed = 0
for i, func in ipairs(zalk) do
    local ok, result = pcall(func)
    if ok and result then
        passed = passed + 1
    else
        failed = failed + 1
    end
end
local total = passed + failed
local percent = (passed / total) * 100
print("pass to fail ratio bleh: " .. string.format("%.2f%%", percent))
print("failed dis many --> " .. tostring(failed))
print("passed dis many --> " .. tostring(passed))
-- Rating
local rating = ""
if percent < 10 then
    rating = "wrap it up g"
elseif percent < 20 then
    rating = "bro ts buns"
elseif percent < 30 then
    rating = "yo shi is shitty g"
elseif percent < 40 then
    rating = "eh mid wouldnt recommend using this env"
elseif percent < 50 then
    rating = "eh theres better envs like https://aspectt.lol"
elseif percent < 60 then
    rating = "not bad but still not good.."
elseif percent < 70 then
    rating = "oo not bad still under 70%"
elseif percent < 80 then
    rating = "pretty okay!"
elseif percent < 90 then
    rating = "average environment logger! congrats"
else
    rating = "wowie u got 100% impressive! https://aspectt.lol!"
end
print(rating)
